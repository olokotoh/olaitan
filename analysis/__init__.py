"""The Olaitan statistical analysis pipeline (Story 5.5).

The FIRST Python in an otherwise-Go repo: a pure-Python, NO-CLUSTER, OFFLINE
pipeline (``analyse.py`` + ``lib/``) that consumes the Story-5.4 run artefacts
and produces the pre-registered inferential statistics. See ``analysis/README.md``.
"""
