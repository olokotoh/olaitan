# Story 5.5: the runtime dependency set for analysis/analyse.py.
#
# The FIRST Python toolchain in an otherwise-Go repo (BI-1). The set is the
# minimal honest one for the pre-registered statistics; every version is PINNED
# exactly (the manifest-hash reproducibility discipline extends to the
# toolchain). NO deep-learning, NO plotting at CI (matplotlib is pulled
# transitively by pingouin but is NOT imported by the CI path, OQ3).
#
# Direct dependencies (the resolver pins the transitive tree):
#   pandas           the AC1 per-cell aggregation.
#   scipy            Kruskal-Wallis, Wilcoxon, the one-sided Poisson rate test.
#   statsmodels      McNemar (contingency_tables.mcnemar) + multipletests.
#   scikit-posthocs  Dunn's post-hoc with Holm (posthoc_dunn p_adjust='holm').
#   pingouin         ICC(2,k) (intraclass_corr; OQ2 chose the library over a
#                    hand-rolled variance-component ICC for correctness).
#   PyYAML           reading the Story-5.4 metadata.yaml.
pandas==3.0.3
scipy==1.17.1
statsmodels==0.14.6
scikit-posthocs==0.14.0
pingouin==0.6.1
PyYAML==6.0.3
