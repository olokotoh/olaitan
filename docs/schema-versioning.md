# Schema Versioning

This document records additive, semver-bounded changes to the canonical
event and posture schemas in `internal/schema/`. Architecture reference:
architecture.md:130 (schema versioning policy).

The Olaitan project follows semver for the on-the-wire schema:

- **MAJOR** bumps require a coordinated stream-cutover and break the
  EVENTS.raw consumer contract.
- **MINOR** bumps add fields that existing consumers may safely ignore
  (json `omitempty` so the pre-bump wire form remains byte-identical
  for the steady-state case).
- **PATCH** bumps adjust documentation, comments, or examples and do
  not touch the marshalled bytes.

| Date | Story | Version | Change | Field(s) | Backwards compatible? |
|---|---|---|---|---|---|
| 2026-05-15 | 1.13 | MINOR | Added per-event sampling annotation for the per-source rate-limit circuit breaker. | `Event.Sampled` (`bool`, `json:"sampled,omitempty"`); `Event.SamplingRate` (`float64`, `json:"sampling_rate,omitempty"`) | Yes. `omitempty` keeps unsampled events bit-identical to the pre-1.13 wire form; the EVENTS.raw `MaxMsgSize` budget is unaffected. |
| 2026-05-29 | 2.2 | MINOR | Extended `StateTransition` with the FSM provenance fields (BI-2). The on-wire shape is mirrored in `docs/schemas/state_transition.yaml` (`state_transition.v2`). | `StateTransition.WorkloadID` (`string`, `json:"workload_id,omitempty"`); `StateTransition.PackageID` (`string`, `json:"package_id,omitempty"`); `StateTransition.Reason` (`string`, `json:"reason,omitempty"`) | Yes. All three are `omitempty`, so the pre-2.2 wire form (the Story 1.x `schema.Incident.Transitions` slice) stays byte-identical for the zero value. No existing field was renamed or removed. |
| 2026-05-31 | 2.3 | NEW (storage) | New durable Redis storage format for FSM state persistence (FR37). A Redis hash format defined in `internal/response/fsm/store.go` (`SchemaVersionFSMState = "fsm_state.v1"`), mirrored in `docs/schemas/fsm_state.yaml`. NOT an EVENTS.raw wire type; the field set is the persisted projection of the in-memory FSM `workloadState`. | `fsm:{workload_id}` hash fields: `schema_version`, `current_state`, `state_entered_at_ns`, `cooldown_anchor_ns`, `updated_at_ns` | Yes. First version of this format; no prior persisted state to migrate. An unknown `schema_version` is skipped on load rather than crashing recovery. |
| 2026-06-03 | 2.7 | NEW (storage) | New durable Redis storage format for operator overrides (FR39). A Redis hash defined in `internal/response/override/store.go` (`SchemaVersionOverride = "override.v1"`). Unlike the no-TTL `fsm:` family, the `override:{workload_id}` key carries a NATIVE Redis TTL equal to the requested override duration: the native TTL IS the FR39 release mechanism and is a HARD DEADLINE measured from first application. The reconcile is edge-triggered, so the controller writes the key only on a NEW override or an operator EDIT (a change of `requested_state` or `ttl_seconds`); it does NOT refresh the TTL while an unchanged annotation merely remains present, so the override auto-releases when the TTL elapses even with the annotation still on the workload (AC2). `operator_id` is populated from the optional `olaitan.io/state-override-by` annotation (empty when absent) and is NOT part of the hard-deadline signature (an operator-id change alone does not re-arm the TTL). NOT an EVENTS.raw wire type; the field set is the durable record of an active operator pin. | `override:{workload_id}` hash fields: `schema_version`, `requested_state`, `ttl_seconds`, `operator_id`, `applied_at_ns`, `source` | Yes. First version of this format; no prior persisted state to migrate. An unknown `schema_version` is skipped on `ListActive` rather than aborting the scan. |
| 2026-06-03 | 2.7 | NEW (wire) | New published NATS subject `OVERRIDES.applied` (BI-10, 365-day JetStream retention) carrying one event per applied AND per rejected operator override (FR38/FR39). Event type `override.OverrideApplied` (`schema_version = "override.v1"`), published via `PublishJS` with a `WithMsgID` dedup key. This is the inverse of the Stories 2.4-2.6 "schema-versioning untouched" note: Story 2.7 DOES introduce a new persisted family AND a new wire subject. The append-only `AUDIT.overrides` SIEM mirror is Story 2.8, not here. | `OverrideApplied` fields: `schema_version`, `workload_id`, `requested_state`, `before_state`, `ttl_seconds`, `operator_id`, `source`, `rejected`, `reason`, `published_at`, `applied_at_ns` | Yes. First version of this subject; no prior consumers. Rejection events carry `rejected:true` + a `reason` (`state_unavailable`/`invalid_state`); applied events carry `rejected:false` and an empty `reason`. |
| 2026-06-04 | 2.8 | NEW (wire) | New append-only SIEM subject `AUDIT.transitions` (`audit.transitions.v1`, 90-day LimitsPolicy JetStream retention, FR40/NFR16). Event type `audit.AuditTransition`, published by a non-blocking `fsm.TransitionSink` appended to the existing MultiSink so every actual FSM state change (including operator pins, BI-1/BI-10) is audited. Field-name normalisation is literal on the wire (`before_state`/`after_state`/`triggering_threat_score`). **Retention reconciliation (BI-7):** architecture.md:239 generalises "AUDIT.* 365 d", but AC4 is more specific and binding for transitions (90 d, matching `STATE_TRANSITIONS.applied`); 90 d is what ships. Authoritative schema: `docs/schemas/audit/transitions.json`. | `AuditTransition` fields: `schema_version`, `before_state`, `after_state`, `triggering_threat_score`, `package_id`, `workload_id`, `trigger_type`, `reason`, `operator_id`, `decided_at`, `published_at` | Yes. First version; no prior consumers. `decided_at` (FSM decision time) and `published_at` (audit-emit time) are deliberately distinct (BI-1.3). |
| 2026-06-04 | 2.8 | NEW (wire) | New append-only SIEM subject `AUDIT.overrides` (`audit.overrides.v1`, 365-day LimitsPolicy retention, FR40), the SIEM sibling of `OVERRIDES.applied`. Event type `override.AuditOverride`, published as a SECOND publish off the override controller at its existing applied + rejection points (BI-2), reusing the operational `WithMsgID` dedup so a poll re-emit does not inflate the audit record. Versions independently of `override.v1`. Authoritative schema: `docs/schemas/audit/overrides.json`. | `AuditOverride` fields: `schema_version`, `workload_id`, `requested_state`, `before_state`, `ttl_seconds`, `operator_id`, `source`, `rejected`, `reason`, `published_at`, `applied_at_ns` | Yes. First version; no prior consumers. An applied override emits BOTH `AUDIT.transitions` (the state change) and `AUDIT.overrides` (the application); a rejection emits only `AUDIT.overrides` (BI-10). |
| 2026-06-04 | 2.8 | NEW (wire) | New append-only SIEM subject `AUDIT.policies` (`audit.policies.v1`, 365-day LimitsPolicy retention, FR40). Event type `audit.AuditPolicy`, published via a new one-method `netpol.PolicyAuditPublisher` seam the manager calls at exactly its real mutation points (apply/supersede_delete/deescalation_remove/gc_delete, BI-9), keeping netpol NATS-free (BI-3/BI-12). `spec_summary` is a compact closed enum, NOT a full-object diff (BI-11); `policy_name`/`policy_kind` are omitted on a set removal. Authoritative schema: `docs/schemas/audit/policies.json`. | `AuditPolicy` fields: `schema_version`, `action`, `workload_id`, `namespace`, `policy_name`, `policy_kind`, `fsm_state`, `result`, `spec_summary`, `package_id`, `published_at` | Yes. First version; no prior consumers. Idempotent noops / NotFound deletes are NOT audited (BI-9, Open Assumption 2). |
| 2026-06-09 | 3.1 | NEW (wire) | New append-only SIEM subject `AUDIT.redactions` (`audit.redactions.v1`, 365-day LimitsPolicy retention, FR41/FR44), the fifth and final SIEM audit subject, completing the architecture's five-subject surface. Event type `redact.AuditRedaction`, published by a buffered non-blocking `redact.RedactionAuditSink` (best-effort, never blocks or fails the redaction, BI-6.2) with a `WithMsgID` dedup keyed on package + field_path + redacted_at. One event per redacted field, carrying `field_path` + `reason` (closed enum `secret_pattern`/`jwt_body`/`raw_payload`/`file_contents`) + `package_id`. **CRITICAL (NFR18, BI-5.3):** the event carries NO secret value (no value/before field); it records WHERE and WHY a redaction happened, never the redacted secret, so the trail is safe to ship to a SIEM. `redacted_at` (decision time) is distinct from `published_at` (emit time). Retention defaults to 365 d (no AC carves out a shorter window, unlike transitions' 90 d). Authoritative schema: `docs/schemas/audit/redactions.json`. | `AuditRedaction` fields: `schema_version`, `package_id`, `field_path`, `reason`, `workload_id`, `redacted_at`, `published_at` | Yes. First version; no prior consumers. |
| 2026-06-12 | 3.5 | NEW (role schema) | New role schema `L1Hypothesis` (`l1_hypothesis.v1`, FR21), the structured first-pass verdict of the L1 triage analyst. NOT an EVENTS.raw wire type yet: in 3.5 it is the validated in-process output of the L1 runner; Story 3.9 checkpoints it to `INVESTIGATIONS.{package_id}.l1`. `schema_version` is optional on the MODEL side and force-stamped to `l1_hypothesis.v1` by the runner after validation (a model that omits it cannot burn a Story 3.10 retry strike on metadata). The AC3 referential rule (every cited `event_id` exists in the input `EvidencePackage`) is enforced programmatically by the runner. Authoritative schema: `docs/schemas/l1_hypothesis.json` (mirror: `l1_hypothesis.yaml`; embedded runtime copy: `internal/decision/analyst/l1_hypothesis_schema.json`). | `L1Hypothesis` fields: `schema_version` (optional or null, maxLength 64), `hypothesis` (maxLength 2000), `cited_evidence` (array of `EvidenceCitation`: `event_id` maxLength 256, optional `note` maxLength 500; 1-50 items, uniqueItems), `follow_up_probes` (optional, up to 20 x maxLength 500), `confidence` (int in [0,100]) | Yes. New types only; no existing wire shape was touched. Size bounds and null-tolerant `schema_version` added by the Story 3.5 round-1 review before any consumer existed (same `l1_hypothesis.v1`). |
| 2026-06-12 | 3.6 | NEW (role schema) | New role schema `L2Verification` (`l2_verification.v1`, FR23), the structured verification verdict of the L2 analyst over an EvidencePackage plus the Story 3.5 L1Hypothesis. NOT an EVENTS.raw wire type yet: in 3.6 it is the validated in-process output of the L2 runner; Story 3.9 checkpoints it to `INVESTIGATIONS.{package_id}.l2`. `schema_version` optional (or null) on the MODEL side, force-stamped by the runner. Referential rule (verified event_ids exist in the package) and whitespace-only rejection of `finding` and `contradictory_findings` entries are runner-enforced. Size bounds follow the 3.5 convention. The L1Hypothesis travels to L2 on the new optional `provider.Request.PriorHypothesis` field, marshalled + angle-escaped + framed in a `<l1_hypothesis>` block by the shared transport (Story 3.6 BI-2; nil keeps the wire form byte-identical for every pre-3.6 caller). Authoritative schema: `docs/schemas/l2_verification.json` (mirror: `.yaml`; embedded runtime copy: `internal/decision/analyst/l2_verification_schema.json`). | `L2Verification` fields: `schema_version` (optional or null, maxLength 64), `verdict` (enum confirmed/refuted/inconclusive), `verified_evidence` (array of `EvidenceVerification`: `event_id` maxLength 256, `finding` maxLength 500; 1-50 items, uniqueItems), `contradictory_findings` (optional, up to 20 x maxLength 500), `confidence` (int in [0,100]) | Yes. New types plus one ADDITIVE optional field on the in-process `provider.Request`; no existing wire shape was touched. |
| 2026-06-12 | 3.7 | NEW (role schema) | New role schema for the SENIOR MODEL-FACING verdict (`threat_assessment.v1`, FR24): threat_type, reasoning, mitre_techniques (pattern `T####(.###)`, <=20), noted_disagreements (<=20 x 500), kill_chain_stage, RAW confidence int [0,100]. Validated by the Story 3.7 Senior runner; the orchestrator combines it with the cap arithmetic into the persisted `schema.ThreatAssessment`. Authoritative: `docs/schemas/threat_assessment.json` (mirror `.yaml`; embedded copy in internal/decision/analyst). The L1Hypothesis and L2Verification travel to the Senior on `provider.Request.PriorHypothesis` and the new ADDITIVE `provider.Request.PriorVerification` (escaped `<l2_verification>` block; nil keeps pre-3.7 wire form byte-identical). | `SeniorVerdict` fields: `schema_version` (optional/null), `threat_type`, `reasoning`, `mitre_techniques`, `noted_disagreements`, `kill_chain_stage`, `confidence` | Yes. New schema; one additive optional Request field. |
| 2026-06-12 | 3.7 | MINOR | Extended the persisted `ThreatAssessment` with four omitempty audit fields (BI-4): the FR24 disagreement record, the AC2 raw/capped confidence pair, and the AC4 sub-agent availability record. `Confidence` (ConfidenceScore fold) and `RecommendedState` stay zero until Story 3.11/FSM. | `ThreatAssessment.NotedDisagreements` (`[]string`, omitempty); `.RawConfidence` (`int`, omitempty); `.LLMCappedConfidence` (`int`, omitempty); `.AgentsAvailable` (`[]string`, omitempty, bounded values l1/l2/senior) | Yes. All omitempty: the pre-3.7 wire form is byte-identical for zero values (pinned by TestThreatAssessmentPre37WireFormUnchanged). |
| 2026-06-13 | 3.8 | NEW (wire) | New append-only SIEM subject `AUDIT.assessments` (`audit.assessments.v1`, the LLM-verdict audit subject, FR53/NFR27/AC4) -- the subject `subjects.go` had reserved for an Epic 3 story. The MINIMAL projection of one investigation-chain run: `agents_available` records which roles ran (its complement is which the ablation skipped) and `mode` records the configured boundary (full / l1_l2 / l1_only). Published synchronously by the Story 3.8 chain consumer with `WithMsgID` keyed on `package_id`; the chain consumer runs off its own JetStream goroutine (decoupled from the FSM hot path), so a bounded synchronous publish is acceptable (unlike the buffered Story 2.8 transition sink). Story 3.14 owns the broader audit pipeline and MAY supersede/extend this payload under a new version. Event type `audit.AuditAssessment`. | `AuditAssessment` fields: `schema_version`, `package_id`, `workload_id`, `mode`, `agents_available`, `threat_type` (omitempty; empty in an ablation mode with no Senior), `raw_confidence`, `llm_capped_confidence`, `decided_at` | Yes. First version; no prior consumers. |
| 2026-06-13 | 3.9 | NEW (wire home) | The existing `L1Hypothesis` (`l1_hypothesis.v1`) and `L2Verification` (`l2_verification.v1`) role schemas now ALSO ride dedicated wire subjects: `INVESTIGATIONS.{package_id}.l1` and `.l2` (FR29), the per-investigation checkpoints that let a controller restart resume the chain. The SCHEMAS are unchanged (no version bump) -- only their wire home is new; the L1/L2 runners stamp the same `*.v1` versions before checkpointing. The new `INVESTIGATIONS` JetStream stream is LimitsPolicy/FileStorage with a 6h default retention (architecture.md:235), Helm-tunable via `analyst.checkpoint_retention`. It is NOT an append-only SIEM audit stream (it is resume data); the checkpoint store publishes with `WithMsgID` = package_id+step so a re-publish is idempotent and `GetLastMsgForSubject` reads the last checkpoint per subject. | `L1Hypothesis`/`L2Verification` unchanged (Story 3.5/3.6 rows above). New wire subjects `INVESTIGATIONS.{id}.{l1,l2}`. | Yes. No schema change; an additive new wire home + stream. No prior consumers of the INVESTIGATIONS subjects. |
| 2026-06-13 | 3.10 | MINOR | Extended the persisted `ThreatAssessment` with one additive omitempty field, `llm_unavailable` (bool, FR26/FR28). The Story 3.10 retry + Ollama-fallback ladder sets it (with `llm_capped_confidence: 0`) when the boundary role's primary and fallback both exhaust, so the FSM (Story 3.11) proceeds on the deterministic ThreatScore only. `Confidence` (ConfidenceScore fold) and `RecommendedState` stay zero until Story 3.11/FSM. | `ThreatAssessment.LLMUnavailable` (`bool`, omitempty) | Yes. Additive omitempty: the pre-3.10 wire form is byte-identical for the false zero value (pinned by `TestThreatAssessmentLLMUnavailableWireForm` and the extended `TestThreatAssessmentPre37WireFormUnchanged`). |
| 2026-06-14 | 4.1 | PATCH (enum extension) | Added one value, `kill_condition_met`, to the `reason` enum of `StateTransition` / `AuditTransition` (the Story 4.1 QUARANTINED -> PRESERVED_KILLED kill transition, FR31/BI-9). The wire `schema_version` stays `audit.transitions.v1` / `state_transition.v2`: the change is an additive enum value, not a field add/rename, so a pre-4.1 consumer that never produces a kill simply never sees it, and the marshalled bytes for every existing transition are unchanged. The `PRESERVED_KILLED` STATE value was already in both state enums since the schema's inception, so no state-enum change was needed. The new value is threaded through all three closed audit enums (`docs/schemas/audit/transitions.json`, `docs/schemas/audit/transitions.yaml`, `docs/schemas/state_transition.yaml`); the source-schema enum `state_transition.yaml` additionally gained the previously-missing `operator_override` value so it matches the authoritative `transitions.json`. | `StateTransition.Reason` / `AuditTransition.reason` enum gains `kill_condition_met` (and `state_transition.yaml` gains `operator_override`) | Yes. Additive enum value; no field added, renamed, or removed; the pre-4.1 wire form is byte-identical for every non-kill transition. Pinned by the non-vacuous drift trap in `TestAuditTransition_KillReasonValidatesAndDriftFails`. |
| 2026-06-14 | 4.2 | NONE (metric surface only) | The forensic capture controller (FR36, documented fallback Path A) introduces NO on-the-wire, persisted, or role-schema change: it captures a `forensic-bundle.tar.gz` to S3 (not an EVENTS.raw/NATS type) and adds three Prometheus metrics only. Recorded here because the metric surface is the observable contract. The S3 object key is `forensic-bundles/<sha256>/forensic-bundle.tar.gz` (content-addressed; the `<incident-id>/` prefix shape from ADR-2026-05-02-01 is deferred until Story 4.3 introduces `INCIDENTS.finalised`). No `internal/schema/` type changed. | Prometheus: `olaitan_forensic_capture_seconds` (histogram), `olaitan_forensic_capture_total{result}` (counter), `olaitan_forensic_writes_deferred_total` (counter). No `internal/schema/` field. | Yes. No wire/persisted/role-schema bytes changed; metrics are additive and consumer-ignorable. |
| 2026-06-14 | 4.3 | NEW (wire) | New published NATS subject `INCIDENTS.finalised` (`incidents.finalised.v1`, FR42/NFR7) on a NEW `INCIDENTS` JetStream stream (the 12th; LimitsPolicy/FileStorage, 365 d audit-grade retention by analogy with the AUDIT_* family, Helm-tunable via `response.settling.retention_days`, `MaxBytes: 0` so it inherits the per-stream cap and adds nothing to the 160 GiB EVENTS/EVENTS_RAW/EVIDENCE budget). The settling-window controller publishes one event when a workload's FSM has been stable in a non-CLEAN state for the settling window (default 60 s); the Story 4.4 DFIR agent consumes it as its invocation trigger (Story 4.3 only produces it, BI-10). This resolves the line-36 deferral: the deferred `<incident-id>/` S3 prefix can now key off the finalisation. Event type `settling.IncidentFinalised`, published via `PublishJS` with `WithMsgID = package_id + ":" + finalised_at_ns` so a drainer re-publish / restart-replay of the SAME finalisation is server-side deduplicated while a NEW incident for the same workload after a CLEAN cycle (distinct `finalised_at`) is NOT suppressed (BI-8). `final_state` is one of the four non-CLEAN states (CLEAN never finalises, AC4); `threat_score` is the `Confidence` on the transition that established the final state, captured at entry and NOT re-sampled (BI-7); `history` is the full FSM transition history, read restart-safe from the Redis `fsm:{workload_id}:history` list when persistence is enabled (Open Assumption 2) else accumulated in-process. Authoritative schema: `docs/schemas/incidents/finalised.json` (mirror `.yaml`). | `IncidentFinalised` fields: `schema_version`, `package_id`, `workload_id`, `final_state`, `threat_score`, `finalised_at`, `history` (`[]schema.StateTransition`) | Yes. First version of this subject; no prior consumers. |
| 2026-06-13 | 3.14 | MINOR | `AUDIT.assessments` payload `audit.assessments.v1` -> `v2`: extends the Story 3.8 MINIMAL projection with the full SIEM trail -- per-role `prompt_versions`/`providers`/`models` maps (Story 3.13 content hashes + FR25 routing), the `l1_hypothesis`/`l2_verification`/`threat_assessment` verdicts, the REDACTED `redacted_evidence` package, and `redaction_applied` + `published_at`. The cmd caller redacts at the audit boundary via the same pure `redact.Redact` the providers ran, so `redacted_evidence` is byte-identical to what the LLM saw and carries no secret values (NFR15/NFR18). Per-role maps and the verdict objects are omitempty so an ablation mode (`l1_only`/`l1_l2`) omits the roles it did not run. Additive (NFR29 MINOR): an older v1 consumer ignores the new fields. Authoritative schema `docs/schemas/audit/assessments.json` (draft 2020-12, additionalProperties:false). `AUDIT_ASSESSMENTS` retention stays 365 d, now Helm-tunable via `response.audit.retention_assessments_days`. | `AuditAssessment` adds `prompt_versions`, `providers`, `models`, `l1_hypothesis`, `l2_verification`, `threat_assessment`, `redacted_evidence`, `redaction_applied`, `published_at` (v1 fields retained). | Yes. Additive over v1; the v1 fields keep their shape, new fields are omitempty/role-keyed. Validated against three committed exemplars + a built event + a drift-rejection set (`TestAssessment*`). |
| 2026-06-14 | 4.4 | NEW (role schema) | New role schema `ForensicReport` (`report.v1`, FR43), the structured DFIR forensic report. Per the JSON->render hybrid (BI-6) the LLM returns a single JSON document conforming to it; the DFIR runner validates it via the Epic-3 jsonschema/v6 pipeline, then RENDERS it deterministically to YAML-front-matter + Markdown (the model never controls raw Markdown). NOT an EVENTS.raw wire type: it is the validated in-process output of the DFIR runner. The deterministic front-matter fields are force-stamped by the runner from the `IncidentFinalised` event + the persisted `ThreatAssessment`/`WorkloadPosture` (AC2, not invented by the model); `attack_techniques` items reuse the established ATT&CK string form `^T[0-9]{4}(\.[0-9]{3})?$` (ATT&CK for Containers v18); `final_fsm_state` excludes CLEAN (AC4). Round-1 review follow-up (anti-hallucination): the model returns ONLY the interpretive `narrative` (renamed from `body`); the factual sections (kill-chain timeline, containment actions, MITRE ATT&CK annotations) are assembled DETERMINISTICALLY by the Go renderer from the `IncidentFinalised` history + the chosen `ThreatAssessment`, so the model cannot fabricate a fact. A whitespace-only `narrative` is runner-rejected. Round-2 review follow-up (model-contract honesty): the schema `required` array is ONLY `["narrative"]` -- a compliant model returns `{"narrative": "..."}` and the runner force-stamps EVERY other (controller-owned) field after validation, so the deterministic front-matter properties stay OPTIONAL on the model contract (they are always present in the rendered report, just never model-supplied; requiring them would fail-close every real narrative-only reply). `contributing_posture_findings` is ALSO force-stamped (derived from the package `WorkloadPosture`, nil in 4.4 prod -> the renderer prints "not recorded"), never model-supplied, closing the posture-hallucination hole. `report.v1` is unreleased; this PR introduces the field-set. Authoritative schema: `docs/schemas/forensic_report.json` (mirror `forensic_report.yaml`; embedded runtime copy: `internal/report/dfir/forensic_report_schema.json`). | `ForensicReport` fields: `schema_version` (optional/null, maxLength 64), `incident_id` (maxLength 256), `final_fsm_state` (enum SUSPICIOUS/RESTRICTED/QUARANTINED/PRESERVED_KILLED), `threat_score_at_decision` (number), `attack_techniques` (optional, up to 20 x ATT&CK pattern), `contributing_posture_findings` (optional, up to 50 x maxLength 500), `containment_actions` (optional, up to 20 x maxLength 256), `report_generated_at` (date-time), `prompt_hash` (maxLength 128), `dfir_provider` (maxLength 64), `dfir_model` (maxLength 128), `narrative` (interpretive prose, maxLength 65536) | Yes. New schema; no existing wire shape was touched. |
| 2026-06-14 | 4.4 | NEW (wire) | New published NATS subject `REPORTS.generated` (`reports.generated.v1`, FR43/AC5): the DFIR-report announcement. Published by the DFIR agent when it has generated a `ForensicReport` for a finalised incident, carrying the report SHA256 and a backend-neutral `report_url` populated with the COMPUTED content-addressed key `reports/{yyyy}/{mm}/{dd}/{sha256}.md` (OA4: the field is NAMED `report_url`, NOT `s3_url`, and maps to AC5's "S3 URL" in provenance; the durable S3-compatible write + KMS + object-lock are Story 4.6). Published via `PublishJS` with `WithMsgID = incident_id + ":" + report_sha256` so a re-announcement of the SAME report is server-side deduplicated. The event carries NO LLM-cappable confidence/score (the trust-bound fence: a post-mortem artifact downstream of the FSM). Event type `dfir.ReportGenerated`. The DFIR call is ALSO recorded in `AUDIT.assessments` via the role-keyed `dfir` maps (mode `dfir`), alongside the l1/l2/senior keys. | `ReportGenerated` fields: `schema_version`, `incident_id`, `workload_id`, `report_sha256`, `report_url`, `final_fsm_state`, `generated_at` | Yes. First version of this subject; no prior consumers. |
| 2026-06-14 | 4.5 | MINOR (additive optional) | Extended `AUDIT.redactions` (`audit.redactions.v1`) with two additive OPTIONAL (omitempty) fields, `source` (closed enum `evidence`/`llm_response`/`posture`/`audit`) and `incident_id` (string), for the Story 4.5 persistence-bound redaction boundary (FR44/NFR15, AC4). The wire `schema_version` STAYS `audit.redactions.v1` (PO ratification OA2): the change is additive-optional, mirroring the Story 4.1 additive precedent, so the Story 3.1 LLM-bound producers leave both fields empty and stay byte-identical on the wire (an empty `source` is treated as `evidence` by the consumer). The persistence path (the DFIR agent's pre-render report redaction, the SHA-of-redacted-bytes reuse of the ONE pattern engine `redact.RedactText`) stamps `source` per report region (narrative -> `llm_response`, posture findings -> `posture`, evidence-derived lists -> `evidence`, audit-metadata fields -> `audit`) and `incident_id` = the finalised `package_id`. SHA-OF-REDACTED-BYTES (OA1): the `REPORTS.generated` `report_sha256` / `report_url` content-address the RENDERED-AND-REDACTED bytes actually persisted (identical to the 4.4 render when the model surfaced no secret; the wire shape of `reports.generated.v1` is unchanged). Threaded through both `docs/schemas/audit/redactions.json` and the `redactions.yaml` mirror. | `AuditRedaction` adds `source` (omitempty enum), `incident_id` (omitempty string); v1 fields retained. | Yes. Additive omitempty: the pre-4.5 LLM-bound wire form is byte-identical for the empty zero value (pinned by `TestAuditRedaction_LLMBoundWireByteIdentical`), and the additive enum is pinned by the non-vacuous drift trap in `TestRedactions_SourceIncidentIDAdditive`. No field renamed or removed. |

## BI-2 reconciliation: redaction lands in Epic 3, Epic 4 only reuses it

The FR-traceability map (epics.md:374) lists "FR44 Redaction policy | Epic 4 |
Pre-LLM, pre-persistence" and the Epic 3 intro (epics.md:421) annotates "NFR15
... DFIR-side completed in Epic 4", which read literally would place all of
FR44/NFR15 in Epic 4. The AUTHORITATIVE source for Story 3.1 is its AC block (an
Epic 3 story, epics.md:1290-1316), which delivers the reusable `Redact()`, the
`AUDIT.redactions` subject, and the gopter 100-percent-LLM-boundary property
HERE. AC5 makes the split explicit: Epic 4 ships NOTHING NEW in redaction logic;
it CALLS the same `Redact()` at the pre-S3-write boundary and EXTENDS the
property suite with persistence-boundary cases. This is a resolved documentation
conflict, NOT scope drift (BI-2).

## How to add a row

1. Edit `internal/schema/event.go` (or the relevant sub-type).
2. Add the field with a `json:"...,omitempty"` tag so consumers
   unaware of the new field marshal a byte-identical wire form for
   the zero value.
3. Add round-trip tests in `internal/schema/schema_test.go` proving
   the new field omits at zero and renders when set.
4. Append a row above with the date, story ID, version bump category,
   one-line description, exact field declaration, and a yes/no on
   backwards compatibility.
5. Note any downstream consumer that should pick the field up in the
   relevant story's Dev Notes hand-off section.
