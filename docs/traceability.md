# Traceability matrix

This file is the single source of truth for the code-to-thesis claim chain
required by NFR42. Every functional requirement, non-functional requirement,
and Chapter 3 methodology claim that lands in the codebase must be reflected
here with a row that points readers from the chapter to the package, the
tests that exercise it, and (where applicable) the evaluation runs that
produced its empirical numbers.

## Column schema (NFR42, verbatim)

The matrix table below carries exactly six columns, matching NFR42:

| Column          | Meaning |
|-----------------|---------|
| `claim_id`      | Stable slug of the form `c<chapter>.<section>[.<sub>]-<short-slug>`, e.g. `c3.8-helm-chart-skel`. The chapter and section anchor must match a real heading in `report/chapter-N-*.md`. The slug must be unique within the matrix and stable across re-renders. |
| `ch3_section`   | The Chapter 3 section heading the claim attaches to, prefixed with the section sign (e.g. `§3.8`). The matrix is currently scoped to Chapter 3; if Chapter 4 (Implementation and Results) lands its own claims they extend this column with `§4.x`. |
| `code_package`  | The Go package (or non-Go directory) that implements the claim, relative to the repository root and ending with a trailing slash. Single package per row keeps the diff narrow. If a claim spans multiple packages, split it into multiple rows under the same `claim_id` family or pick the canonical owner. |
| `test_files`    | Comma-separated list of test files that exercise the claim, relative to the repository root. Helm-chart Go tests, integration tests, and spike tests are all valid here. Empty cell is **not** allowed (use `n/a` only for rows that are intentionally test-free, with the rationale recorded in *Provenance* below). |
| `test_ids`      | Comma-separated list of `Test*` function names inside `test_files` that assert the claim. Names must be byte-equal to the `func Test...` line in source so `grep` finds them deterministically. |
| `eval_run_ids`  | Comma-separated identifiers from `_bmad-output/implementation-artifacts/eval-runs/` once the evaluation harness lands (Epic 5). Substrate, infrastructure, and spike rows that pre-date the harness use `n/a` and the rationale is recorded in *Provenance*. |

The schema is self-documenting in the sense NFR42 requires: a reader can pick
any row and trace the claim to ground without consulting an external
glossary.

## How rows are added

Every PR carries a `traceability_updated:` field in its body (enforced by
[`.github/PULL_REQUEST_TEMPLATE.md`](../.github/PULL_REQUEST_TEMPLATE.md))
and is gated by the
[`traceability` job](../.github/workflows/ci.yml) on every pull request.

- `traceability_updated: yes` requires that **this file** appears in the
  PR's changed-file list. Add a row (or amend an existing one) whose
  `claim_id` is unique, sort the table by `claim_id`, and update the
  *Provenance* annex below the table with the PR number, merge SHA, and the
  FRs/NFRs the PR satisfies or informs.
- `traceability_updated: no` requires a non-empty *Traceability rationale*
  section in the PR body explaining why the matrix is unaffected (typical
  cases: docs-only PRs that touch no FR/NFR, build-system tweaks already
  traced under an infrastructure NFR, code-review patch passes that reuse
  an existing row).

The CI gate is implemented at
[`.github/scripts/check-traceability.sh`](../.github/scripts/check-traceability.sh)
and unit-tested at `.github/scripts/check-traceability.bats`. See the
*Conventions* footer for the slug grammar and provenance conventions.

## Matrix

| claim_id | ch3_section | code_package | test_files | test_ids | eval_run_ids |
|---|---|---|---|---|---|
| `c3-traceability-bootstrap` | §3-meta | `docs/` | `.github/scripts/check-traceability.bats` | `AC6a: yes path with matrix change passes`, `AC6b: yes path without matrix change fails`, `AC6c: no path with non-empty rationale passes`, `AC6d: no path with HTML-comment-only rationale fails`, `AC6e: missing traceability_updated field fails`, `Invalid traceability_updated value fails` | n/a |
| `c3.4-helm-evaluation-wiring` | §3.4 | `deploy/helm/olaitan/values.yaml`, `deploy/helm/olaitan/templates/_helpers.tpl`, `deploy/helm/olaitan/templates/configmap.yaml`, `deploy/helm/olaitan/templates/deployment.yaml`, `deploy/helm/olaitan/EVAL.md`, `Makefile`, `tests/e2e/rs_smoke_test.go`, `hack/kind-config.yaml`, `.github/workflows/ci.yml`, `cmd/olaitan/main.go`, `internal/config/config.go`, `internal/correlator/correlator.go`, `internal/correlator/trigger/trigger.go`, `internal/correlator/assembler/assembler.go`, `internal/nats/streams.go`, `docs/traceability.md` | `deploy/helm/helm_test.go`, `deploy/helm/testdata/golden/default.golden.yaml`, `deploy/helm/testdata/golden/rs.golden.yaml`, `deploy/helm/testdata/golden/f.golden.yaml`, `tests/e2e/rs_smoke_test.go`, `internal/config/config_test.go`, `internal/correlator/assembler/assembler_test.go` | `TestEvaluationConfig_Default_NoOverlay`, `TestEvaluationConfig_F_DisablesRulesAndBaselines`, `TestEvaluationConfig_F_DisablesAllNonFalcoSources`, `TestEvaluationConfig_RS_EnablesRulesAndBaselinesNoLLM`, `TestEvaluationConfig_RS_DoesNotForceNonFalcoSourcesOff`, `TestEvaluationConfig_RSL_RaisesProviderToApi`, `TestEvaluationConfig_RSLT_RaisesProviderToApi`, `TestEvaluationConfig_OperatorKnobs_RespectedWhenEmpty`, `TestEvaluationConfig_FailsFast_OnInvalidConfig`, `TestEvaluationConfig_FailsFast_OnInvalidProvider`, `TestEvaluationConfig_FailsFast_OnCaseInsensitiveProvider`, `TestAnalystProviderNone_StandaloneFlag`, `TestEvaluationAnchorsPresentInOlaitanYAML`, `TestGoldenFile_Default`, `TestGoldenFile_RS`, `TestGoldenFile_F`, `TestKindSmoke_RS_EmitsRuleMatchAndBaselineDeviation`, `TestProviderNoneAccepted`, `TestAssemblerSkipsKubeFetchWhenResolvedIdentitySupplied` | TBD |
| `c3.4-applog-sidecar-adapter` | §3.4 | `internal/collector/applog/`, `internal/admission/applog/`, `cmd/olaitan/applog.go` | `internal/collector/applog/translate_test.go`, `internal/collector/applog/tail_test.go`, `internal/collector/applog/applog_test.go`, `internal/collector/applog/applog_integration_test.go`, `internal/collector/applog/applog_bench_test.go`, `internal/admission/applog/inject_test.go`, `internal/admission/applog/webhook_test.go`, `internal/admission/applog/webhook_integration_test.go`, `internal/nats/nats_test.go`, `cmd/olaitan/main_test.go`, `deploy/helm/helm_test.go` | `TestTranslate_HappyPath_Stdout`, `TestTranslate_HappyPath_Stderr`, `TestTranslate_RejectsNilLine`, `TestTranslate_AcceptsEmptyLine`, `TestTranslate_RejectsZeroTimestamp`, `TestTranslate_RejectsPreEpochTimestamp`, `TestTranslate_RejectsFutureTimestamp`, `TestTranslate_RejectsUnknownStream`, `TestTranslate_RejectsEmptyContainer`, `TestTranslate_DeterministicEventID`, `TestTranslate_DistinctOffsetsProduceDistinctIDs`, `TestTranslate_LineLongerThan64KiB_Truncated`, `TestTranslate_InvalidUTF8_Replaced`, `TestTranslate_EmbeddedNUL_PreservedInRaw_StrippedInSummary`, `TestTranslate_ControlCharsInLabel_Sanitized`, `TestTranslate_NonOlaitanLabelsDropped`, `TestTranslate_IDDerivedFromTruncatedNotOriginal`, `TestTail_LFLineEnding`, `TestTail_CRLFLineEnding`, `TestTail_NoTrailingNewline_EmittedOnReaderClose`, `TestTail_LineLongerThan1MiB_TruncatedAtBoundary`, `TestTail_ReaderEOF_ReturnsCleanly`, `TestTail_BackpressureSheddingTriggers_AtChannelFull`, `TestTail_BackpressureRecoversAtLowWaterMark`, `TestTail_ContextCancelExitsCleanly`, `TestTail_FileTail_ReadsBasicFileLines`, `TestNew_RejectsEmptyStdoutPath`, `TestNew_RejectsEmptyStderrPath`, `TestNew_RejectsIdenticalPaths`, `TestNew_RejectsEmptyContainer`, `TestNew_RejectsNonPositiveChannelBuffer`, `TestNew_RejectsTinyMaxLineBytes`, `TestNew_RejectsNilPublisher`, `TestRun_PublishesLinesToRawAppLogSubject`, `TestRun_ContextCancelExitsCleanly`, `TestRun_DropsTranslateError_NoCrash`, `TestRun_TerminalPublishErrorIsDropped`, `TestRun_StalenessWatchdog_QuietButHealthy_DoesNotFlipUnhealthy`, `TestRun_StalenessWatchdog_StaleAndReaderError_FlipsUnhealthy`, `TestRun_TopLevelPanic_MarksUnhealthy_DoesNotPropagate`, `TestRun_BackpressureShedding_SignalsViaCounter`, `TestIsPermanentPublishError_RecognisesTypedErrors`, `TestIntegration_LFLineEnding_PublishedToRawAppLog`, `TestIntegration_StdoutAndStderr_BothPublished`, `TestIntegration_CRLFLineEnding`, `TestIntegration_InvalidUTF8_ReplacedAndTagged`, `TestIntegration_EmbeddedNUL_PreservedInRaw`, `TestIntegration_MsgIDDistinctOffsets`, `TestIntegration_MsgIDDedup_TrueDedup`, `TestIntegration_BackpressureBurstDeliveredAgainstFastConsumer`, `BenchmarkAdapter_PublishLatency`, `TestInject_HappyPath`, `TestInject_NoAnnotation_Skipped`, `TestInject_DeprecatedKey_AcceptedAsAlias`, `TestInject_CanonicalKeyTakesPrecedence`, `TestInject_Idempotent`, `TestInject_NativeSidecarPatchShape`, `TestInject_FallbackToContainers_WhenNativeSidecarDisabled`, `TestInject_VolumeMountedOnPeerContainer`, `TestInject_TwoAppContainers_TargetsAnnotated`, `TestInject_ExistingVolumesArray_Preserved`, `TestInject_ZeroContainers_Errors`, `TestInject_TargetContainerNotFound_Errors`, `TestInject_EmptyImage_Errors`, `TestInject_NilPod_Errors`, `TestWebhookHandler_DecodesAdmissionReviewV1`, `TestWebhookHandler_NoAnnotation_Allows_NoPatch`, `TestWebhookHandler_DeprecatedKey_StillInjects`, `TestWebhookHandler_KubeSystemNamespace_NotInjected`, `TestWebhookHandler_KubePublic_NotInjected`, `TestWebhookHandler_RejectsNonPost`, `TestWebhookHandler_RejectsNonJSON`, `TestWebhookHandler_HealthzOK`, `TestNewWebhook_RejectsEmptyTLSCert`, `TestNewWebhook_RejectsEmptySidecarImage`, `TestWebhookHandler_RequestsTotal_Increments`, `TestWebhookHandler_InjectedTotal_Increments`, `TestWebhookHandler_SkippedTotal_IncrementsOnNoAnnotation`, `TestIntegration_WebhookOverRealTLS`, `TestIntegration_TLSConfig_RequiresValidCert`, `TestStreamConfigsCoversAppLogSubject`, `TestRun_DispatchesAppLogSidecar_MissingEnv_Exits1`, `TestRun_DispatchesAppLogWebhook_MissingEnv_Exits1`, `TestApplogSidecarRenders_WhenEnabled`, `TestApplogSidecarAbsent_WhenDisabled`, `TestApplogWebhookFailurePolicyDefaultIgnore`, `TestApplogWebhookNamespaceSelectorExcludesKubeSystem`, `TestApplogWebhookFailFast_OnEmptyCABundle`, `TestApplogWebhookFailFast_OnRelativeStdoutPath`, `TestApplogWebhookFailFast_OnIdenticalPaths`, `TestApplogWebhookFailFast_OnShellMetachars`, `TestApplogSidecarTLSCertManagerCertificateRendered`, `TestApplogSidecarHAReplicaCountDefault` | n/a |
| `c3.4-audit-webhook-receiver` | §3.4 | `internal/collector/audit/` | `internal/collector/audit/translate_test.go`, `internal/collector/audit/audit_test.go`, `internal/collector/audit/audit_integration_test.go`, `internal/collector/audit/audit_bench_test.go`, `internal/sourcehealth/sourcehealth_test.go`, `deploy/helm/helm_test.go` | `TestTranslate_HappyPath`, `TestTranslate_RejectsZeroTimestamp`, `TestTranslate_RejectsFutureTimestamp`, `TestTranslate_RejectsEmptyAuditID`, `TestTranslate_NonResponseCompleteStage_ReturnsSentinel`, `TestTranslate_PodResource_PopulatesPodRef`, `TestTranslate_NonPodResource_ZeroesPodNameUID`, `TestTranslate_ClusterScopedResource_ZeroesNamespace`, `TestTranslate_StripsLargeRequestObject`, `TestTranslate_DeterministicEventID`, `TestTranslate_TagsCopied_NotAlias`, `TestTranslate_ForbidDecisionTagged`, `TestTranslate_AnonymousUser`, `TestTranslate_NilObjectRef`, `TestTranslate_NilEvent`, `TestTranslate_SecretsAccessIsWarning`, `TestTranslate_PodsExecIsWarning`, `TestTranslate_RawIsValidJSON`, `TestReceiver_HappyPath_Publishes_204`, `TestReceiver_Rejects_NonPOST`, `TestReceiver_Rejects_NonJSON`, `TestReceiver_Rejects_OversizePayload`, `TestReceiver_Rejects_NoClientCert`, `TestReceiver_Rejects_UnknownClientCA`, `TestReceiver_5xx_When_AllPublishesFailTransiently`, `TestReceiver_204_When_AllPublishesFailPermanently`, `TestReceiver_Health_FlipsHealthyOnFirstPublish`, `TestReceiver_Health_StalenessFlipsUnhealthy`, `TestNew_RejectsMissingFields`, `TestIntegration_HappyPath_PublishesToJetStream`, `TestIntegration_RetryDedup_AtLeastOnce`, `TestIntegration_TLSHandshakeWithCorrectCert`, `BenchmarkAdapter_PublishLatency`, `TestTracker_ZeroValueReportsUnhealthy`, `TestTracker_MarkHealthyClearsError`, `TestTracker_MarkUnhealthyRetainsError`, `TestTracker_MarkUnhealthyNilErr`, `TestTracker_LastWriterWins`, `TestTracker_Concurrent`, `TestAuditPolicyConfigMapRenders_WhenEnabled`, `TestAuditWebhookKubeconfigSecretRenders_WhenEnabled`, `TestAuditWebhookTLSSecretRenders_WhenEnabled`, `TestAuditWebhookFailsFast_WhenApiserverClientCertEmpty`, `TestAuditWebhookFailsFast_WhenClusterCADataEmpty`, `TestAuditWebhookGate_AllResourcesGated`, `TestDaemonsetMountsAuditTLSSecret_WhenEnabled`, `TestValidatingWebhookConfigurationUntouched` | n/a |
| `c3.4-calico-flow-adapter` | §3.4 | `internal/collector/cni/`, `cmd/olaitan/main.go` | `internal/collector/cni/translate_test.go`, `internal/collector/cni/cni_test.go`, `internal/collector/cni/cni_integration_test.go`, `internal/collector/cni/cni_bench_test.go`, `internal/collector/cni/fixture_test.go`, `internal/nats/nats_test.go`, `internal/config/config_test.go`, `cmd/olaitan/main_test.go`, `deploy/helm/helm_test.go` | `TestTranslate_HappyPath`, `TestTranslate_NilCases`, `TestTranslate_ZeroTimestamp_Rejected`, `TestTranslate_PreEpochTimestamp_Rejected`, `TestTranslate_FarFutureTimestamp_Rejected`, `TestTranslate_ProtoUnknown_DefaultsToUnknown`, `TestTranslate_ActionUnspecified_StillTranslates`, `TestTranslate_DestPortZero_RetainsFlow`, `TestTranslate_DestPortOutOfUint16Range_TagsAsInvalid`, `TestTranslate_EmptySourceAndNamespace_TagsUnknownSource`, `TestTranslate_DeterministicEventID`, `TestTranslate_OversizeRaw_RejectedAsErrEventTooLarge`, `TestTranslate_RoundTripJSON_SemanticallyStable`, `TestTranslate_RawIsValidJSON`, `TestTranslate_NoSvcTagWhenServiceEmpty`, `TestTranslate_NoConnsStartedTagWhenZero`, `TestTranslate_PodRefNode_PopulatedFromHostname`, `TestTranslate_Summary_NotDoublySanitised`, `TestTranslate_SourceLabels_AllowlistForwarded`, `TestTranslate_PolicyTrace_TagsEmitted`, `TestValidateTimestamp_EndTimeBeforeStartTime_TagAnomaly`, `TestValidateTimestamp_EndTimeFarFuture_TagAnomaly`, `TestSanitizeForTag_StripsControlChars`, `TestSanitizeForTag_PreservesTab`, `TestSanitizeForTag_Caps256Bytes`, `TestSanitizeForTag_UTF8RuneBoundary_TruncatesCleanly`, `TestNew_ValidationErrors`, `TestNew_NilPublisher`, `TestNew_DefaultsApplied`, `TestNew_AggregationIntervalNon15_Rejected`, `TestNew_StartTimeGtePositive_Rejected`, `TestNew_StartTimeGteExplicitZero_Preserved`, `TestNew_StartTimeGteOmitted_DefaultsToReplay`, `TestRun_FirstFlowFlipsHealthy`, `TestRun_DialFailureMarksUnhealthy`, `TestRun_UnauthenticatedIsTerminal`, `TestRun_MissingTLSFileIsTerminal`, `TestRun_TopLevelPanic_MarksUnhealthy_DoesNotPropagate`, `TestRun_TranslateError_IncrementsCounterContinues`, `TestRun_OversizeFlow_IncrementsOversizeCounter`, `TestRun_WatchdogQuietWhenReady_DoesNotFlipUnhealthy`, `TestRun_WatchdogStaleAndNotReady_FlipsUnhealthy`, `TestRun_WatchdogNeverReceivedFlow_DoesNotFabricate`, `TestRun_ConnReadyDeferredUntilFirstRecv`, `TestRun_LastEventTime_UpdatedAfterPublishSuccess`, `TestPublishWithRetry_PerAttemptDeadline_DoesNotCollapseBudget`, `TestLoadTLSConfigFromDisk_CABundleNotPEM_Terminal`, `TestIsTerminalConnectError`, `TestIsTerminalConnectError_PermissionDeniedSubstring`, `TestIsTerminalTLSError`, `TestIsTerminalTLSError_TypedClassifier`, `TestConnectivityCheckInterval`, `TestIntegration_ConnectsAndPublishesOneFlow`, `TestIntegration_HealthFlipOnDialFailure`, `TestIntegration_TerminalErrorOnUnauthenticatedClient`, `TestIntegration_MsgIDDedupAcrossRepublish`, `TestIntegration_StreamRecvErrorTriggersReconnect`, `TestIntegration_HealthRecoversOnReconnect`, `TestIntegration_TLSConfig_RequiresValidCert`, `TestBench_NFR1ReceiveToPublishP99`, `TestUpdateExpectedJSON`, `TestStreamConfigsCoversNetworkFlowSubject`, `TestStartCollectorRing_WiresCalicoAdapter`, `TestCalicoSensorAbsent_WhenDisabled`, `TestCalicoSensorPathB_RendersTLSSecret`, `TestCalicoSensorPathA_SkipsTLSSecret`, `TestCalicoSensorEnabled_RequiresTLS`, `TestCalicoSensorDaemonsetMountsTLS`, `TestCalicoSensorNetworkPolicyAllowsGoldmaneEgress`, `TestCalicoSensorConfigMapBridgesValues`, `TestCalicoSensorConfigMapBridgesStartTimeGte` | n/a |
| `c3.4-calico-flow-spike` | §3.4 | `spikes/calico-flow/` | `spikes/calico-flow/main_test.go` | `TestTranslateContract`, `TestRoundTripJSON`, `TestTimestampStability` | n/a |
| `c3.4-evidence-correlator` | §3.4 | `internal/correlator/`, `internal/schema/evidence.go`, `cmd/olaitan/main.go`, `internal/config/config.go`, `internal/subjects/subjects.go`, `config/olaitan.yaml`, `deploy/helm/olaitan/templates/configmap.yaml`, `deploy/helm/olaitan/values.yaml`, `docs/traceability.md` | `internal/correlator/window/window_test.go`, `internal/correlator/trigger/trigger_test.go`, `internal/correlator/assembler/assembler_test.go`, `internal/correlator/correlator_integration_test.go`, `internal/schema/schema_test.go`, `internal/config/config_test.go`, `internal/subjects/subjects_test.go`, `cmd/olaitan/main_test.go`, `deploy/helm/helm_test.go` | `TestStoreAddEvictsByWindowPerWorkload`, `TestStoreSetWindowDurationAffectsFutureAdds`, `TestStoreConcurrentAdds`, `TestEvaluateMultiSignalRequiresDistinctSources`, `TestExternalTriggerConstructors`, `TestAssemblerBuildsPackageWithPosture`, `TestAssemblerMarksPostureDegradedWhenUnavailable`, `TestEnforceSizeCapDeterministic`, `TestIntegration_CorrelatorPublishesEvidenceForAllTriggersAndCap`, `TestIntegration_CorrelatorTriggerToPublishLatencyBudget`, `TestEvidencePackageRoundTrip`, `TestContainmentEvidenceRoundTrip`, `TestCorrelatorConfigOmittedSubstitutesDefault`, `TestCorrelatorConfigExplicitKnobsHonoured`, `TestCorrelatorConfigRejectsInvalidKnobs`, `TestEvidencePackagesConstant`, `TestStartAggregator_BuildsPostureClientWhenEnabled`, `TestStartAggregator_PostureDisabledLeavesClientNil`, `TestCorrelatorConfigMapBridgesValues`, `TestCorrelatorInvalidValuesFailFast` | n/a |
| `c3.4-olt-sigma-rule-engine` | §3.4 | `internal/decision/rules/`, `internal/decision/rules/parser/`, `internal/decision/rules/matcher/`, `internal/decision/rules/loader/`, `internal/metrics/metrics.go`, `internal/config/config.go`, `cmd/olaitan/main.go`, `config/olaitan.yaml`, `deploy/helm/olaitan/templates/configmap.yaml`, `deploy/helm/olaitan/values.yaml` | `internal/decision/rules/engine_test.go`, `internal/decision/rules/engine_integration_test.go`, `internal/decision/rules/engine_bench_test.go`, `internal/decision/rules/parser/parser_test.go`, `internal/decision/rules/parser/parser_property_test.go`, `internal/decision/rules/matcher/resolver_test.go`, `internal/decision/rules/matcher/modifiers_test.go`, `internal/decision/rules/loader/loader_test.go`, `internal/decision/rules/loader/loader_integration_test.go`, `internal/metrics/metrics_test.go`, `internal/config/config_test.go`, `cmd/olaitan/main_test.go`, `deploy/helm/helm_test.go` | `TestEvaluatePackage_MatchesProcessExeRule`, `TestEvaluatePackage_NoMatchesProducesEmpty`, `TestEvaluatePackage_PerEventFanOut`, `TestEvaluatePackage_K8sFieldsResolvedFromPosture`, `TestEvaluatePackage_NoEventsFallsBackToPackageID`, `TestEvaluatePackage_NoLoadedRulesEmpty`, `TestChooseEventID`, `TestReEntrancyGuard_ContractStability`, `TestIntegration_EngineEmitsRuleMatchForFalcoXmrigPackage`, `TestIntegration_ReEntrancyGuardSkipsRuleMatchPackages`, `TestIntegration_HotReloadAddsNewRule`, `BenchmarkRulesEngine_PerPackageLatency`, `TestRulesEngineLatencyBudget`, `TestParseRule_ValidOLTRule`, `TestParseRule_MissingAttack`, `TestParseRule_EmptyAttackList`, `TestParseRule_NilAttack`, `TestParseRule_InvalidTechniqueFormat`, `TestParseRule_ValidTechniqueVariants`, `TestParseRule_ExplicitSeverityNull`, `TestParseRule_OutOfRangeSeverity`, `TestParseRule_SeverityWinsOverLevel`, `TestParseRule_LevelOnlyFallback`, `TestParseRule_UnknownLevel`, `TestParseRule_DefaultSeverityWhenAbsent`, `TestParseRule_SeverityExplicitZero`, `TestParseRule_RuleIDGrammar`, `TestParseRule_MalformedYAML`, `TestProperty_ValidAttackTokensAccepted`, `TestProperty_InvalidAttackTokensRejected`, `TestProperty_InRangeSeverityAccepted`, `TestProperty_OutOfRangeSeverityRejected`, `TestProperty_ValidRuleIDsAccepted`, `TestProperty_InvalidRuleIDsRejected`, `TestResolver_K8sRoutesToPosture`, `TestResolver_NonK8sRoutesToEventFields`, `TestResolver_MissingKeyReturnsNil`, `TestResolver_CaseFoldedLookup`, `TestResolver_EventKeyCaseCollisionRejected`, `TestResolver_NilPostureSurfacesAllK8sLookupsAsNil`, `TestResolver_EntryFieldsMirrorPreservesOriginalCase`, `TestPostureFields_ProjectsCanonicalKeys`, `TestPostureFields_NilPostureEmptyMap`, `TestEventFields_FlattensRawJSON`, `TestModifier_Contains`, `TestModifier_All`, `TestModifier_StartsWith`, `TestModifier_EndsWith`, `TestModifier_Windash`, `TestModifier_Base64`, `TestModifier_Base64Offset`, `TestModifier_Re`, `TestModifier_CIDR`, `TestModifier_Expand`, `TestModifierCoverageMatrix`, `TestLoad_EmptyDirEmptyCorpus`, `TestLoad_SingleValidRule`, `TestLoad_MultipleRulesAcrossCategories`, `TestLoad_RejectsMalformedYAML`, `TestLoad_RejectsMissingAttack`, `TestLoad_DuplicateIDRejected`, `TestLoad_StableOrderByPath`, `TestLoad_FailureRetainsPreviousCorpusOnSecondCall`, `TestLoad_IgnoresNonYAMLFiles`, `TestWatch_ReloadsOnConfigMapDataSwap`, `TestWatch_RejectsBadReloadAndRetainsPrev`, `TestWatch_DoubleStartReturnsRunningError`, `TestRegisterHistogram_BasicObserve`, `TestRegisterHistogram_DuplicateNameRejected`, `TestRegisterHistogram_SourceLabelMerged`, `TestRegisterHistogram_NilRegistry`, `TestRulesDefaultsAppliedOnOmittedBlock`, `TestRulesExplicitDisableHonoured`, `TestRulesValidateRejectsEmptyPathWhenEnabled`, `TestRulesValidateRejectsRelativePath`, `TestRulesEnabledOrDefault`, `TestStartAggregator_RulesEngineEnabledWiresGoroutines`, `TestStartAggregator_RulesEngineDisabledSkipsWiring`, `TestRulesConfigMapMountedOnAggregator`, `TestRulesValuesBridgedIntoConfigMap`, `TestRulesAnchorsPresentInOlaitanYAML` | n/a |
| `c3.4-olt-rule-corpus-initial` | §3.4 | `rules/`, `internal/decision/rules/`, `deploy/helm/olaitan/templates/configmap.yaml`, `Makefile`, `docs/sigma-extensions.md`, `docs/traceability.md` | `internal/decision/rules/corpus_lint_test.go`, `internal/decision/rules/corpus_integration_test.go`, `deploy/helm/helm_test.go` | `TestCorpusLint_AllRulesParse`, `TestCorpusLint_FailsOnMissingAttackAnnotation`, `TestIntegration_CorpusMatchesScenarioFixture`, `TestRulesConfigMapPopulatedFromCorpus`, `TestRulesConfigMapKeysAreFlatNoDirectory` | n/a |
| `c3.6.3-trust-bound` | §3.6.3 | `internal/decision/score/`, `internal/config/config.go`, `cmd/olaitan/main.go`, `config/olaitan.yaml`, `deploy/helm/olaitan/values.yaml`, `deploy/helm/olaitan/templates/configmap.yaml`, `docs/traceability.md`, `docs/runbook.md` | `internal/decision/score/score_test.go`, `internal/decision/score/score_property_test.go`, `internal/decision/score/score_metrics_test.go`, `internal/config/config_test.go`, `cmd/olaitan/main_test.go`, `deploy/helm/helm_test.go` | `TestScore_ZeroPackage`, `TestScore_SingleRuleMatch_AtMaxSeverity`, `TestScore_MultipleRuleMatches_UsesMax`, `TestScore_MultipleBaselineDeviations_UsesMaxSigma`, `TestScore_RuleAndBaselineTie`, `TestScore_SeverityClamping`, `TestScore_SeverityParseFallback`, `TestScore_LLMTermIsZero`, `TestScore_NilPackage`, `TestScore_Determinism`, `TestScore_ConfigSnapshotChange`, `TestScore_NegativeSigmaIsSkipped`, `TestScore_NaNInfSigmaIsSkipped`, `TestScore_TotalClamping`, `TestProperty_ScoreInRange`, `TestProperty_NoLLMOnlyEscalation`, `TestProperty_MonotonicInRuleSeverity`, `TestProperty_MonotonicInBaselineSigma`, `TestProperty_DeterministicForFixedSnapshot`, `TestProperty_HotReloadViaSubscribe`, `TestNew_RegistersMetricFamilies`, `TestScore_IncrementsEvaluationsCounter`, `TestScore_HistogramLabelCardinalityFinite`, `TestScore_NilRegistryConstructs`, `TestScoreConfig_DefaultsRender`, `TestScoreConfig_OperatorOverride`, `TestScoreConfig_FailsFast_OnInvalidSum` | n/a |
| `c3.4-deterministic-detection-observability` | §3.4 | `internal/metrics/metrics.go`, `internal/correlator/`, `internal/decision/rules/engine.go`, `internal/decision/baseline/engine.go`, `internal/decision/baseline/sigma_bucket.go`, `internal/decision/severitybucket/`, `internal/correlator/assembler/assembler.go`, `cmd/olaitan/main.go`, `docs/runbook.md`, `docs/traceability.md` | `internal/metrics/metrics_test.go`, `internal/correlator/correlator_metrics_test.go`, `internal/decision/rules/engine_metrics_test.go`, `internal/decision/baseline/engine_metrics_test.go`, `internal/decision/baseline/sigma_bucket_test.go`, `internal/decision/severitybucket/severitybucket_test.go` | `TestRegisterCounterVec_BasicIncrement`, `TestRegisterCounterVec_NilRegistry`, `TestRegisterCounterVec_EmptyNameRejected`, `TestRegisterCounterVec_DuplicateNameRejected`, `TestRegisterHistogramVec_BasicObserve`, `TestRegisterHistogramVec_NilRegistry`, `TestRegisterHistogramVec_EmptyNameRejected`, `TestIntegration_CorrelatorMetricsExposeAllThreeFamilies`, `TestBumpMatchesVec_FansOutPerTechnique`, `TestEngine_DeviationsVecLabelledBySigmaBucket`, `TestSigmaBucket_Boundaries`, `TestSigmaBucket_BelowMin`, `TestSigmaBucket_NaN`, `TestScore_NamedLabels`, `TestScore_SyslogNumeric`, `TestScore_OutOfRangeNumeric`, `TestBucket_Boundaries`, `TestBucketFromLabel_RoundTrip` | n/a |
| `c3.4-welford-baseline-engine` | §3.4 | `internal/decision/baseline/`, `internal/config/config.go`, `cmd/olaitan/main.go`, `config/olaitan.yaml`, `deploy/helm/olaitan/templates/configmap.yaml`, `deploy/helm/olaitan/values.yaml`, `docs/traceability.md` | `internal/decision/baseline/welford_test.go`, `internal/decision/baseline/welford_property_test.go`, `internal/decision/baseline/store_test.go`, `internal/decision/baseline/warmup_test.go`, `internal/decision/baseline/engine_test.go`, `internal/decision/baseline/metrics_test.go`, `internal/decision/baseline/engine_integration_test.go`, `internal/decision/baseline/engine_latency_integration_test.go`, `deploy/helm/helm_test.go` | `TestWelford_EmptyState`, `TestWelford_SingleSample`, `TestWelford_TwoSample`, `TestWelford_TenSample_BatchAgreement`, `TestWelford_Sigma`, `TestWelford_Reset`, `TestWelford_MarshalUnmarshal_RoundTrip`, `TestWelford_UnmarshalRedisHash_Empty`, `TestWelford_UnmarshalRedisHash_Partial`, `TestWelford_UnmarshalRedisHash_BadInt`, `TestWelford_Merge_EmptyOther`, `TestWelford_Merge_EmptySelf`, `TestWelford_Merge_Equivalence`, `TestProperty_Welford_MatchesBatchMeanVariance`, `TestProperty_Welford_StdDevAtFixedSizes`, `TestProperty_Welford_MergeAdditive`, `TestStore_LoadEmptyWorkloadReturnsZeroWelfords`, `TestStore_SaveLoadRoundTripsAllFive`, `TestStore_SaveLoadWarmup`, `TestStore_LoadWarmupAbsentReturnsNotPresent`, `TestStore_RestartSimulation`, `TestStore_NewStoreRejectsNilClient`, `TestWarmup_BeginActivates`, `TestWarmup_ExpiresAfterDuration`, `TestWarmup_AbsentReturnsInactive`, `TestWarmup_SurvivesRestart`, `TestWarmup_NewWarmupRejectsNilStore`, `TestWarmup_ActiveWorkloads_ReapsExpired`, `TestNew_RejectsNilNATS`, `TestNew_RejectsNilStore`, `TestNew_RejectsNilWarmup`, `TestNew_RejectsNilEmitter`, `TestNew_HappyPath_RegistersAllMetrics`, `TestEngine_HistogramBucketsMatchNFR3`, `TestApplyReEntrancyGuard_SkipsBaselineDeviationPackages`, `TestApplyReEntrancyGuard_AllowsMultiSignalPackages`, `TestResolveWorkloadKey_PrefersWorkloadIdentity`, `TestResolveWorkloadKey_OrphanFallback`, `TestResolveWorkloadKey_PodRefFallback`, `TestResolveWorkloadKey_UnresolvableReturnsEmpty`, `TestDetectRestart_TaggedLifecycleTriggers`, `TestDetectRestart_AttemptZeroDoesNotTrigger`, `TestEngine_SetSigmaMultiplier`, `TestExtract_OutboundUniqueDstIPs`, `TestExtract_DistinctExecPaths`, `TestExtract_OutboundBytes`, `TestExtract_AuditEventsPerMin`, `TestExtract_ContainerRestartsPerHour`, `TestMetricNames_IsStableSet`, `TestDefaultExtractors_AllPresent`, `TestHasRestartAttemptTag`, `TestIntegration_BaselineEngineEmitsDeviationForOutboundIPSurge`, `TestIntegration_ReEntrancyGuardSkipsBaselineDeviationPackages`, `TestIntegration_WarmupActiveSuppressesDeviation`, `TestIntegration_BaselineSurvivesControllerRestart`, `TestIntegration_BaselineEngineLatencyBudget`, `TestBaselinesConfigMapBridgedFromValues`, `TestBaselinesAnchorsPresentInOlaitanYAML`, `TestBaselinesEnabledFalseDisablesAggregatorBlock` | n/a |
| `c3.4-per-source-rate-limit` | §3.4 | `internal/ratelimit/`, `internal/schema/event.go`, `internal/schema/schema_test.go`, `internal/config/config.go`, `cmd/olaitan/main.go`, `cmd/olaitan/metrics.go`, `internal/collector/falco/falco.go`, `internal/collector/audit/audit.go`, `internal/collector/cri/cri.go`, `internal/collector/cni/cni.go`, `internal/collector/applog/applog.go`, `config/olaitan.yaml`, `deploy/helm/olaitan/values.yaml`, `deploy/helm/olaitan/templates/configmap.yaml`, `docs/schema-versioning.md` | `internal/ratelimit/limiter_test.go`, `internal/ratelimit/limiter_integration_test.go`, `internal/schema/schema_test.go`, `internal/config/config_test.go`, `internal/collector/falco/falco_test.go`, `cmd/olaitan/metrics_test.go`, `deploy/helm/helm_test.go` | `TestNew_DefaultsApplied`, `TestNew_RejectsBadOptions`, `TestAllow_DisabledShortCircuit`, `TestAllow_BelowThresholdNotEngaged`, `TestAllow_AboveThresholdEngages`, `TestAllow_SustainedEngagementOneTransition`, `TestAllow_CooldownDisengagement`, `TestAllow_CooldownResetsOnReengage`, `TestAllow_SampledFractionWithinTolerance`, `TestAllow_SamplingDeterministic`, `TestUpdate_AcceptsAndRejects`, `TestUpdateThreshold_TakesEffectWithinOneBucket`, `TestUpdateEnabled_FalseShortCircuitsImmediately`, `TestAllow_ConcurrentRace`, `TestAllow_NotEngagedSteadyState`, `TestNew_DisabledDoesNotRequireNode`, `TestIntegration_LimiterEndToEnd`, `TestIntegration_LimiterMetricsRegistryEndToEnd`, `TestIntegration_RateLimitHotReloadViaManager`, `TestEventSampledOmitemptyBackwardsCompatible`, `TestEventSampledExplicitlySet`, `TestEventPre113WireFormUnmarshals`, `TestRateLimitOmittedSubstitutesDefault`, `TestRateLimitExplicitlySetHonoured`, `TestRateLimitDisabledShortCircuitsValidation`, `TestRateLimitValidateRejectsBadKnobs`, `TestNew_RateLimitFallbackDisabledWhenNil`, `TestNew_PreservesCallerSuppliedLimiter`, `TestDroppedBySampling_ZeroOnConstruct`, `TestRegisterAdapterCounters_FalcoBindsCircuitBreaker`, `TestRateLimitDefaultsRendered`, `TestRateLimitBridgeRendersThreshold`, `TestRateLimitDisabledRendered` | n/a |
| `c3.4-per-source-metrics` | §3.4 | `internal/metrics/`, `cmd/olaitan/metrics.go`, `cmd/olaitan/main.go`, `internal/sourcehealth/`, `internal/collector/falco/falco.go`, `internal/collector/audit/audit.go`, `internal/collector/cri/cri.go`, `internal/collector/cni/cni.go`, `internal/collector/applog/applog.go`, `internal/config/config.go`, `config/olaitan.yaml`, `deploy/helm/olaitan/values.yaml`, `deploy/helm/olaitan/templates/daemonset.yaml`, `deploy/helm/olaitan/templates/deployment.yaml`, `deploy/helm/olaitan/templates/configmap.yaml` | `internal/metrics/metrics_test.go`, `internal/metrics/server_test.go`, `internal/metrics/metrics_integration_test.go`, `cmd/olaitan/metrics_test.go`, `internal/config/config_test.go`, `internal/collector/falco/falco_test.go`, `internal/collector/audit/audit_test.go`, `internal/collector/cri/cri_test.go`, `internal/collector/cni/cni_test.go`, `internal/collector/applog/applog_test.go`, `deploy/helm/helm_test.go` | `TestNewRegistry_NotNil`, `TestRegisterAdapter_NilRegistryRejected`, `TestRegisterAdapter_NilArgsRejected`, `TestRegisterAdapter_DuplicateSourceRejected`, `TestRegisterAdapter_HealthGaugeTracksTracker`, `TestRegisterAdapter_EventsCounterAdvances`, `TestRegisterAdapter_ConcurrentIncrementsNoDataRace`, `TestRegisterAllFiveAdaptersRendered`, `TestRegisterPostureCounters_HappyPath`, `TestRegisterPostureCounters_MissingGetterRejected`, `TestRegisterPostureDisabled`, `TestRegisterCounter_LabelMerge`, `TestRegisterCounter_NilGetterRejected`, `TestRegister_GatherCount`, `TestServer_Start_ServesMetrics`, `TestServer_Start_DoubleStartRejected`, `TestServer_Start_NilRegistryRejected`, `TestServer_Start_ListenFailure`, `TestServer_Start_ReturnsOnContextCancel`, `TestServer_NilReceiver`, `TestServer_Shutdown_BeforeStart`, `TestIntegration_HealthGaugeWithinTenSecondBudget`, `TestIntegration_DualServerGracefulShutdown`, `TestIntegration_PromlintZeroProblems`, `TestIntegration_ConcurrentScrapesNoDataRace`, `TestStartMetricsServer_NilConfigRejected`, `TestStartMetricsServer_EmptyAddressRejected`, `TestStartMetricsServer_RegistersStreamingAdapters`, `TestStartMetricsServer_PostureDisabledGaugeRegistered`, `TestStartMetricsServer_DuplicateAdapterRegistrationSurfaces`, `TestRegisterAdapterCounters_FalcoIsNoOp`, `TestStartMetricsServer_PropagatesRegistrationError`, `TestMetricsConfigOmittedSubstitutesDefault`, `TestMetricsConfigExplicitAddressHonoured`, `TestMetricsConfigValidateRejectsEmptyAddress`, `TestEventsTotal_ZeroOnConstruct`, `TestEventsTotal_IncrementsOnPublishSuccess`, `TestEventsTotal_AdvancesAfterIncrement`, `TestEventsTotal_ReflectsIncrement`, `TestMetricsContainerPortPresent`, `TestMetricsAnnotationsPresentByDefault`, `TestMetricsAnnotationsAbsentWhenDisabled`, `TestMetricsBridgeRendersAddress` | n/a |
| `c3.4-workload-posture-client` | §3.4 | `internal/collector/posture/`, `internal/schema/posture.go`, `internal/keys/workload.go`, `cmd/olaitan/main.go`, `deploy/helm/olaitan/templates/rbac.yaml`, `deploy/helm/olaitan/templates/configmap.yaml`, `deploy/helm/olaitan/values.yaml` | `internal/collector/posture/posture_test.go`, `internal/collector/posture/owner_test.go`, `internal/collector/posture/netpol_test.go`, `internal/collector/posture/cache_test.go`, `internal/collector/posture/posture_integration_test.go`, `internal/schema/schema_test.go`, `internal/keys/workload_test.go`, `internal/config/config_test.go`, `cmd/olaitan/main_test.go`, `deploy/helm/helm_test.go` | `TestResolveWorkloadIdentity_PodOwnedByDeploymentViaReplicaSet`, `TestResolveWorkloadIdentity_PodOwnedByReplicaSetWithoutDeployment`, `TestResolveWorkloadIdentity_PodOwnedByJobInCronJob`, `TestResolveWorkloadIdentity_PodOwnedByStandaloneJob`, `TestResolveWorkloadIdentity_PodOwnedByStatefulSet`, `TestResolveWorkloadIdentity_PodOwnedByDaemonSet`, `TestResolveWorkloadIdentity_OrphanPodFallback`, `TestResolveWorkloadIdentity_ReplicaSetNotFoundFallsBackToOrphan`, `TestResolveWorkloadIdentity_NilPodReturnsError`, `TestResolveWorkloadIdentity_ReplicaSetWithNonDeploymentParent`, `TestApplicableNetworkPolicies_MatchLabels`, `TestApplicableNetworkPolicies_EmptySelectorMatchesAll`, `TestApplicableNetworkPolicies_MatchExpressionsIn`, `TestApplicableNetworkPolicies_CrossNamespaceBleedGuarded`, `TestApplicableNetworkPolicies_DeterministicSort`, `TestApplicableNetworkPolicies_EmptyListReturnsEmpty`, `TestApplicableNetworkPolicies_NilClientsetReturnsError`, `TestApplicableNetworkPolicies_EmptyNamespaceReturnsError`, `TestApplicableNetworkPolicies_InvalidSelectorWraps`, `TestCache_HitWithinTTL`, `TestCache_MissAfterTTL`, `TestCache_MissOnUnknownKey`, `TestCache_PutRefreshesExistingEntry`, `TestCache_Reset`, `TestCache_ConcurrentGetPutRaceClean`, `TestCache_StaleEvictionUnderConcurrentRefresh`, `TestCache_ZeroTTLImmediatelyExpired`, `TestNew_RejectsZeroCacheTTL`, `TestNew_RejectsCacheTTLAboveCeiling`, `TestNew_RejectsZeroFetchTimeout`, `TestNew_RejectsNilClientset`, `TestGet_HappyPathPopulatesIdentityAndSecurityContext`, `TestGet_OrphanPodMarksOrphanAndCachesUnderFallbackKey`, `TestGet_CacheHitOnSecondCall`, `TestGet_CacheMissAfterTTL`, `TestGet_BypassCacheForcesRefetch`, `TestGet_FetchErrorMarksUnavailableTransient`, `TestGet_PermissionDeniedClassifiedCorrectly`, `TestGet_NetworkPoliciesMatchedAndSummarised`, `TestGet_RoleBindingsAndClusterRoleBindingsMatchByServiceAccount`, `TestGet_NilPodReturnsError`, `TestIntegration_PostureHappyPath`, `TestIntegration_OrphanPodFallback`, `TestIntegration_CacheHitOnSecondCall`, `TestIntegration_CacheMissAfterTTL`, `TestIntegration_BypassCacheOption`, `TestIntegration_NetworkPolicyMatchExpressions`, `TestIntegration_StatefulSetLineage`, `TestIntegration_CronJobLineage`, `TestIntegration_UnavailableReasonStaysClassedNotRaw`, `TestWorkloadPosturePopulatedRoundTrip`, `TestWorkloadPostureOrphanFallbackRoundTrip`, `TestWorkloadPostureUnavailableRoundTrip`, `TestWorkloadPostureOmitemptyDropsZeroFields`, `TestWorkloadIDHappyPath`, `TestPodFallbackIDHappyPath`, `TestWorkloadIDValidation`, `TestPodFallbackIDValidation`, `TestWorkloadIDSegmentSeparatorCount`, `TestPodFallbackIDOwnerKindIsLiteralPod`, `TestPostureConfigOmittedAccepted`, `TestPostureConfigValidDefaults`, `TestPostureConfigCacheTTLAboveCeilingRejected`, `TestPostureConfigNegativeFetchTimeoutRejected`, `TestPostureConfigZeroBlockWhenDisabledAccepted`, `TestStartAggregator_BuildsPostureClientWhenEnabled`, `TestStartAggregator_PostureDisabledLeavesClientNil`, `TestStartAggregator_KubeClientFailureWrapsError`, `TestPostureConfigMapBridgesValues`, `TestPostureCacheTTLAboveCeilingFails`, `TestPostureRBACRulesPresent` | n/a |
| `c3.4-containerd-cri-adapter` | §3.4 | `internal/collector/cri/` | `internal/collector/cri/translate_test.go`, `internal/collector/cri/cri_test.go`, `internal/collector/cri/cri_integration_test.go`, `internal/collector/cri/cri_bench_test.go`, `internal/nats/nats_test.go`, `deploy/helm/helm_test.go` | `TestTranslate_HappyPath_Started`, `TestTranslate_HappyPath_Created`, `TestTranslate_HappyPath_Stopped`, `TestTranslate_HappyPath_Deleted`, `TestTranslate_RejectsNilEvent`, `TestTranslate_RejectsZeroTimestamp`, `TestTranslate_RejectsNegativeTimestamp`, `TestTranslate_RejectsPreEpochTimestamp`, `TestTranslate_RejectsFutureTimestamp`, `TestTranslate_RejectsEmptyContainerID`, `TestTranslate_RejectsUnknownEventType`, `TestTranslate_NilSandboxStatus_GracefulDegradation`, `TestTranslate_NilMetadata_GracefulDegradation`, `TestTranslate_DeterministicEventID`, `TestTranslate_DeterministicEventID_SurvivesStrip`, `TestTranslate_ShortContainerID_NoPanic`, `TestTranslate_StripsLargeRawPayload`, `TestTranslate_AttemptZero_NoTag`, `TestTranslate_AttemptNonZero_TaggedRestart`, `TestTranslate_SandboxNotReady_TagApplied`, `TestTranslate_LabelsCopied_NotAlias`, `TestTranslate_NonAllowlistedLabelsDropped`, `TestTranslate_AllowlistedLabelsForwarded`, `TestTranslate_RawIsValidJSON`, `TestTranslate_DifferentEventsYieldDifferentIDs`, `TestNew_RejectsNilPublisher`, `TestNew_RejectsEmptySocketPath`, `TestNew_RejectsEmptyHostname`, `TestNew_RejectsNegativeDialTimeout`, `TestNew_RejectsNegativeStaleness`, `TestNew_DefaultsApplied`, `TestRun_ContextCancelExitsCleanly`, `TestRun_HealthFlipsOnConnectError`, `TestRun_PublishesTranslateableEvent`, `TestRun_DropsTranslateError_NoCrash`, `TestRun_TerminalPublishErrorIsDropped`, `TestRun_StalenessWatchdog_QuietButConnected_DoesNotFlipUnhealthy`, `TestRun_StalenessWatchdog_DisconnectedAndStale_FlipsUnhealthy`, `TestAdapter_EndToEnd_Bufconn`, `TestAdapter_DropsTranslateError_RealBoundary`, `BenchmarkAdapter_PublishLatency`, `TestStreamConfigsCoversContainerdLifecycleSubject`, `TestContainerdSensorAbsent_WhenDisabled`, `TestContainerdSensorRenders_WhenEnabled`, `TestContainerdSocketPathConfigurable`, `TestContainerdSensorMountsParentDirectoryNotFile`, `TestContainerdSensorEmptySocketPathFails` | n/a |
| `c3.4-falco-syscall-adapter` | §3.4 | `internal/collector/falco/` | `internal/collector/falco/falco_test.go`, `internal/collector/falco/falco_integration_test.go`, `internal/collector/falco/translate_test.go`, `internal/collector/falco/health_test.go`, `internal/retry/retry_test.go` | `TestNew_RejectsNilPublisher`, `TestNew_RejectsEmptyEndpoint`, `TestNew_RejectsEmptyHostname`, `TestNew_AppliesDefaultRetryWhenZeroValued`, `TestNew_PreservesCallerSuppliedRetry`, `TestHealth_ReturnsTrackerInZeroState`, `TestAdapter_EndToEnd_Bufconn`, `TestAdapter_RetriesOnDialFailure`, `TestTranslate_HappyPath`, `TestTranslate_DeterministicIDFallback`, `TestTranslate_DifferentInputsYieldDifferentIDs`, `TestTranslate_HostEventWithoutPodFields`, `TestTranslate_MissingTimeReturnsError`, `TestTranslate_NilResponseReturnsError`, `TestTranslate_EmptyOutputFields`, `TestTranslate_PrioritySeverityMapping`, `TestSourceHealth_ZeroValueIsUnhealthy`, `TestSourceHealth_MarkHealthy`, `TestSourceHealth_MarkUnhealthyCarriesError`, `TestSourceHealth_MarkUnhealthyNilErrorIsAllowed`, `TestSourceHealth_HealthyClearsLastError`, `TestSourceHealth_ConcurrentReadersAndWriters`, `TestSourceHealth_StatusReturnsConsistentSnapshot`, `TestStrategyDo_SuccessOnFirstAttempt`, `TestStrategyDo_SuccessAfterTransientFailures`, `TestStrategyDo_MaxAttemptsExhaustionReturnsLastErrorWrapped`, `TestStrategyDo_ContextCancelMidBackoff`, `TestStrategyDo_ContextAlreadyCancelled`, `TestStrategyDo_ZeroValueReturnsConfigError`, `TestStrategyDo_InvalidConfigVariants`, `TestStrategyDo_BackoffProgressionRespectsMultiplier`, `TestStrategyDo_BackoffCappedAtMax`, `TestStrategyDo_JitterBoundsRespected`, `TestStrategyDo_UnlimitedAttemptsTerminateOnSuccess`, `BenchmarkAdapter_PublishLatency` | n/a |
| `c3.6.4-fsm-escalation` | §3.6.4 | `internal/response/fsm/`, `internal/schema/state.go`, `internal/config/config.go`, `internal/metrics/metrics.go`, `cmd/olaitan/main.go`, `config/olaitan.yaml`, `docs/schemas/state_transition.yaml`, `docs/schema-versioning.md`, `docs/traceability.md` | `internal/response/fsm/fsm_test.go`, `internal/response/fsm/fsm_property_test.go`, `internal/schema/schema_test.go`, `internal/config/config_test.go` | `TestEscalation_SingleStepAtBoundaries`, `TestEscalation_BoundaryScores`, `TestDwellGuard_DefersThenAllows`, `TestEdge_ScoreClampingBoundaries`, `TestEncapsulation_EvaluateIsSoleMutator`, `TestState_UnknownWorkloadIsCleanAndNotInserted`, `TestIdempotency_RepeatedSameInputs`, `TestMetricsRegistered_AndUpdated`, `TestProperty_MonotonicEscalation`, `TestProperty_NoEscalationPastSuspiciousForBenign`, `TestProperty_DwellGuardDefersEscalation`, `TestProperty_Idempotent`, `TestProperty_PreservedKilledUnreachable`, `TestStateTransitionRoundTrip`, `TestStateTransitionOmitsStory22FieldsAtZero`, `TestLoad_FSM_DefaultsOnOmittedBlock`, `TestFSMConfig_ExplicitValuesHonoured`, `TestFSMConfig_RejectsNegativeDuration`, `TestConfidenceBands_MonotonicityRejected`, `TestFSMConfig_OrDefaultOnZeroValue` | n/a |
| `c3.6.5-fsm-deescalation` | §3.6.5 | `internal/response/fsm/`, `internal/schema/state.go`, `internal/config/config.go`, `config/olaitan.yaml` | `internal/response/fsm/fsm_test.go`, `internal/response/fsm/fsm_property_test.go` | `TestDeescalation_OneStepPerCooldown`, `TestDeescalation_RollingResetsOnSpike`, `TestProperty_DeescalationOneStepPerCooldown`, `TestProperty_PreservedKilledUnreachable` | n/a |
| `c3.7.3-netpol-restricted` | §3.7.3 | `internal/response/netpol/`, `internal/response/fsm/multisink.go`, `internal/config/config.go`, `cmd/olaitan/main.go`, `config/olaitan.yaml`, `docs/runbook.md` | `internal/response/netpol/manager_test.go`, `internal/response/netpol/manager_run_test.go`, `internal/response/netpol/netpol_integration_test.go`, `internal/response/fsm/multisink_test.go`, `internal/config/config_network_policy_test.go`, `deploy/helm/helm_test.go` | `TestApply_CreateThenIdempotentReapply`, `TestApply_UpdatesOnChangedPackageID`, `TestHandle_AppliesPolicyForRestrictedTransition`, `TestHandle_SkipsExcludedNamespace`, `TestHandle_WithMetricsObservesLatencyAndCounts`, `TestBuildPolicy_LabelsAnnotationsAndEgress`, `TestResolveSelector_OwnerKinds`, `TestResolveSelector_DaemonSetAndReplicaSet`, `TestParseWorkloadID`, `TestPolicyName_DeterministicAndPrefixed`, `TestPublish_FiltersAndEnqueues`, `TestPublish_DropsWhenQueueFull`, `TestReconcileGC_DeletesOrphanKeepsLive`, `TestReconcileGC_IgnoresUnmanagedPolicies`, `TestRun_AppliesQueuedTransitionThenStopsOnCancel`, `TestRun_ReconcileTickGarbageCollects`, `TestPolicyEqual`, `TestOwnerExists`, `TestMultiSink_FansOutToEveryMember`, `TestMultiSink_SkipsNilMembers`, `TestIntegration_ApplyIdempotentReapplyAndOrphanGC`, `TestNetworkPolicyConfig_Validate`, `TestNetworkPolicyConfig_Defaults` | n/a |
| `c3.7.x-netpol-quarantined` | §3.7.3 | `internal/response/netpol/`, `docs/runbook.md`, `docs/traceability.md` | `internal/response/netpol/manager_test.go`, `internal/response/netpol/manager_run_test.go`, `internal/response/netpol/netpol_integration_test.go` | `TestEnforcedState_AcceptSet`, `TestPublish_FiltersAndEnqueues`, `TestPolicyNameFor_PerStatePrefixedSameSuffix`, `TestBuildPolicy_QuarantinedDenyAll`, `TestBuildPolicy_RestrictedGoldenUnchangedAlongsideQuarantine`, `TestHandle_QuarantineAppliesAndDeletesRestricted`, `TestHandle_QuarantineWithoutPriorRestrictedIsNoOpDelete`, `TestDeleteSupersededRestricted_NoOpWhenAbsent`, `TestHandle_QuarantineIdempotentReapply`, `TestHandle_QuarantineUnresolvableSkipped`, `TestIntegration_RestrictedToQuarantineAtomicReplacement` | n/a |
| `c3.6.1-sigma-parser-spike` | §3.6.1 | `spikes/sigma-parser/` | `spikes/sigma-parser/wrap/main_test.go`, `spikes/sigma-parser/custom/main_test.go` | `TestFixturesAgainstRule`, `TestRuleMatchShape`, `TestParseOLTExtrasRejectsViolations`, `TestSeverityFallbackFromLevel`, `TestBuildCorpusFailsOnAnchorDrift`, `TestLintID`, `TestPatternMatchesModifiers`, `TestPatternMatchesErrors`, `TestEmptyPatternRejected`, `TestParseAndCondition`, `TestValidateRejectsDialectViolations`, `TestEvaluateExercisesParseAndCondition` | n/a |
| `c3.7.x-netpol-deescalation` | §3.7.3 | `internal/response/netpol/`, `internal/response/fsm/fsm.go`, `cmd/olaitan/main.go`, `docs/runbook.md`, `docs/traceability.md` | `internal/response/netpol/manager_test.go`, `internal/response/netpol/netpol_integration_test.go`, `internal/response/fsm/fsm_test.go` | `TestRemovalState_Predicate`, `TestPublish_EnqueuesRemovalStates`, `TestHandle_RestrictedDeletesSupersededQuarantine`, `TestHandle_RestrictedWithoutPriorQuarantineIsNoOpDelete`, `TestDeleteSupersededQuarantine_NoOpWhenAbsent`, `TestHandle_SuspiciousRemovesBothPolicies`, `TestHandle_CleanRemovesBothAndVerifiesAbsence`, `TestHandle_CleanWithNothingPresentIsCleanNoOp`, `TestHandle_RemovalSkipsExcludedNamespace`, `TestHandle_RemovalMalformedWorkloadIDIsError`, `TestHandle_CleanReDeletesStraySurvivor`, `TestReconcileGC_TargetRestrictedDeletesStaleQuarantine`, `TestReconcileGC_TargetQuarantinedDeletesStaleRestricted`, `TestReconcileGC_TargetSuspiciousDeletesBothWhenOwnerExists`, `TestReconcileGC_NotOkLeavesPolicyWhenOwnerExists`, `TestReconcileGC_NotOkOrphanStillCollected`, `TestReconcileGC_NilOraclePreservesShippedBehaviour`, `TestCurrentState_TrackedFlag`, `TestIntegration_DeescalationLifecycle`, `TestIntegration_DeescalationResidueReconcile`, `TestIntegration_CleanNoOpWithNothingPresent` | n/a |
| `c3.7.4-criu-checkpoint-spike` | §3.7.4 | `spikes/criu-checkpoint/` | `spikes/criu-checkpoint/main_test.go` | `TestKubeletCheckpointURL`, `TestTruncate` | n/a |
| `c3.8-helm-chart-skel` | §3.8 | `deploy/helm/olaitan/` | `deploy/helm/helm_test.go` | `TestDefaultPermutation`, `TestSubchartsDisabled`, `TestRedisDisabledOnly`, `TestRBACVerbs`, `TestPodSecurityContext`, `TestReplicasGuard`, `TestRedisAuthGuard`, `TestAuditWebhookCABundleGuard`, `TestEndpointsTemplated`, `TestAggregatorIsSingletonRecreate`, `TestNetworkPolicyDefault`, `TestKubeconform` | n/a |
| `c3.7.x-operator-override-pin` | §3.7.3 | `internal/response/override/`, `internal/response/fsm/fsm.go`, `internal/schema/state.go`, `internal/keys/keys.go`, `internal/redis/setters.go`, `internal/config/config.go`, `cmd/olaitan/main.go`, `config/olaitan.yaml`, `docs/runbook.md` | `internal/response/override/controller_test.go`, `internal/response/override/publisher_test.go`, `internal/response/override/override_integration_test.go`, `internal/response/fsm/fsm_test.go`, `internal/keys/keys_override_test.go`, `internal/redis/setters_override_test.go`, `internal/config/config_override_test.go`, `internal/nats/nats_test.go` | `TestPin_EmitsOverrideTransition`, `TestPin_RejectsInvalidTarget`, `TestPin_IdempotentSameState`, `TestEvaluate_NoOpWhilePinned`, `TestReleasePin_ResumesScoreDrivenControl`, `TestPin_CleanRoutesRemoval`, `TestReconcile_AnnotationPinsAndWritesRedis`, `TestReconcile_PodOverOwnerPrecedence`, `TestReconcile_OwnerAnnotationApplies`, `TestReconcile_MostIsolatingTieBreak`, `TestReconcile_ExcludedNamespaceSkipped`, `TestReconcile_IdempotentReapply`, `TestResolveWorkloadID_Variants`, `TestOverride_RoundTrip`, `TestSetOverride_WritesNativeTTL`, `TestOverrideConfig_Validate`, `TestIntegration_AnnotationObservationAndTTL`, `TestIntegration_PinDrivesEnforcementSink`, `TestStreamConfigsCoversOverridesApplied` | n/a |
| `c3.7.x-operator-override-ttl` | §3.7.3 | `internal/response/override/`, `internal/keys/keys.go`, `internal/redis/setters.go`, `docs/runbook.md`, `docs/schema-versioning.md`, `docs/traceability.md` | `internal/response/override/controller_test.go`, `internal/response/override/override_integration_test.go`, `internal/redis/setters_override_test.go` | `TestReconcile_DefaultTTLWhenAbsent`, `TestReconcile_MalformedTTLDefaultsNotRejected`, `TestReconcile_TTLExpiryReleases`, `TestReconcile_ManualRemovalReleasesAndClearsRedis`, `TestReconcile_RejectsPreservedKilled`, `TestReconcile_RejectsUnknownState`, `TestSetOverride_TTLExpiry`, `TestStore_ListActiveExpiry`, `TestStore_PutGetDeleteRoundTrip`, `TestIntegration_TTLExpiryReleases`, `TestIntegration_ManualRemoval`, `TestIntegration_RejectedOverride` | n/a |

## Provenance (bootstrap rows, in arrears)

Per AC4, the four bootstrap rows above carry their merged-PR coordinates here
(rather than as extra table columns) so the matrix remains schema-pure to
NFR42 while the auditable chain is preserved row-for-row. New PRs from Story
1.6 onward append their own provenance block here whenever they add a row.

### `c3-traceability-bootstrap`

- **Story:** 1.5 (Traceability matrix bootstrap and PR template). Self-row, back-filled from Story 1.6 because PR number and merge SHA were not knowable at Story 1.5 PR-open time (chicken-and-egg).
- **Merge SHA:** `215f36e` (PR #12, 2026-05-04). Three commits on the merged branch: `21206dd` (initial), `2f310b0` (CI permissions fix on the new traceability job), `26d579c` (Copilot review patches: PR template wording aligned with gate, `strip_html_comments` docstring corrected).
- **FRs/NFRs:** NFR42 (satisfied; the matrix-or-rationale CI gate is now active on every PR). NFR34 (forward reference only; runbook sample row lands with Story 6.8).
- **ADRs:** none (cross-cutting infrastructure; no deferred decisions recorded).
- **eval_run_ids rationale:** infrastructure / process gate, not evaluation-bearing.

### `c3.4-helm-evaluation-wiring`

- **Story:** 1.19 (Helm chart wiring for sensing-only mode and the Epic 5 evaluation matrix).
- **Merge SHA:** `8bf26dba4983a9c095b1ccb8e8656f38d9cfa1c2` (round-2 follow-up; rebase-merge HEAD on `main` after PR #28 closed 2026-05-26T11:09:06Z). Branch `epic-1/story-1-19-helm-evaluation-wiring` carried 19 commits on rebase: `b1c025d` (main implementation `Story 1.19: Helm chart wiring for sensing-only mode (RS configuration)`), 17 intermediate CI/e2e hardening commits (`4071bfa` chart image.repository fix → `c16e03b` baseline-extractor dst_ip key fix) including `f102254` round-1 correlator pod-fetch patch and `aa014cf` config none-sentinel patch, and `8bf26db` (round-2 follow-up applying 33 patches across D1-D7 + P1-P26 from the second bmad-code-review).
- **PR:** #28 (merged 2026-05-26).
- **FRs/NFRs:** FR53 (the chart supports the four-way evaluation matrix `F` / `RS` / `RSL` / `RSLT-full` plus the internal RSLT ablation through configuration alone; Story 1.19 lands the `F` and `RS` arms end-to-end and pre-wires `RSL` / `RSLT` for Epic 3 Story 3.x). NFR13 (reproducibility; the per-arm canonical values are pinned in the chart, so an operator override under a named arm is intentionally clobbered to keep the across-arm comparison valid). NFR31 (`helm template` golden-file diff; three goldens land at `deploy/helm/testdata/golden/` covering the `default` / `RS` / `F` permutations with `Source:` lines and chart/app-version labels normalised out). NFR42 (this row records the chart-to-thesis chain).
- **Surface:** one new top-level `evaluation.config` knob (`""` / `F` / `RS` / `RSL` / `RSLT`) plus one `analyst.provider` knob (`none` / `api` / `local`). Setting `evaluation.config` to a canonical arm overlays three downstream knobs (`rules.enabled`, `baselines.enabled`, `analyst.provider`) with the per-arm canonical values; the operator-supplied individual knobs flow through verbatim when `evaluation.config=""`.
- **Helm helpers:** four new named templates in `_helpers.tpl`: `olaitan.evaluation.validate` (fail-fast guard on both enums), `olaitan.evaluation.effectiveRulesEnabled`, `olaitan.evaluation.effectiveBaselinesEnabled`, `olaitan.evaluation.effectiveAnalystProvider`. The configmap.yaml bridge invokes the validator at the top of the rendered `olaitan.yaml` block and overlays the three effective values via anchor-guarded `regexReplaceAll` mirroring the Story 1.15 / 1.17 bridge pattern. The `analyst:` block lives at column 0 in `config/olaitan.yaml` so its anchor uses a `\nanalyst:\n  ` prefix rather than the 2-space indent the rules/baselines bridges use.
- **Binding interpretations:** the chart-side default for `analyst.provider` is `"none"` (the file-side default in `config/olaitan.yaml` stays at `"api"` as the future-default for Epic 3 Story 3.x); the bridge overlays the chart-side default onto the rendered output. `RSL` and `RSLT` are wired but functionally inert until Epic 3 lands the L1/L2/Senior chain; selecting either today renders `analyst.provider: api` even though the analyst ring is not constructed.
- **Golden-file harness:** `deploy/helm/testdata/golden/{default,rs,f}.golden.yaml` are byte-stable snapshots of the rendered chart. `normaliseGolden` in `helm_test.go` strips `# Source:` comments and redacts `helm.sh/chart: olaitan-<semver>` plus `app.kubernetes.io/version: "<semver>"` so a chart version bump does not force a golden refresh. Regeneration: `HELM_GOLDEN_UPDATE=1 go test -tags=helm -run TestGoldenFile ./deploy/helm/... -count=1`.
- **Kind smoke test:** `tests/e2e/rs_smoke_test.go` (build tag `e2e`) drives a single-node kind cluster via `make e2e-local`. Synthetic events are published to `EVENTS.raw` via the local NATS client because Falco's eBPF probe is unavailable inside kind nodes (kind nodes are containers; eBPF is host-scoped); the assertions cover `olaitan_decision_rules_matches_by_attribute_total{rule_id="OLT-PRIV-001"} >= 1`, `olaitan_decision_baseline_deviations_total >= 1`, `olaitan_correlator_evidence_packages_total >= 1`, and `olaitan_source_healthy == 1` for every enabled source.
- **CI:** a new `e2e` job in `.github/workflows/ci.yml` runs alongside `go` / `helm` / `docker`, using `helm/kind-action@v1.12.0` (kind v0.26.0). Expected runtime ~5-7 minutes; the four CI jobs run in parallel so `e2e` becomes the critical path.
- **Round-2 follow-up (2026-05-26):** bmad-code-review on PR #28 surfaced seven decision-needed items; all seven resolved with the recommended (research-backed) options per the `feedback_web_search_for_decisions` memory. Material round-2 changes that extend this row:
  - **D1 (AC2 F-arm purity):** new `olaitan.evaluation.isFArm` helper plus four F-arm-gated bridges in `configmap.yaml` disabling `detection.sources.{audit,containerd,calico}.enabled` and `detection.posture.enabled`. New tests `TestEvaluationConfig_F_DisablesAllNonFalcoSources`, `TestEvaluationConfig_RS_DoesNotForceNonFalcoSourcesOff`. `f.golden.yaml` regenerated to show `posture.enabled: false` under the F arm. AC2 verbatim now holds.
  - **D2 (Story 1.14 BI P11):** `internal/correlator/correlator.go` extends `identityCacheEntry` to cache the resolved `*corev1.Pod` alongside identity; `internal/correlator/trigger.Trigger` gains a `Pod *corev1.Pod` field; `internal/correlator/assembler/assembler.go` cache-warm branch reads `tr.Pod` instead of issuing a fresh `Pods.Get`. Test `TestAssemblerSkipsKubeFetchWhenResolvedIdentitySupplied` restored to the original P11 contract ("no kube actions at all when ResolvedIdentity+Pod are supplied").
  - **D3 (scope-creep retro):** story file Tasks 12-15 added retroactively documenting the production Go surface changes; this row now reflects them as production source.
  - **D4 (e2e topology honesty):** story file Task 8.6 reworded to describe the synthesised-EvidencePackages design (correlator's 60s rising-edge gate makes the spec's "100 raw events" path infeasible inside a 30s smoke test budget); richer end-to-end coverage remains in Epic 5.
  - **D5 (NFR8 fail-fast):** `cmd/olaitan/main.go` `wireBaselineEngine` fails fast when `Detection.Baselines.Enabled && os.Getenv("REDIS_PASSWORD")==""`; `TrimRight` guards trailing-newline secretKeyRef pitfalls.
  - **D6 (NATS sizing honesty):** new `OLT_NATS_STREAM_MAXBYTES_OVERRIDE` env var in `internal/nats/streams.go` caps each stream's `MaxBytes`; `nats.streamMaxBytesOverride` chart value plumbs it through `deployment.yaml`. The CI `--set nats.config.jetstream.fileStore.maxSize=200GB` band-aid is replaced by `--set nats.streamMaxBytesOverride=1073741824`.
  - **D7 (validator alignment):** `_helpers.tpl` `olaitan.evaluation.validate` normalises `analyst.provider` via `lower` before the enum check so `analyst.provider=NONE` no longer renders an error. New test `TestEvaluationConfig_FailsFast_OnCaseInsensitiveProvider`. Matches the Go-side `strings.ToLower(a.Provider)` lenience.
  - **Patches applied:** P1-P26 plus P27-P33 (decision-derived). 9 deferred items recorded in `_bmad-output/implementation-artifacts/deferred-work.md` under the round-2 heading. 11 findings dismissed as noise. All tests pass: `go build`, `go vet`, `golangci-lint`, `go test -race ./...`, `go test -tags=helm ./deploy/helm/...`, `helm lint`. Em-dash audit on user-owned content clean; AI-authorship audit clean.
- **eval_run_ids rationale:** the chart is the substrate Epic 5's evaluation harness installs against; per-cell run identifiers backfill once Story 5.1 lands.

### `c3.4-applog-sidecar-adapter`

- **Story:** 1.9 (Application log sidecar adapter, opt-in).
- **Merge SHA:** `f0845974db72597844bdd784957b4b52d2fcee3c` (PR #18, merged 2026-05-11). Branch carried three commits: `df6c515` (main implementation), `3447d2d` (round-1 bmad-code-review follow-up), and `b9abc5f` (round-2 Copilot follow-up: `MaxLineBytesAbsoluteCap = 192 KiB` enforced in `Config.Validate` + `effectiveMaxLineBytes` + Helm fail-fast; `tail.go` long-line-policy comment rewritten now that Validate guarantees override < `scannerBufferLimit`; `inject.go` `buildResourceRequirements` + `buildSidecarContainer` propagate `ParseQuantity` errors via the existing `injectErrorTotal` counter instead of silently injecting empty Requests/Limits; inline justification for the read-write peer volumeMount citing the FR5 cooperation contract; `OLAITAN_WEBHOOK_SIDECAR_IMAGE` moved to the required env-var block with fail-fast guard; `APPLOG.md` subcommand corrected from `olaitan applog-injector` to `olaitan applog-webhook`; this row's `test_ids` synced to actual `func Test` names; bench-test invocation comment now documents `-tags=integration`).
- **FRs/NFRs:** FR5 (satisfied; the sidecar tails an opt-in workload's stdout / stderr from a shared `emptyDir` log volume and produces canonical `schema.Event` records of `Source=SourceAppLog` + `Category=CategoryLog`). FR6 (satisfied; per-event publish to `subjects.RawAppLog` via `jetstream.WithMsgID(ev.ID)` for at-least-once with server-side dedup over the JetStream 2-minute window). FR8 (partially satisfied; the in-process `sourcehealth.Tracker` is wired and Story 1.12 will bind `Adapter.Health()` to the Prometheus gauge `source_healthy{source="applog"}`). FR13 (satisfied; the sidecar populates `PodRef{Name, Namespace, UID, Node}` from the downward API at startup and treats it as constant per-process). NFR1 (satisfied; the bench `BenchmarkAdapter_PublishLatency` measures p50=441.96µs / p99=1.006ms receive→PubAck on a real embedded NATS server (2026-05-11 re-measure on the dev machine: Intel i7-10510U, benchtime=10s, n=999) -- well under the 50ms p99 ceiling, with the AC3 50ms gate baked in via `b.Fatalf`). NFR11 (informed; the admission webhook's apiserver hop is mTLS via the cert-manager Certificate or a manually-provided `kubernetes.io/tls` Secret -- documented in `deploy/helm/olaitan/APPLOG.md`). NFR14 (informed; the webhook Deployment runs nonroot, read-only root filesystem, all capabilities dropped, no privilege escalation -- Pod Security `restricted` compliant; the sidecar itself runs nonroot and read-only with an injected SecurityContext). NFR35 (satisfied; ≥80% line coverage on `internal/collector/applog/` and `internal/admission/applog/`, integration test uses real boundaries (real `os.Pipe` + embedded NATS server + real `httptest.NewTLSServer` for the webhook handler)). NFR36 (informed; integration test exercises the real `os.Pipe` boundary, the real JetStream publish path, and the real `admissionv1.AdmissionReview` decoder -- no mock decoders, no mock NATS publisher, no mock pipe).
- **Architecture / naming reconciliation (five binding interpretations):**
  1. Annotation key `olaitan.io/log-sidecar` is canonical (per AC1); `olaitan.io/log-monitor` (the `architecture.md:463` placeholder) is accepted as a deprecated alias for one Olaitan minor cycle with a WARN log on every hit. To be removed in a future hardening story; an architecture errata note belongs alongside the next architecture-doc update.
  2. Injection mechanism: a `MutatingAdmissionWebhook` owned by Olaitan (option (a) in the story file). The helper-chart (b) and documentation-only (c) alternatives are documented but not implemented. Trade-off: the webhook is a privileged surface (apiserver path), mitigated by `failurePolicy: Ignore`, `namespaceSelector` excluding `kube-system` / `kube-public`, HA replica count, and re-using the already-Trivy-scanned `olaitan-agent` image.
  3. Stdout / stderr access via shared `emptyDir` log volume (option (i)): the cooperating application is responsible for redirecting its stdout / stderr to `/var/log/app/stdout.log` and `/var/log/app/stderr.log` -- documented in `deploy/helm/olaitan/APPLOG.md`. Option (ii) hostPath `/var/log/pods/<ns>_<pod>_<uid>/<container>/0.log` is documented as a future-hardening alternative for non-cooperative apps but not implemented.
  4. Source enum: `schema.SourceAppLog="applog"` and `subjects.RawAppLog="olaitan.events.raw.applog"` were already present in the codebase from Story 1.6's bootstrap (verified at file-level). No edits to `internal/schema/event.go` or `internal/subjects/subjects.go` were needed; only the regression test extension in `internal/nats/nats_test.go` (`TestStreamConfigsCoversAppLogSubject`) is new.
  5. Sidecar binary location: multi-call binary subcommands `olaitan applog-sidecar` and `olaitan applog-webhook` (option (b) in the story file), consistent with the existing `cmd/olaitan-eval/` precedent. One image, one Dockerfile, one Trivy scan, smaller registry surface.
- **Native sidecar requirement (KEP-753):** the default `applogSidecar.useNativeSidecar=true` injects the sidecar into `spec.initContainers` with `restartPolicy: Always`. The `SidecarContainers` feature gate is on by default in Kubernetes 1.29 (Olaitan's pinned cluster version). For older clusters or operators with the gate explicitly disabled, the chart values include `applogSidecar.useNativeSidecar=false` which falls back to injecting into `spec.containers`. Locked in by `TestInject_NativeSidecarPatchShape` and `TestInject_FallbackToContainers_WhenNativeSidecarDisabled`.
- **Watchdog design difference vs Story 1.7 (audit), shared with Story 1.8 (CRI):** app-log streams are quiet by design (a stable batch job can run for hours emitting no log lines). The staleness watchdog therefore does NOT flip `MarkUnhealthy` on staleness alone -- it requires staleness AND a recent (within the same `StalenessTimeout` window) non-EOF reader error. Locked in by `TestRun_StalenessWatchdog_QuietButHealthy_DoesNotFlipUnhealthy`.
- **Failure-domain guardrail (NEW for Story 1.9):** a sidecar runs INSIDE the workload pod and shares the workload's failure domain. A panicking sidecar must NOT take down the workload. `Adapter.Run` wraps every errgroup goroutine body with `defer recover()` (item 18 of the Story 1.6 / 1.7 / 1.8 patch list); a recovered panic flips the source unhealthy and the run loop returns nil so the process exits cleanly with code 0. The native-sidecar `restartPolicy: Always` then restarts the sidecar without affecting the workload. Locked in by `TestRun_TopLevelPanic_MarksUnhealthy_DoesNotPropagate`.
- **User-controlled content guardrail (NEW for Story 1.9):** stdout / stderr lines are user-controlled content with no schema. `Translate` sanitises via `bytes.ToValidUTF8` (replacement char U+FFFD), caps the per-line payload at 64 KiB (well below JetStream's 256 KiB MaxMsgSize), strips control characters and NUL bytes from the human-readable Summary via `sanitizeForTag` (256-byte cap, control-char strip), preserves NUL in the structured Raw payload (legal UTF-8 byte; some legacy daemons emit NUL-delimited records), and tags `truncated:true` / `encoding:replaced` whenever those guards fire. Pod labels are forwarded only under the `app.kubernetes.io/*` and `olaitan.io/*` whitelist with `sanitizeForTag` applied to every value. Locked in by `TestTranslate_LineLongerThan64KiB_Truncated`, `TestTranslate_InvalidUTF8_Replaced`, `TestTranslate_EmbeddedNUL_PreservedInRaw_StrippedInSummary`, `TestTranslate_ControlCharsInLabel_Sanitized`, `TestTranslate_NonOlaitanLabelsDropped`.
- **External dependency added:** `github.com/nxadm/tail v1.4.8` (the actively-maintained fork of the abandoned `hpcloud/tail`; provides inotify-driven tailing on Linux with rotation handling). Transitive deps: `gopkg.in/tomb.v1`. License audit: BSD-3-Clause for `nxadm/tail` and `tomb.v1`, MIT for transitive `fsnotify` (already in go.sum from another dep).
- **ADRs:** none. The five binding interpretations above are inline in this row and in the package docstrings (`internal/collector/applog/translate.go`, `internal/admission/applog/webhook.go`); a future ADR may capture the option (ii) hostPath migration if that hardening story lands.
- **eval_run_ids rationale:** infrastructure adapter; the bench is for AC3 verification, not for the Epic 5 evaluation harness's per-scenario runs.

### `c3.4-audit-webhook-receiver`

- **Story:** 1.7 (Kubernetes audit webhook receiver).
- **Merge SHA:** *pending* (this PR).
- **FRs/NFRs:** FR2 (satisfied; the receiver ingests `audit.k8s.io/v1` `EventList` batches from the kube-apiserver and produces canonical `schema.Event` records of `Source=SourceAudit` + `Category=CategoryAudit`). FR6 (satisfied; per-event publish to `subjects.RawAudit` via `jetstream.WithMsgID(ev.ID)` for at-least-once with server-side dedup over the JetStream 2-minute window). FR8 (partially satisfied; the in-process `sourcehealth.Tracker` is wired and Story 1.12 will bind `Adapter.Health()` to the Prometheus gauge `source_healthy{source="kube_audit"}`). NFR1 (informed; the bench `BenchmarkAdapter_PublishLatency` measures p50/p99 receive→PubAck on a real embedded NATS server -- production-class measurement under load awaits Story 5.1 eval harness). NFR11 (informed; mTLS `RequireAndVerifyClientCert` is enforced on the receiver, with rejection counters for `method_not_allowed` / `unsupported_media_type` / `payload_too_large` / `decode_error`). NFR35 (satisfied; ≥80% line coverage on hand-written files in `internal/collector/audit/` and `internal/sourcehealth/`, integration test uses real boundaries (embedded NATS server + real HTTPS+mTLS handshake against synthetic CA)). NFR36 (informed; integration test exercises the receiver against the real `audit.k8s.io/v1` parser path and the real JetStream publish path -- apiserver-side audit policy enforcement is operator configuration, not part of the receiver contract; full envtest-driven flow deferred per Story 1.7 Task 8.5).
- **Architecture reconciliation:** The architecture document and the AC text use the phrase "ValidatingWebhookConfiguration audit-webhook variant". This is incorrect Kubernetes terminology -- admission webhook backends and audit webhook backends are distinct features. Story 1.7 implements the audit webhook backend (configured via the apiserver `--audit-webhook-config-file` flag pointing at a kubeconfig Secret); the existing `templates/validatingwebhookconfiguration.yaml` admission stub from Story 1.1 is left alone. See `deploy/helm/olaitan/AUDIT.md` for the full reconciliation and operator wiring.
- **ADRs:** none. The Path A vs Path B TLS choice (operator-supplied static cert vs cert-manager) is documented inline in Task 7 and `AUDIT.md`; a future ADR may capture the cert-manager migration if and when that hardening story lands.
- **Audit-policy filter scope:** `omitStages: [RequestReceived]`; `level: None` for high-volume kubelet/controller-manager read paths (events, endpoints, leases, nodes); `level: RequestResponse` for `secrets`, `configmaps`, RBAC and NetworkPolicy mutations; `level: Request` for `pods/exec`, `pods/portforward`, `pods/attach`; `level: Metadata` everywhere else. Documented in `deploy/helm/olaitan/files/audit-policy-default.yaml` and tunable via `auditWebhook.policy.custom`.
- **eval_run_ids rationale:** infrastructure adapter; the bench is for AC4 verification, not for the Epic 5 evaluation harness's per-scenario runs.

### `c3.4-calico-flow-adapter`

- **Story:** 1.10 (Calico CNI flow adapter).
- **Merge SHA:** `a2c30034f018ec8b59075747dc993f7d0e489f79` (merged 2026-05-12T20:29:24Z).
- **PR:** [#19](https://github.com/olokotoh/olaitan/pull/19).
- **FRs/NFRs:** FR4 (satisfied; the adapter ingests `goldmane.Flows.Stream` `FlowResult` messages from the Calico Goldmane gRPC API and produces canonical `schema.Event` records of `Source=SourceNetwork` + `Category=CategoryFlow`. The conditional descope path documented in `epics.md:514-533` is **not** activated because Story 1.3 succeeded). FR8 (partially informed; the in-process `sourcehealth.Tracker` is wired with the human-facing label "calico" (see *Naming-conflict note* below) and Story 1.12 will bind `Adapter.Health()` to the Prometheus gauge `source_healthy{source="calico"}`). FR13 (partially informed; the `FlowKey.source_name` is GenerateName-derived per the spike's hand-off note, so the adapter populates `PodRef.Name` verbatim and emits a `pod_name_kind:generatename` tag so the correlator (Story 1.14) can drive K8s API enrichment via the on-demand workload posture client (Story 1.11). Doing the K8s lookup inline would violate the read-on-demand posture pattern at `architecture.md:455-460` and break the NFR1 latency budget). NFR1 (satisfied; `TestBench_NFR1ReceiveToPublishP99` measures translate+publish over 1000 fixture iterations against embedded NATS with a hard `t.Fatalf` at p99 > 50 ms. Measured p99 = ~363us, ~140x headroom under the gate). NFR22 (informed; per-source 1000 events/sec rate-limit circuit breaker is Story 1.13's deliverable; Goldmane's 15s aggregation acts as a natural ceiling but the formal cap is downstream). NFR35 (satisfied; ≥80% line coverage on `internal/collector/cni/`, integration tests use real boundaries (real `grpc.NewServer` + real TCP listener + real mTLS handshake against a fresh self-signed CA + embedded NATS server)). NFR36 (informed; integration tests exercise the real protobuf decoder, the real JetStream publish path, the real gRPC framing, and the real mTLS handshake. Kind-cluster + Tigera operator + traffic-generator e2e test deferred per Task 6 to a future Epic 6 hardening pass).
- **Architecture reconciliation:** AC1 names the source `NETWORK_FLOW`; the schema constant is `SourceNetwork = "network"` (mirrors the five-source enum bootstrap from Story 1.6). AC1 names the category `flow`; the schema constant is `CategoryFlow = "flow"`. Subject is `subjects.RawNetwork = "olaitan.events.raw.network"` already in place from Story 1.6's speculative bootstrap; no schema/subjects/stream edits were needed beyond a new regression-net assertion `TestStreamConfigsCoversNetworkFlowSubject`.
- **Naming-conflict note:** the schema enum value is `"network"` (the abstract category, on the wire), while the source-health gauge label is `"calico"` (the human-facing provider name an operator sees in Grafana). The asymmetry is intentional and documented in `internal/collector/cni/translate.go` package docstring -- a future reader should not "fix" the inconsistency.
- **Bootstrap migration:** Story 1.10 carries the one-time-per-cluster Calico install-path migration (manifest install on v3.29.0 -> Tigera operator install on v3.31.5) flagged by ADR-2026-04-30-01's hand-off subsection. `deploy/demo/setup.sh` bumps `CALICO_VERSION` and switches to the operator-install pair (`tigera-operator.yaml` + `custom-resources.yaml`); `hack/bootstrap-kubeadm.md` rewrites the "Install Calico CNI" section to walk through the operator install. The dev sandbox cannot run kubeadm end-to-end (same constraint as Story 1.1 AC5); hardware verification is deferred to operator follow-up logged in `_bmad-output/implementation-artifacts/deferred-work.md`.
- **mTLS topology:** Goldmane enforces mTLS; the chart wires two operator-facing provisioning paths (Path A cert-manager-issued; Path B operator-supplied PEMs). The adapter loads the three TLS file paths from disk on every connect-loop iteration so cert-manager-driven rotations are picked up without an adapter restart (Story 1.10 guardrail 20).
- **Watchdog quiet-by-design:** like Stories 1.8 (CRI) and 1.9 (applog), Goldmane flows are quiet by design in a low-traffic cluster. The watchdog only flips `MarkUnhealthy` when staleness AND a non-Ready connection state coincide; staleness alone never trips it. Locked in by `TestRun_WatchdogQuietWhenReady_DoesNotFlipUnhealthy`.
- **External dependency vendoring:** `internal/collector/cni/goldmanepb/` carries vendored Goldmane proto stubs pinned to upstream SHA `2e4da40144aac869e1ed2cc220b6c4b62f32efdd` (the `v3.31.5` tag commit of `projectcalico/calico` at `goldmane/proto/api.proto`). Apache 2.0; compatible with the project's MIT licence. The `google.golang.org/grpc v1.81.0` and `google.golang.org/protobuf v1.36.11` deps were already in the main module via Stories 1.6 / 1.8.
- **ADRs:** ADR-2026-04-30-01 (Calico flow record export mechanism: Goldmane gRPC API chosen, manifest-install alternative rejected); ADR-2026-05-12-01 (Calico bootstrap migration to Tigera operator install on v3.31.5; historical correction of ADR-2026-04-27-01's v3.29.0 claim).
- **eval_run_ids rationale:** infrastructure adapter; the bench is for AC2 verification, not for the Epic 5 evaluation harness's per-scenario runs.

### `c3.4-evidence-correlator`

- **Story:** 1.14 (Sliding-window correlator and EvidencePackage assembly).
- **Merge SHA:** pending.
- **PR:** pending.
- **FRs/NFRs:** FR9-FR13 (satisfied/informed by `internal/correlator/`: per-workload sliding windows, three trigger inputs, posture attachment, deterministic overflow summarisation, and orphan-pod fallback workload IDs). NFR2 (covered by trigger-to-publish integration substrate; full production p99 measurement lands with Epic 5 evaluation harness). NFR42 (satisfied for this story by deterministic package IDs, stable overflow ordering/counts, and this traceability row).
- **Architecture reconciliation:** AC text names `EVENTS.raw` and `EVIDENCE.packages`; the live subject hierarchy uses `subjects.RawPrefix + ">"` for raw events and `subjects.EvidencePackages` (`olaitan.evidence.packages`) for EvidencePackages. The old DFIR/containment-shaped `schema.EvidencePackage` was renamed to `schema.ContainmentEvidence`; the Ring-2 package now owns `schema.EvidencePackage`.
- **Helm/config wiring:** `detection.correlator` carries `window_duration`, `max_package_bytes`, `multi_signal_min_sources`, and `high_severity_threshold`. The chart bridges the top-level `correlator:` values block into the rendered ConfigMap via anchor-guarded regex replacements and fails fast if `maxPackageBytes` diverges from 131072.
- **eval_run_ids rationale:** runtime detection substrate; Epic 5 will record scenario-level runs once the rule engine and baseline engine feed this correlator.

### `c3.4-olt-sigma-rule-engine`

- **Story:** 1.15 (OLT Sigma rule engine with hot-reload).
- **Merge SHA:** pending.
- **PR:** pending.
- **FRs/NFRs:** FR15 (rule hot-reload via fsnotify on the ConfigMap-projected `/etc/olaitan/rules` directory; reload-rejected log path retains the previous corpus). FR49 (config hot-reload: the `detection.rules` knobs (`enabled`, `path`) reach the engine through `config.Manager.Subscribe`). FR8 / FR14 (rule matching: every consumed `EvidencePackage` is evaluated against the loaded corpus and per-match `schema.RuleMatch` annotations re-enter the Ring-2 trigger contract via the correlator's `FireRuleMatch` entry point). NFR3 (detection latency: bench + integration latency gate confirm p99 ≤ 100 ms across the four spike-inherited fixtures × a 50-rule corpus; locally measured p99 is in the 30-1000 µs range). NFR30 (OLT Sigma as a strict superset of SIGMA-HQ: the parser wraps `github.com/runreveal/sigmalite` without source patches and adds the OLT-only `attack:` + severity convention on top of upstream-parseable rules). NFR35 (>=80% line coverage on `internal/decision/rules/...`; measured during pre-push validation). NFR42 (this row + the per-package code mapping records the code-to-thesis chain).
- **ADRs:** ADR-2026-04-28-01 (Sigma rule parser strategy: wrap-existing-parser path, sigmalite commit `f180cb50a6a1bba454874c844500bd79f4b30a41`, gopter v0.2.11; Hand-off section drove the package layout and the bench fixture migration).
- **Architecture reconciliation:** The architecture.md "Sigma Rule Engine" section originally cited `k8s.io/client-go/tools/cache.NewFilteredListWatchFromClient` for ConfigMap watching. ADR-2026-04-28-01 corrects this to fsnotify-on-projected-volume, matching the Story 1.6/1.14 hot-reload substrate. Story 1.15 rewrites the architecture.md paragraph so future readers see the live substrate.
- **Package layout:** root `internal/decision/rules/` hosts the JetStream-consuming `Engine`; three subpackages own the parser, matcher (FieldResolver), and loader (Watcher + Corpus). The engine package imports the correlator only through a one-method `RuleMatchEmitter` interface so future cross-rule chaining does not invite an import cycle.
- **Apache 2.0 NOTICE:** `internal/decision/rules/parser/NOTICE` carries the sigmalite attribution per Apache License §4(d). The Olaitan project as a whole remains MIT-licensed; the NOTICE applies only to the parser package.
- **Per-match fan-out:** if N rules match a single inbound `EvidencePackage`, the engine calls `FireRuleMatch` N times producing N downstream packages. Batching is intentionally not supported; the trigger contract carries a single `RuleMatch` per `Trigger`.
- **Re-entrancy guard:** packages whose `Trigger.Type == trigger.TypeRuleMatch` are short-circuited and counted via `olaitan_decision_rules_skipped_self_total`. Without the guard a rule that matches its own downstream package would infinite-fold-back.
- **Helm/config wiring:** `detection.rules` adds `Path` (default `/etc/olaitan/rules`) and `Enabled *bool` (default true, pointer per Story 1.14 D2 precedent). The chart's `configmap.yaml` bridge propagates both knobs via `mustRegexFind + regexReplaceAll` with anchor guards. Sensing-only deployments set `--set rules.enabled=false` to skip the engine entirely.
- **Metrics surface (FR50):** five metrics under `olaitan_decision_rules_*`: `loaded` gauge, `evaluations_total{outcome=match|miss|error}` counter, `reloads_total{outcome=success|rejected}` counter, `skipped_self_total` counter, `evaluation_seconds` histogram with NFR3-sized buckets. `Registry.RegisterHistogram` is added to `internal/metrics/metrics.go` for the histogram (no helper existed before; the previous registry surface only exposed counters and gauges).
- **eval_run_ids rationale:** runtime detection substrate; Epic 5's harness will record scenario-level runs once Stories 1.17-1.18 add the baseline engine and observability.

### `c3.4-olt-rule-corpus-initial`

- **Story:** 1.16 (Initial OLT rule corpus covering S1-S5).
- **Merge SHA:** `5fd67d1` (PR #25, merged 2026-05-23T08:38:10Z by `olokotoh`). Branch `epic-1/story-1-16-initial-olt-rule-corpus` carried three commits: `4b70117` (main impl), `a2e660b` (Round 1 bmad-code-review follow-up applying 22 patches across D1-D10 and P1-P12), and `c7f7ff1` (Round 2 bmad-code-review re-review follow-up applying 18 patches across R2-D1-R2-D7 and R2-P1-R2-P11). Rebase-merged onto `main` so the linearised history points at `c7f7ff1` as origin/main HEAD; `gh api` reports merge_commit_sha `5fd67d1cd282ae224e99d16a4bd381710c8ad811`. Post-merge backfill commit `chore: backfill Story 1.16 merge SHA in traceability matrix` updates this row.
- **PR:** #25.
- **FRs/NFRs:** FR14 (rule corpus is what the engine matches against: ten OLT rules under repo-root `rules/<category>/` covering the five evaluation scenarios with at least two rules per scenario). NFR30 (every committed rule passes the SIGMA-HQ superset gate via `parser.ParseRule`; the lint test `TestCorpusLint_AllRulesParse` re-uses the same parser the engine startup gate runs so there is exactly one source of truth for what makes a rule legal, and `TestCorpusLint_FailsOnMissingAttackAnnotation` locks the AC4 missing-annotation rejection in by asserting the exact `attack: required field is missing` error string the parser emits). NFR42 (this row plus the Provenance block below records the corpus to thesis-claim chain; the helm staging artefact is a build output covered by `deploy/helm/olaitan/files/` in `.gitignore`).
- **Corpus inventory:** ten rules pinned in the §Dev Notes table at `_bmad-output/implementation-artifacts/1-16-initial-olt-rule-corpus-10-rules-covering-s1-s5.md`. S1 (T1611): `OLT-PRIV-001` (CAP_SYS_ADMIN/PTRACE acquisition, sev 90), `OLT-EXEC-001` (host PID namespace entry, sev 75). S2 (T1552/T1552.005): `OLT-CRED-001` (ServiceAccount token read by non-system process, sev 75), `OLT-CRED-002` (cloud metadata IP outbound, sev 50). S3 (T1613): `OLT-RECON-001` (cluster-scope pod listing by non-system SA, sev 50), `OLT-LATERAL-001` (kubectl exec in tenant Pod, sev 75). S4 (T1071/T1071.001): `OLT-NET-001` (outbound to non-RFC1918 with small payload from tenant Pod, sev 50), `OLT-NET-002` (outbound TCP to C2-favoured ports from tenant Pod, sev 50). S5 (T1496): `OLT-IMPACT-005` (cryptominer process pattern, sev 75; byte-identical to spike POC fixture per BI-6), `OLT-IMPACT-006` (stratum mining pool port outbound, sev 75).
- **Architecture reconciliation:** `architecture.md` already declared the corpus structure at the directory tree (`├── rules/                                # OLT Sigma rule corpus`) and the ATT&CK Containers v18 reference. Story 1.16 implements that paragraph exactly; no architecture-doc edit required (story Task 9.1).
- **Adapter-to-canonical field normalisation (BI-1):** the OLT dialect spec uses canonical field names (`process.exe`, `network.dst_port`, `file.path`). Adapter `translate.go` files emit adapter-native names in `Event.Raw` today (Falco wraps fields inside `output_fields`, audit emits its own flat keys, CNI emits Calico/Goldmane shape). Story 1.16 rules use the canonical OLT names; AC3 is satisfied via hand-built integration fixtures at `internal/decision/rules/testdata/scenarios/S{1..5}/package.json` whose `Event.Raw` is keyed by canonical names. A separate follow-up story owns the per-adapter normalisation that would let real cluster Falco / audit / CNI events match these rules end-to-end. This is recorded as a follow-up entry in `_bmad-output/implementation-artifacts/deferred-work.md` (under the Story 1.16 code-review deferred section) so Epic 5's reproducible evaluation harness picks the dependency up before S1-S5 runs go live.
- **Helm/config wiring:** `Makefile` gains `helm-prepare-rules` which flattens `rules/<category>/*.yaml` into `deploy/helm/olaitan/files/rules/<basename>` because Helm's `.Files.Glob` uses `path.Match` and does not support recursive globbing. The chart's `templates/configmap.yaml` now populates the `olaitan-rules` ConfigMap by enumerating `.Files.Glob "files/rules/*.yaml"` and emitting one key per file (basename = key, contents = value via `.Files.Get`). The mount surface at `templates/deployment.yaml` (aggregator volume + readOnly mountPath) is unchanged; the Story 1.15 fsnotify watcher picks up the ConfigMap projection without any controller restart.
- **AC2 enforcement (severity + attack + falsepositives):** `parser.ParseRule` already enforces the severity 0-100 range (`extractSeverity` rejects out-of-range integers and explicit nulls) and the attack non-empty list with valid ATT&CK ID regex (`extractAttack`). The falsepositives half is enforced by Story 1.16's lint test asserting `len(rule.FalsePositives) > 0` for every walked rule, because sigmalite stores the field but does not require it. The three CI-side gates together satisfy AC2 with exactly one parser-truth shared with the engine startup gate.
- **AC4 enforcement (PR rejection on missing ATT&CK):** `TestCorpusLint_FailsOnMissingAttackAnnotation` constructs an in-memory invalid rule in a tempdir, walks it through the same parser the engine uses at startup, and asserts the exact error string `attack: required field is missing`. If a future contributor changes the parser's error string, the lint test breaks before a PR with a malformed rule could ever pass the existing `go` CI job; no `.github/workflows/ci.yml` edit is required (story Task 7.1).
- **eval_run_ids rationale:** corpus substrate; Epic 5's harness will record scenario-level runs once the corpus is consumed by Epic 5's harness via the engine landed in Story 1.15. The five hand-built fixtures at `testdata/scenarios/S{1..5}/` validate AC3 per rule but do not constitute evaluation runs; the harness produces those.

### `c3.6.3-trust-bound`

- **Story:** 2.1 (Deterministic ThreatScore calculator). Implements FR30 (`prd.md:504`) and pins the algebraic trust-bound `max(LLM-only) = LLMWeight × LLMCap = 0.3 × 35 = 10.5 < 20` (SUSPICIOUS threshold per `prd.md:294`) via the property test `TestProperty_NoLLMOnlyEscalation`.
- **Merge SHA:** TBD (post-merge backfill commit `chore: backfill Story 2.1 merge SHA in traceability matrix` will replace this once the PR lands).
- **PR:** TBD.
- **FRs/NFRs:** FR30 (formula `0.4 × rule_severity + 0.3 × normalised_baseline_deviation + 0.3 × llm_capped_confidence`; LLM term zero in Epic 2 with the slot wired and ready for Epic 3 `epics.md:1589` to populate). FR49 (Helm-tunable weights hot-reload via `internal/config/watcher`; the calculator reads `config.Manager.Get()` on every `Score()` invocation, so a `helm upgrade --set score.ruleWeight=0.5` lands at the next package without a controller restart). NFR3 (per-invocation latency well under the 100 ms p99 detection-ring ceiling; `score_metrics_test.go` registers `olaitan_decision_score_evaluation_seconds` with `ExponentialBucketsRange(1e-6, 100e-3, 10)`). NFR35 (property-based tests via `gopter` for the score-range invariant + trust-bound + monotonicity). NFR42 (this row plus the runbook scenario row records the code-to-thesis chain; the literal `c3.6.3-trust-bound` identifier comes from the NFR42 sample row in `prd.md:611`).
- **Package layout:** `internal/decision/score/` hosts `score.go` (Calculator + Config + algorithm), `score_test.go` (14 unit tests), `score_property_test.go` (6 gopter properties including the algebraic trust-bound and the real `*config.Manager` fsnotify hot-reload integration property), `score_metrics_test.go` (3 metric-registration tests). Constructor signature is `score.New(mgr *config.Manager, registry *metrics.Registry) (*Calculator, error)`; a nil `mgr` falls through to `DefaultConfig()` for test ergonomics in `cmd/olaitan/main_test.go`.
- **Binding interpretations:** BI-1 the AC6 phrasing `internal/response/score/` is loose; `architecture.md:439` ("`internal/decision/score`, not `internal/decision/threat_score`") is the authoritative path. The FSM (Story 2.2) at `internal/response/fsm/` consumes the score; the score itself lives upstream in the decision ring per the file-tree at `architecture.md:780-787`. BI-2 `RuleMatch.Severity` is a decimal-string-encoded int (`parser.SeverityString()` always renders `strconv.Itoa(r.Severity)` where `r.Severity ∈ [0, 100]`); the calculator parses with `strconv.Atoi` and deliberately does NOT use `severitybucket.Score` (which would accept keyword labels and let the Epic 3 LLM tier drift the contract). BI-3 the calculator uses `*config.Manager.Get()` (lock-free atomic load) per `Score()` call rather than `Subscribe` because it holds no derived per-config state; Story 2.2's FSM will use Subscribe for the same reason Story 1.13's rate-limiter does (it holds thresholds in its own state). BI-4 the SigmaNormaliser is derived from `detection.baselines.sigma_multiplier` rather than declared as a separate `score.sigma_normaliser` knob, so a single operator knob controls both the deviation gate and the calculator's normalisation step. BI-5 the FR30 weights validator rejects sums > 1.0 + 1e-9 (1e-9 tolerance for float round-off when an operator sets `0.333 × 3`); rejecting weights that would break the trust-bound stops the calculator from emitting scores above SUSPICIOUS under LLM-only input. BI-6 Story 2.1 deliberately does NOT add evaluation-arm overlays for score weights, because the F arm disables the rules and baseline engines entirely (Story 1.19), so no input ever reaches the calculator in the F arm; documenting this in `values.yaml` so a future operator does not "fix" a non-bug.
- **Helm/config wiring:** `detection.score` adds `RuleWeight *float64`, `BaselineWeight *float64`, `LLMWeight *float64`, `LLMCap *int` (pointer fields per the Story 1.14 D2 / 1.15 / 1.17 precedent so the loader distinguishes "operator omitted, default" from "operator explicitly set 0"). The chart's `configmap.yaml` bridges the four knobs via anchor-guarded `mustRegexFind + regexReplaceAll` mirroring the Story 1.15 / 1.17 pattern; the bridge runs BEFORE the Story 1.19 evaluation overlay because score weights are unchanged across arms.
- **Metrics surface (FR50):** three metrics under `olaitan_decision_score_*`: `evaluations_total` counter (unlabelled, increments once per `Score()` call), `score_total` histogram-vec labelled `{component_contribution, result_bucket}` with buckets `[0, 25, 50, 75, 100]` and the `result_bucket` label re-using the Story 1.18 `severitybucket.Bucket` helper (3 × 4 = 12 series cardinality envelope), `evaluation_seconds` histogram with `prometheus.ExponentialBucketsRange(1e-6, 100e-3, 10)` aligned to the NFR3 ceiling.
- **Latency envelope:** algorithm is `O(|RuleMatches| + |BaselineDeviations|)` with a single `mgr.Get()` atomic load per call and three `WithLabelValues(...).Observe(...)` per call; expected p99 well under 100 µs (3 orders of magnitude below NFR3 ceiling).
- **Coverage:** `go test -cover ./internal/decision/score/` reports 88.4% line coverage, above the AC6 80% floor.
- **eval_run_ids rationale:** the algebraic trust-bound is machine-checked by `TestProperty_NoLLMOnlyEscalation`; the empirical FR55 counterpart (100-trial adversarial-LLM experiment) is Epic 5 work and will populate this field once Story 5.6 lands.

### `c3.6.4-fsm-escalation`

- **Story:** 2.2 (FSM state machine and transition logic). Implements FR31 (deterministic `CLEAN -> SUSPICIOUS -> RESTRICTED -> QUARANTINED` escalation, one state at a time, with per-state dwell guards) in `internal/response/fsm/`. The FSM is the documented Story 2.2 consumer that closes the Story 2.1 `_ = scoreCalc` discard in `cmd/olaitan/main.go`.
- **Merge SHA:** TBD (post-merge backfill commit `chore: backfill Story 2.2 merge SHA in traceability matrix` will replace this once the PR lands).
- **FRs/NFRs:** FR31 (escalation FSM, satisfied). NFR6 (deterministic, idempotent transitions, pinned by `TestProperty_Idempotent` and `TestProperty_MonotonicEscalation`). NFR35 (gopter property tests, >= 1000 randomised sequences per property; `internal/response/fsm/` line coverage 92.0%). NFR42 (this row plus `c3.6.5-fsm-deescalation` records the code-to-thesis chain).
- **Package layout:** `internal/response/fsm/` hosts `fsm.go` (`Machine` + `Evaluate` sole-mutator + `State` read-only query + `TransitionSink`/`NopSink` seam + `Clock`), `fsm_test.go` (unit + metrics tests), `fsm_property_test.go` (gopter properties). Constructor is `fsm.New(mgr *config.Manager, registry *metrics.Registry, sink TransitionSink, clock Clock) (*Machine, error)`.
- **Binding interpretations:** BI-1 canonical path `internal/response/fsm/` (Ring 4). BI-2 reused and EXTENDED `schema.StateTransition` (added `WorkloadID`/`PackageID`/`Reason`, all `omitempty`) with no consumer break; bumped the schema to `state_transition.v2` in `docs/schema-versioning.md` + `docs/schemas/state_transition.yaml`. BI-3 reused `ConfidenceBands` for thresholds and set the defaults to 20/40/70 in `config/olaitan.yaml` (they were 40/70/90). BI-4 added `FSMConfig` (dwell + cooldown only) mirroring `ScoreConfig`. BI-7 PRESERVED_KILLED unreachable, pinned by `TestProperty_PreservedKilledUnreachable`.
- **eval_run_ids rationale:** deterministic FSM logic is machine-checked by the property suite; no evaluation-harness run is required (the Epic 5 harness exercises the end-to-end pipeline, not the unit-level transition invariants).

### `c3.6.5-fsm-deescalation`

- **Story:** 2.2 (FSM state machine and transition logic). Implements FR32 (de-escalation one step per cooldown elapse when the rolling ThreatScore stays continuously below the current state's threshold) in `internal/response/fsm/`.
- **Merge SHA:** TBD (shared backfill commit with `c3.6.4-fsm-escalation`).
- **FRs/NFRs:** FR32 (de-escalation with cooldown, satisfied). The rolling sub-threshold semantics (a single low sample does NOT de-escalate) are pinned by `TestDeescalation_RollingResetsOnSpike` and the one-step-per-cooldown invariant by `TestProperty_DeescalationOneStepPerCooldown`.
- **eval_run_ids rationale:** as `c3.6.4-fsm-escalation`.

### `c3.6.6-fsm-state-persistence`

- **Story:** 2.3 (FSM state persistence in Redis with restart recovery). Implements FR37 (persist FSM state to `fsm:{workload_id}` plus `fsm:{workload_id}:history` and recover on controller restart) and NFR24 (FSM state restored from Redis within 60 s of pod readiness) in `internal/response/fsm/`.
- **Merge SHA:** TBD (post-merge backfill commit will replace this once the PR lands).
- **FRs/NFRs:** FR37 (durable FSM-state persistence + restart recovery, satisfied). NFR24 (restart recovery; the in-memory map rehydrates from all `fsm:*` keys via `Machine.Restore` before the FSM consumer starts). NFR8 (mandatory Redis AUTH via `REDIS_PASSWORD`, reused from the deployment env wired for the baseline engine). NFR36 (integration tests hit a real miniredis boundary, not a mock). NFR35 (`internal/response/fsm/` coverage maintained above the 80% floor with the new store/sink/restore tests). NFR42 (this row records the code-to-thesis chain).
- **Package layout:** `internal/response/fsm/store.go` (Redis-backed `Store`: CAS `Save` + history append + `LoadAll` restore + `fsm_state.v1` (de)serialisation), `internal/response/fsm/sink_redis.go` (`RedisSink` implementing the Story 2.2 `TransitionSink` seam, with a bounded outage buffer + `Run` replayer), and `Machine.Restore(ctx, *Store)` in `fsm.go`. Redis transport added in `internal/redis/setters.go` (`SetFSMStateCAS`, `GetFSMState`, `AppendFSMHistory`, `ScanFSMStateKeys`); key builders in `internal/keys/keys.go` (`FSMState`, `FSMHistory`, `FamilyFSM`).
- **New metric introduced:** `olaitan_response_fsm_state_recovered_total` (counter; workloads rehydrated from Redis on restart; AC5).
- **Binding interpretations:** BI-1 NEW `fsm:` key family (durable, keyed by the canonical three-segment `workload_id`) rather than reusing the 1h-TTL two-segment `state:` family that cannot round-trip the WorkloadID and would expire durable state. BI-2 NO TTL on the `fsm:` family (state must survive an arbitrary restart gap). BI-3 `fsm:{workload_id}` hash written via compare-and-swap on `current_state` + `fsm:{workload_id}:history` Redis list (`RPUSH` + `LTRIM` to 1000). BI-4 persistence wired through the `TransitionSink` on actual transitions only (no Redis write on the non-transition hot path; NFR3 preserved). BI-5 `Machine.Restore` seeds the map before the consumer starts so a restored workload is never re-initialised to CLEAN. BI-6 clock-on-restore clamps `stateEnteredAt` (closes deferred item DW2.2-2) and resets the cooldown anchor to restore-now (restart invalidation, conservative). BI-7 outage buffer + replay via `internal/retry`, idempotent by CAS (closes deferred item DW2.2-1 for same-workload ordering). BI-8 `FSMConfig.PersistenceEnabled` + `RedisAddr` mirror `BaselinesConfig`; `wireFSMPersistence` mirrors `wireBaselineEngine` (NFR8 `REDIS_PASSWORD` + `TrimRight`).
- **Helm/config wiring:** the Story 2.3 `fsm.persistenceEnabled` + `fsm.redisAddr` knobs are bridged into the rendered ConfigMap by a `configmap.yaml` regex bridge mirroring `baselines.redisAddr` exactly (BI-8): `fsm.redisAddr` defaults to empty in `values.yaml` and resolves to the bundled subchart's `<release>-redis-master:6379` Service via the `olaitan.endpoints.redis` helper, so the baked `redis:6379` dev-host literal never reaches a real cluster. Helm tests `TestFSMPersistenceConfigMapBridgedFromValues`, `TestFSMPersistenceRedisAddrResolvesEndpoint`, and `TestFSMPersistenceAnchorsPresentInOlaitanYAML` lock the bridge; goldens regenerated. `REDIS_PASSWORD` is already wired unconditionally in `deployment.yaml` (its comment explicitly anticipates "any future aggregator-side Redis client"), so no deployment change was required.
- **Round-3 review note:** an initial implementation shipped FSM config via the baked `config/olaitan.yaml` only (no values bridge). The round-3 gate review (Acceptance Auditor) flagged that the `olaitan.endpoints.redis` helper resolves to `<release>-redis-master:6379`, so the baked `redis:6379` would make the FSM dial the wrong Redis in-cluster (a real correctness bug, not just operability). The follow-up added the `values.yaml` block + `configmap.yaml` bridge per BI-8, resolving the finding.
- **eval_run_ids rationale:** runtime persistence substrate; the Epic 5 harness exercises restart recovery end-to-end once Story 5.4 lands.

### `c3.7.3-netpol-restricted`

- **Story:** 2.4 (NetworkPolicyManager RESTRICTED-state enforcement). Implements FR33 (apply a NetworkPolicy blocking external-CIDR egress when a workload enters RESTRICTED) and NFR6 (FSM transition to NetworkPolicy presence within 1 s p99) in `internal/response/netpol/`.
- **Merge SHA:** `a85c56d` (PR [#32](https://github.com/olokotoh/olaitan/pull/32), squash-merged to the `epic-2-staging` integration branch 2026-06-03; pending review before `main`). The branch carried three commits: `c470829` (initial implementation), `370beb5` (round-1 bmad-code-review follow-up: empty-selector guard, skipped-not-error reclassification, latency-observed-on-success, package-id-omit, metric series pre-init, plus Dev Agent Record amendments 3-4), and `7ff0fc5` (round-2 follow-up: fix the GC orphan-leak regression so managed orphans are collected regardless of namespace exclusion, and implement the `NETPOL_INTEGRATION_SKIP=1` opt-out per Copilot).
- **FRs/NFRs:** FR33 (RESTRICTED egress-block policy, satisfied; egress allow-list of RFC 1918 + cluster CIDRs + extra CIDRs + explicit DNS, all other egress denied). NFR6 (apply latency measured from the transition timestamp via `olaitan_response_network_policy_apply_seconds`; the async worker applies a single in-flight policy with no batching delay). NFR12 (no RBAC change: the aggregator ClusterRole already grants `networkpolicies` create/update/delete/get/list plus `apps`/`batch` get/list for owner resolution; no `cluster-admin`, no wildcards). NFR36 (the envtest integration suite hits a real kube-apiserver boundary, not a mock). NFR35 (`internal/response/netpol/` line coverage 81.7%, above the 80% floor). NFR42 (this row records the code-to-thesis chain).
- **Package layout:** `internal/response/netpol/` hosts `manager.go` (`Manager` implementing the `fsm.TransitionSink` seam, async `Publish` + `Run` worker, idempotent `apply`, and the orphan-GC `reconcileGC`), `policy.go` (deterministic `PolicyName`, managed labels/annotations, egress allow-list builder), and `selector.go` (canonical workload-id parse + owner-to-podSelector resolution). The FSM fan-out is `internal/response/fsm/multisink.go` (`MultiSink`). Constructor: `netpol.New(cfg Config, cs kubernetes.Interface, registry *metrics.Registry, log *slog.Logger) (*Manager, error)`.
- **New metrics introduced:** `olaitan_response_network_policy_apply_seconds` (histogram; transition-to-apply latency for NFR6) and `olaitan_response_network_policy_apply_total{result}` (counter; result in applied/noop/error/skipped/dropped/gc_deleted). The fuller observability surface (active-policy gauge) is Story 2.9.
- **Binding interpretations:** BI-1 enforcement is hosted in `startAggregatorRing` in-process with the FSM (the single-binary aggregator ClusterRole already holds the NetworkPolicy verbs), resolving the epics "olaitan-controller" vs prd "agent SA" split with no RBAC change. BI-2 NEW Ring-4 package `internal/response/netpol` takes a `kubernetes.Interface` directly and does NOT import Ring 1 (`internal/collector/*`, incl. posture) or `internal/decision/*`; the posture NetworkPolicy reader is a copy reference only. BI-3 the single FSM sink is fanned out via the new `fsm.MultiSink` so the Redis persistence sink (2.3) and the netpol manager both receive transitions. BI-4 non-blocking `Publish` enqueues to a bounded channel; a background `Run` applies with `client-go` retry, mirroring `RedisSink`. BI-5 acts only on `ToState == RESTRICTED` (QUARANTINED is 2.5, de-escalation removal is 2.6). BI-6 deterministic policy name `olaitan-restricted-<sha256(workload_id)[:12]>` + get-then-create-or-update with `RetryOnConflict` + skip-when-equal for idempotent re-apply. BI-7 podSelector resolved by parsing the canonical workload-id and reading the owner `spec.selector` via `apps/v1` + `batch/v1` (orphan-pod fallback uses the pod's own labels). BI-8 egress ALLOW-LIST semantics (`policyTypes: [Egress]`; allow RFC 1918 + cluster CIDRs + explicit DNS; deny the rest) rather than `ipBlock`-`except`. BI-9 labels `app.kubernetes.io/managed-by=olaitan` (+ name/component) and annotations `olaitan.io/fsm-state` + `olaitan.io/package-id` + `olaitan.io/workload-id`. BI-10 orphan GC via a 30 s API-reconcile (the literal CRI "lifecycle adapter" path is deferred since 30 s meets the 60 s budget). BI-11 a `response.network_policy` config block (disabled by default) with cluster-CIDR and reconcile-interval knobs.
- **Amendments to the story spec (Dev Agent Record):** (1) BI-8's `text/template` policy rendering was implemented as a typed `networkingv1.NetworkPolicy` builder instead, because the variable `matchLabels` and CIDR list make a YAML text template error-prone and force a parse round-trip; the egress allow-list semantics are preserved exactly. (2) BI-11's `values.yaml`/`configmap.yaml` value surfacing is deferred to Story 2.10 (which owns the graduated-isolation mode overlays), to avoid premature list-bridging while the manager is off by default; the `response.network_policy` block with safe disabled defaults ships in `config/olaitan.yaml`, so the helm goldens carry it. (3) BI-7 named `batch/v1` CronJob as a resolvable owner, but a CronJob has no `spec.selector` (it owns Jobs which own pods, with no stable cross-CronJob selector); CronJob enforcement is deferred and classified as a `skipped` result (not an `error`), pending Job-lineage label resolution in a follow-up. (4) BI-10/Task 6.1 specified orphan GC on owner-NotFound OR zero-running-pods; the zero-running-pods branch is deferred (a scaled-to-zero owner retains its RESTRICTED policy, which is benign as no pods exist to leak), GC acting only on owner-NotFound.
- **eval_run_ids rationale:** the enforcement path is machine-checked by the envtest integration suite against a real kube-apiserver; the Epic 5 harness exercises the end-to-end detect-to-isolate latency (NFR6) once Stories 5.2 to 5.4 land.

### `c3.7.x-netpol-quarantined`

- **Story:** 2.5 (NetworkPolicyManager QUARANTINED-state enforcement). Implements FR34 (apply a NetworkPolicy blocking all ingress and all egress when a workload enters QUARANTINED) and NFR6 (FSM transition to NetworkPolicy presence within 1 s p99, shared with Story 2.4) as an in-place extension of the shipped Story 2.4 `internal/response/netpol/` package. No new package, host process, config block, metric family, wiring, or RBAC change.
- **Merge SHA:** `954a810` (PR [#33](https://github.com/olokotoh/olaitan/pull/33), squash-merged to the `epic-2-staging` integration branch 2026-06-03; pending review before `main`). Branch `epic-2/story-2-5-networkpolicymanager-quarantined-state-enforcement` carried four commits: `df3...` initial implementation, `27b132b` (round-1 follow-up: explicit apply-before-delete result gate + failure-injection tests), `df7389c` (Copilot follow-up: corrected additive-allow-list semantics wording + reconcile supersession backstop + `superseded` result label), and the squash. Two rounds bmad-code-review (regression hunt clean) + Copilot.
- **FRs/NFRs:** FR34 (QUARANTINED deny-all policy, satisfied; `policyTypes: [Ingress, Egress]` with both rule slices left nil, which is the canonical "isolate this pod completely" expression in additive `networking.k8s.io/v1` semantics; no DNS carve-out, unlike RESTRICTED). NFR6 (the QUARANTINED apply feeds the same `olaitan_response_network_policy_apply_seconds` histogram observed from the transition timestamp; no new latency path). NFR12 (no RBAC change: the aggregator ClusterRole already grants `networkpolicies` create/update/delete/get/list; the supersession delete uses the already-present delete verb). NFR36 (the envtest case `TestIntegration_RestrictedToQuarantineAtomicReplacement` hits a real kube-apiserver boundary). NFR35 (touched-package coverage held at or above the 80% floor; Story 2.4 shipped 81.7%). NFR42 (this row records the code-to-thesis chain).
- **Binding interpretations:** BI-1 the Story 2.4 BI-5 trigger guard in `Manager.Publish` is widened from a RESTRICTED-only check to an accept-set (`enforcedState`) admitting BOTH `schema.StateRestricted` and `schema.StateQuarantined`; every other `ToState` still returns early, and `fsm.go`/`MultiSink`/`startAggregatorRing` are untouched. BI-2 `buildPolicy` is generalised to render by `st.ToState`: RESTRICTED keeps the egress allow-list unchanged; QUARANTINED renders the deny-all spec; `AnnFSMState` is set to `string(st.ToState)` so AC4's `olaitan.io/fsm-state: QUARANTINED` is satisfied by the same code path that satisfies RESTRICTED, and `managedLabels()` is reused verbatim. BI-3 a state-keyed name helper `policyNameFor(state, workloadID)` returns `olaitan-quarantined-<sha256(workloadID)[:12]>` for QUARANTINED and the existing `olaitan-restricted-<hash>` for RESTRICTED (same 12-hex suffix, distinct prefix); the exported `PolicyName` delegates to `policyNameFor(StateRestricted, ...)` so no caller breaks. BI-4 the deny-all is `policyTypes: [Ingress, Egress]` with Ingress and Egress left NIL; no stray empty `{}` rule is emitted (an empty rule element would invert to allow-all). BI-5 supersession is apply-before-delete with distinct names, which is monotonically tightening rather than "strictly more blocked": Kubernetes NetworkPolicies are additive allow-lists, so while both policies select the pod, ingress is denied immediately (only the QUARANTINED policy declares the Ingress policyType, with no allow rules) but egress remains the UNION of both policies' allows, i.e. still the RESTRICTED allow-list (RFC1918/cluster/DNS) - the deny-all does NOT subtract or override those egress allows. The quarantine policy is applied via the existing idempotent `apply`, and ONLY after that returns success is the `olaitan-restricted-<hash>` policy best-effort deleted (`deleteSupersededRestricted`; `IsNotFound` is success; a real delete failure logs WARN and never fails the handle); the old policy is never deleted before the new one is confirmed, so there is never a window less protected than RESTRICTED and never a policy-less window. A failed inline delete leaves a LINGERING restricted policy that keeps egress at the allow-list level (the workload is NOT yet fully isolated and is NOT benign), and orphan GC will not reap it while the owner exists; the periodic reconcile in `reconcileGC` is the supersession backstop that identifies workloads with a QUARANTINED policy (by the `olaitan.io/fsm-state: QUARANTINED` annotation) and deletes their RESTRICTED policies regardless of owner existence, converging egress to deny-all within one reconcile interval (default 30s) and counting `result="superseded"`. BI-6 the async `Publish`->queue->`Run` worker, `resolveSelector`, idempotent `apply`/`policyEqual`, the latency histogram, and the result counter are reused unchanged; the only new code is the trigger accept-set, the name helper, the deny-all builder branch, and the supersession delete. BI-7 metrics reuse the `apply_total{result}` family and `apply_seconds`; the inline supersession delete is silent best-effort, while the reconcile supersession backstop adds a `result="superseded"` label to the existing counter (pre-initialised to 0 alongside the other labels so the series is stable from startup) and no new metric family. BI-8 the unchanged `managed-by=olaitan` orphan-GC selector already collects quarantine policies (the quarantine policy carries the same managed-by label and `olaitan.io/workload-id` annotation); confirmed by the envtest GC assertion. BI-9 the existing `response.network_policy` config block and RBAC (delete verb present) cover QUARANTINED with no change; the deny-all needs no CIDR inputs.
- **Amendments to the story spec (Dev Agent Record):** none. The implementation follows BI-1 through BI-9 as written; no deviation was required.
- **Scope boundary:** de-escalation removal (QUARANTINED->RESTRICTED restore, ->SUSPICIOUS/->CLEAN removal) is Story 2.6 and is deliberately NOT implemented here; the distinct per-state names are what make 2.6's independent removal/restore tractable. No new persisted/wire schema is introduced (the policy lives in the K8s API), so `docs/schema-versioning.md` is intentionally untouched.
- **eval_run_ids rationale:** the QUARANTINED enforcement path is machine-checked by the envtest integration suite against a real kube-apiserver; the Epic 5 harness exercises the end-to-end detect-to-isolate latency (NFR6) once Stories 5.2 to 5.4 land.

### `c3.7.x-netpol-deescalation`

- **Story:** 2.6 (NetworkPolicyManager de-escalation policy removal). Implements FR35 (remove or relax NetworkPolicies in lock-step when a workload de-escalates below its current isolation level) as an in-place extension of the shipped Story 2.4 + 2.5 `internal/response/netpol/` package. No new package, host process, config block, metric family, or RBAC change; the only NEW cross-component seam is the one-method FSM `StateOracle` wired at the `cmd/olaitan` call site.
- **Merge SHA:** `f07a450` (PR [#34](https://github.com/olokotoh/olaitan/pull/34), squash-merged to the `epic-2-staging` integration branch 2026-06-03; pending review before `main`). Branch `epic-2/story-2-6-networkpolicymanager-de-escalation-policy-removal` carried the initial implementation plus a round-1 follow-up (`d593b91`: fix a StateOracle data race by launching the worker after SetStateOracle, verified with `go test -race`; document the removed-count metric granularity). Two rounds bmad-code-review (round 1 caught the race; round 2 regression hunt clean) + Copilot (no findings).
- **FRs/NFRs:** FR35 (de-escalation removal/relaxation, satisfied; QUARANTINED->RESTRICTED applies the egress-only policy and removes the deny-all, and SUSPICIOUS/CLEAN remove every managed policy with CLEAN verifying absence). NFR6 (the QUARANTINED->RESTRICTED relaxation reuses the existing `apply` accounting and latency histogram; a pure-removal path has no apply latency). NFR12 (no RBAC change: the aggregator ClusterRole already grants `networkpolicies` create/update/delete/get/list; the removal deletes and CLEAN verification Gets use already-present verbs). NFR36 (the envtest case `TestIntegration_DeescalationLifecycle` and the residue/no-op cases hit a real kube-apiserver boundary). NFR35 (`internal/response/netpol/` line coverage 83.8%, above the 80% floor and not regressed from the 83.6% Story 2.5 baseline). NFR42 (this row records the code-to-thesis chain).
- **Desired-state-per-target model:** each enforced/removal `ToState` DEFINES the workload's managed-policy set and both the inline `handle` path and the reconcile backstop converge to it: RESTRICTED => {restricted present, quarantine absent}; QUARANTINED => {quarantine present, restricted absent}; SUSPICIOUS/CLEAN => {both absent}. A missed or failed inline op self-heals at the next reconcile.
- **Binding interpretations:** BI-0/BI-1 the FSM de-escalates one step per transition (QUARANTINED->RESTRICTED->SUSPICIOUS->CLEAN), so `Manager.Publish` is widened with a sibling `removalState` predicate (true for SUSPICIOUS+CLEAN) and enqueues when `enforcedState || removalState`; only PRESERVED_KILLED is dropped. BI-2/BI-2a/BI-3 entering RESTRICTED applies the egress-only policy FIRST then best-effort deletes the superseded `olaitan-quarantined-<hash>` deny-all (`deleteSupersededQuarantine`, the exact mirror of Story 2.5's `deleteSupersededRestricted`), gated on `result == applied || noop`; apply-before-delete is forbidden-to-invert here because delete-before-apply would momentarily fully open egress. During the overlap the workload is, if anything, stricter than RESTRICTED (ingress stays denied until the deny-all is gone) and never policy-less. BI-2b/BI-2c the Story 2.5 reconcile supersession backstop ("quarantine object wins") is reworked into an FSM-target-aware desired-state reconciler keyed on a one-method `StateOracle` (`CurrentState(workloadID) (schema.PodSecurityState, bool)`) defined IN the netpol package and satisfied by a new exported `(*fsm.Machine) CurrentState`; target QUARANTINED deletes a stale RESTRICTED policy (count `superseded`), target RESTRICTED deletes a stale QUARANTINED policy (count `removed`, the de-escalation residue, WITHOUT re-deleting the freshly-applied restricted policy), target SUSPICIOUS/CLEAN with the owner present deletes both (count `removed`). BI-4/BI-5 SUSPICIOUS and CLEAN branch at the top of `handle` (after parse + excluded-namespace skip, BEFORE `resolveSelector`) into `removeManaged`, which deletes both `policyNameFor(RESTRICTED, wid)` and `policyNameFor(QUARANTINED, wid)` (`IsNotFound` = success) without a podSelector; CLEAN additionally verifies absence via a follow-up Get per name and re-deletes a stray survivor once. A removal-with-nothing-to-remove is a successful no-op counted `removed`, never `error`. BI-6 removal is idempotent and self-heals via the FSM-target-aware reconcile within one interval. BI-7 a single new `removed` result label is added to the existing `olaitan_response_network_policy_apply_total{result}` counter (pre-initialised to 0 alongside the others; HELP string updated); no new metric family. BI-8 no new config block, Helm, or RBAC change; `go test -tags=helm` goldens unchanged.
- **StateOracle wiring (BI-2c):** the FSM `*Machine` is constructed AFTER the Manager in `startAggregatorRing` (the Manager is a sink fanned into that very Machine), so the oracle is installed via `(*netpol.Manager).SetStateOracle(stateMachine)` once both exist, before the FSM consumer starts. `*fsm.Machine` satisfies `netpol.StateOracle` via the new read-only `CurrentState` (an `m.mu.RLock()` over `m.states[workloadID].current`). A nil oracle (the seam absent) falls back to the shipped Story 2.5 "quarantine object wins" supersession, so the unit suites that construct the Manager without an oracle keep passing and the change is backward-compatible.
- **not-ok reconcile rule (documented choice, BI-2c):** when the oracle returns not-ok (the FSM has NO entry for the workload, e.g. transiently before the map is populated or after an FSM restart that did not restore the workload), the desired-state backstop does NOT delete the policy. The orphan-GC pass (owner NotFound -> `gc_deleted`) is left as the SOLE authority for not-ok workloads: if the owner is truly gone the policy is reaped as an orphan; if the owner still exists the policy is retained until the FSM next evaluates the workload and the backstop gains an opinion. This conservatively avoids stripping protection from a still-running workload after an FSM state loss, at the cost of leaving a stale policy slightly longer for a genuinely de-escalated-but-forgotten workload (bounded by the orphan-GC cadence and repaired the moment the FSM re-evaluates it).
- **Amendments to the story spec (Dev Agent Record):** none. The implementation follows BI-0 through BI-8 as written; BI-2c (the FSM oracle) was implemented as the default, not the BI-2c-alt newer-object-wins fallback. The pre-existing read-only query was already named `State`, so the new tracked-flag query is `CurrentState` (matching the spec's interface signature) rather than overloading `State`.
- **Scope boundary:** this story handles ONLY automated ThreatScore-driven de-escalation transitions; operator-override pins (`olaitan.io/state-override`, FR38/FR39) and their TTL release are Story 2.7 and are NOT implemented here. No new persisted/wire schema is introduced (the policy lives in the K8s API), so `docs/schema-versioning.md` is intentionally untouched.
- **eval_run_ids rationale:** the de-escalation lifecycle and the FSM-target-aware reconcile are machine-checked by the envtest integration suite against a real kube-apiserver; the Epic 5 harness exercises the end-to-end recover-to-relax latency once Stories 5.2 to 5.4 land.

### `c3.4-deterministic-detection-observability`

- **Story:** 1.18 (Deterministic-detection observability surface).
- **Merge SHA:** `db38289` (PR #27 merged 2026-05-23T17:44:50Z by `olokotoh`; rebase-merge style; `gh api` reports `merge_commit_sha` `db3828924c6f55d9e2900cd4f84ba86877294562`; origin/main HEAD after rebase is `db38289` which is the follow-up commit). Branch `epic-1/story-1-18-deterministic-detection-observability` carried two commits: `11aeeb6` (main implementation: three new correlator metric families, the new labelled `matches_by_attribute_total` counter, the `sigma_bucket` label extension on `deviations_total`, the new `internal/decision/severitybucket/` shared helper package, the `internal/correlator/metrics.go` + `internal/decision/baseline/sigma_bucket.go` new files, the operator runbook Section 1 covering 18 metric families with six-field schema, the `c3.4-deterministic-detection-observability` row, and the cmd/olaitan/main.go MetricsRegistry wiring) and `db38289` (round-1 bmad-code-review follow-up applying 12 patches across P1-P11 + D1: P1 sigma_bucket label relabel to non-overlapping `"3-5"`/`"5-10"`/`"10+"` per web-research backed Copilot finding; P2 sigma_bucket_test self-mutating expectation table rewrite; P3 engine_metrics_test three-workload pre-warm at mean=20 / stddev=5.085 driving 37/50/80 dst counts to land deterministically in each bucket; P4 `evidence_packages_total` Help "publish attempts" relabel to "successfully published"; P5 `matches_by_attribute_total` Help false-equivalence removal with multi-MitreTag fan-out disclosure; P6 cap-bypass alert PromQL from `histogram_quantile > 131072` to count-based `+Inf − 131072 > 0`; P7 broken `docs/architecture.md` link at `runbook.md:5` rephrased to point at FYP planning artefacts; P8 added 12 Story 1.13 rate-limit and per-source-error metric families to runbook §1.1 closing the Task 8.3 gap; P9 removed stale "moved to severitybucket" comment block in assembler; P10 Completion Note documenting `internal/decision/rules/severity.go` wrapper inlining; P11 added `log.Warn` on the `publishTrigger` re-marshal failure path so a hypothetical concurrent-mutation bug becomes observable). Four-layer review (Blind Hunter / Edge Case Hunter / Acceptance Auditor / Copilot) per `feedback_web_search_for_decisions`; 9 Copilot line-comments folded in. Five findings deferred (W1-W5: hot-reload retired-rule cardinality leak; `MultiSignalMinSources <= 0` silent rewrite; BI-5 latency claim unmeasured; partial-registration on failure; non-decimal severity Atoi fallthrough). Pre-merge CI 4/4 green on `db38289`: go, helm, docker, traceability. Story file at `_bmad-output/implementation-artifacts/1-18-deterministic-detection-observability-surface.md`. Em-dash audit + AI-authorship audit both 0 hits across all touched files.
- **PR:** #27 (rebase-merged 2026-05-23).
- **FRs/NFRs:** FR50 (Prometheus surface on `:9090/metrics` is mandatory; Story 1.18 adds the three correlator metric families and labels for rule + baseline counters so the surface is complete for the deterministic-detection layer). NFR32 (every metric documented with type, unit, label set; the `docs/runbook.md` Section 1 catalogue is the canonical reference). NFR34 (operator runbook covers the deterministic-detection metric catalogue; Sections 2-10 owned by Story 6.8). NFR42 (this row plus the runbook section records the code-to-thesis chain).
- **New metrics introduced:** `olaitan_correlator_evidence_packages_total{trigger_type}`, `olaitan_correlator_window_size_bytes` histogram, `olaitan_correlator_overflow_summarised_total`, `olaitan_decision_rules_matches_by_attribute_total{rule_id, severity_bucket, attack_technique}`. Label extension: `olaitan_decision_baseline_deviations_total` now carries `{metric, sigma_bucket}` (Story 1.17 introduced the unlabelled-per-metric form; Story 1.18 adds `sigma_bucket`).
- **New helpers landed:** `(*metrics.Registry).RegisterCounterVec`, `(*metrics.Registry).RegisterHistogramVec` (both mirror the existing `RegisterHistogram` ownership shape; the HistogramVec helper is a forward-looking deferral for Stories 2.9 + 3.15 and is not used by Story 1.18 itself). New package `internal/decision/severitybucket/` hosts the shared score-to-bucket helper used by both the rules engine emission path (Story 1.18) and the assembler overflow priority sort (Story 1.14; the `severityScore` and `syslogSeverityScore` functions moved out of the assembler and are now imported as `severitybucket.Score`). New file `internal/decision/baseline/sigma_bucket.go` provides the package-private `sigmaBucket` helper.
- **Binding interpretations:** BI-1 the AC2 singular-ring names (`olaitan_decision_rule_matches_total`, `olaitan_decision_rule_eval_duration_seconds`, `olaitan_decision_rule_corpus_size`) are documentation aliases for the established plural-ring names (`olaitan_decision_rules_*`) per `architecture.md:472-475`; the load-bearing AC2 requirement is the LABEL SET on the per-(rule, severity, technique) counter, which lands as the new `matches_by_attribute_total` to avoid a metric-name collision with the unlabelled aggregate. BI-2 the AC3 `{workload}` label is forbidden by `architecture.md:476` cardinality policy; `warmup_active` stays unlabelled and per-workload telemetry surfaces via `AUDIT.transitions`. BI-3 correlator metrics register through `Correlator.New` when `cfg.MetricsRegistry != nil`, mirroring the rules engine and baseline engine constructor pattern. BI-4 multi-MitreTag rules fan out across the `attack_technique` label so a rule covering two techniques is not under-counted; empty MitreTags emits one bump with the `unknown` sentinel. BI-5 one redundant JSON marshal per publish in the correlator's `publishTrigger` to surface the on-wire byte length to `window_size_bytes` (~1 ms p99 at the 128 KiB ceiling; below the NFR2 trigger-to-publish budget). BI-6 Story 1.18 does NOT introduce Helm knobs; the metrics surface is unconditional per `architecture.md:374` + `internal/metrics/metrics.go:28-31`.
- **Helm/config wiring:** no chart edits required (BI-6). The existing `metrics:` block in `deploy/helm/olaitan/values.yaml` covers the port + address.
- **Cardinality envelope:** Story 1.18 net new series at FYP scale: ~30 (typical) to ~6,000 (worst-case `matches_by_attribute_total` cartesian, unreachable because each rule has a single primary severity). All within NFR32 envelope.
- **Latency envelope:** correlator gains one JSON re-marshal per publish + three atomic counter operations (p99 < 1% of NFR2 budget). Rules engine gains one `WithLabelValues(...).Inc()` per MitreTag per match (sub-microsecond). Baseline engine gains one `WithLabelValues(...).Inc()` per emitted deviation (replaces a direct atomic add, same order of magnitude). None reach the NFR3 100 ms p99 detection-engine budget.
- **eval_run_ids rationale:** runtime detection substrate; Epic 5's evaluation harness wires the metric surface into per-scenario package traces once Story 5.4 + Story 5.5 land.

### `c3.4-welford-baseline-engine`

- **Story:** 1.17 (Welford baseline engine with restart-aware warm-up).
- **Merge SHA:** `53c814d` (PR #26, merged 2026-05-23T13:10:27Z by `olokotoh`). Branch `epic-1/story-1-17-welford-baseline-engine` carried two commits: `c1c0bff` (main implementation) and `9b84a9c` (bmad-code-review follow-up applying 6 Copilot patches (C1-C6) plus the 13 strongest findings from Blind Hunter / Edge Case Hunter / Acceptance Auditor: B1+E6+B14 atomicity (zero-Save before warmup.Begin), B7+E4 explicit AckWait=60s + MaxAckPending=1 (web-validated via oneuptime jetstream guide + byronruth pull-consumer pattern), E3+E7 NaN/Inf rejection at Welford.Update + numberOf boundary (web-validated via Robust Perception NaN convention), A1+E1+C5 warmup-duration hot-reload wiring via new Engine.SetWarmupDuration, A10 restart-event dedupe, C3+E2 hasRestartAttemptTag continue-on-malformed, C1 strconv.FormatFloat for byte-faithful round-trip, C4+B11 real Engine.HandleForBench surface for the latency bench, C6 deep nil-validation in Engine.New, C2 Begin docstring correction, plus the CI fix to cmd/olaitan/main_test.go writeTestConfig adding baselines.enabled=false). Rebase-merged onto `main` so the linearised history points at `9b84a9c` as origin/main HEAD; `gh api` reports merge_commit_sha `53c814db25d87eb5ff3cfabc150ef19693efb0a9`. Post-merge backfill commit `chore: backfill Story 1.17 merge SHA in traceability matrix` updates this row.
- **PR:** #26.
- **FRs/NFRs:** FR16 (per-workload Gaussian baselines for the five default metrics maintained via Welford's online algorithm at `internal/decision/baseline/welford.go`). FR17 (`schema.BaselineDeviation` emission via `Correlator.FireBaselineDeviation` when an observation exceeds `SigmaMultiplier` above the rolling mean). FR18 (restart-aware invalidation + 30-minute warm-up controller persisting through `baseline:<namespace>:<pod>:metrics`). NFR3 (per-package evaluation p99 <= 100 ms; locked in by `TestIntegration_BaselineEngineLatencyBudget` over a 50-workload corpus × 1,000 packages). NFR35 (property-based tests on Welford mathematics via `gopter`; 1,000 randomised input streams + sample-size sweep + permutation invariance + merge additivity). NFR42 (this row plus the binding-interpretation detail records the code-to-thesis chain).
- **Package layout:** `internal/decision/baseline/` hosts the JetStream-consuming `Engine` plus the `Welford` state, the Redis-backed `Store`, the `Warmup` controller, the five `MetricExtractor` implementations, and the property + integration suites. The engine package imports the correlator only through a one-method `BaselineDeviationEmitter` interface so future cross-engine chaining does not invite an import cycle (mirrors Story 1.15 `RuleMatchEmitter`).
- **Five metric extractors (AC1):** `outbound_unique_dst_ips` (network flow events, distinct dst IPs/window), `distinct_exec_paths` (Falco syscall events, distinct `process.exe` / `proc.exe`/window), `outbound_bytes` (network flow events, sum of `bytes_out`/window), `audit_events_per_min` (audit-source events divided by window minutes), `container_restarts_per_hour` (lifecycle events tagged `attempt:N` with `N>0`, divided by window hours).
- **Re-entrancy guard:** packages whose `Trigger.Type == trigger.TypeBaselineDeviation` are short-circuited and counted via `olaitan_decision_baseline_skipped_self_total`. Without the guard a deviation that fires its own downstream package would infinite-fold-back. Mirrors Story 1.15 P8 pattern.
- **Helm/config wiring:** `detection.baselines` adds `Enabled *bool`, `WarmupDuration`, `SigmaMultiplier *float64`, `RedisAddr`. The chart's `configmap.yaml` bridges the four knobs via anchor-guarded `mustRegexFind + regexReplaceAll`. Sensing-only deployments set `--set baselines.enabled=false` to skip the engine and Redis client entirely.
- **Restart semantics (BI-3):** `warmupDuration` and `sigmaMultiplier` are hot-reloadable via `config.Manager.Subscribe`; `enabled` and `redisAddr` are restart-required because they reconfigure `startAggregatorRing`. Documented in the chart values block.
- **Metrics surface (FR50, ahead of Story 1.18 per BI-4):** five metrics under `olaitan_decision_baseline_*`: `evaluations_total{outcome=deviation|normal|error}` counter, `deviations_total{metric}` counter, `warmup_active` gauge, `skipped_self_total` counter, `evaluation_seconds` histogram with NFR3-sized buckets.
- **Binding interpretations:** BI-1 the wire key uses the existing `keys.BaselineMetrics(namespace, pod)` builder (one hash per workload carrying all five metrics as prefixed fields) rather than the AC4 literal `baseline:{workload_id}:{metric}` text, because the existing builder + setter + 48h TTL family policy is the production contract since Story 1.5. BI-2 metric extractors fall back to source-native field names (Falco's `output_fields[].proc.exe`, CNI's `dst_ip` top-level key) until Story 6.x lands adapter-to-canonical normalisation; the shim is recorded as a Story 1.17 follow-up entry in `_bmad-output/implementation-artifacts/deferred-work.md`. BI-3 see Restart semantics above. BI-4 Story 1.17 registers the `olaitan_decision_baseline_*` metric families ahead of Story 1.18 observability. BI-5 `BaselineDeviationResult` AC phrasing is treated as documentation alias for the on-wire `(schema.BaselineDeviation, trigger.TypeBaselineDeviation)` pair; no new schema type is introduced. BI-6 `keys.BaselineWindow` / `redis.SetBaselineWindow` stay reserved for a future rolling-window variant; the running statistics in Welford state are themselves the rolling baseline.
- **eval_run_ids rationale:** runtime detection substrate; Epic 5's harness will record scenario-level runs once Stories 1.18 + Epic 5 harness wire the baseline-engine outputs into per-scenario package traces.

### `c3.4-per-source-rate-limit`

- **Story:** 1.13 (Per-source rate-limit circuit breaker).
- **Merge SHA:** `<merge-sha>` (PR `<pr-number>`, merged `<merge-date>` by `olokotoh`). Branch `epic-1/story-1-13-per-source-rate-limit-circuit-breaker` carries the implementation; post-merge backfill commit `chore: backfill Story 1.13 merge SHA in traceability matrix` updates this row.
- **PR:** `<pr-number>`.
- **FRs/NFRs:** FR51 (partially satisfied; the sensing-layer half lands here as `internal/ratelimit/`, applied at each adapter's pre-publish path. The LLM-tier half lands in a future Story 3.x at `internal/decision/agent/circuitbreaker.go` per `architecture.md:966`). NFR22 (satisfied; the breaker engages at 1000 events/sec/source/node by default with 10% deterministic hash sampling, surfaces a Prometheus counter, hot-reloads via the config.Manager.Subscribe callback). NFR23 (informed; the LLM-tier breaker shares this metric naming convention and registry-isolation pattern). FR49 (informed; the four knobs hot-reload through the existing watcher infrastructure via the Subscribe callback wired in `cmd/olaitan/main.go`). NFR32 (satisfied; cardinality stays at 5 sources x 1 node-per-pod = 5 series per pod by registering `node` as a const-label set from `K8S_NODE_NAME` at metric-construction time). NFR35 (informed; the unit + integration tests on `internal/ratelimit/` cover the publish hot path, the engage/disengage transitions under fake and real clocks, the sampling-fraction chi-squared bound, and the hot-reload contract end-to-end). NFR36 (satisfied; integration tests drive a real `*Limiter`, a real `prometheus.Registry`, a real `httptest.NewServer`, and a real `config.Manager` watching a real file -- no mock substitutions of the rate-limit primitive or the metric registry). NFR42 (satisfied; sampling decisions use FNV-1a hash of `event.ID` so two evaluation re-runs against the same event sequence yield identical sample/drop verdicts, locked in by `TestAllow_SamplingDeterministic`).
- **Architecture reconciliation:** `architecture.md:966` maps FR51 to `internal/correlator/trigger/sampler.go` plus `internal/decision/agent/circuitbreaker.go`. The first half is reinterpreted as `internal/ratelimit/` rather than the correlator path because (a) the breaker is applied at the SENSING boundary on each adapter's publish path, not at the correlator's trigger boundary; (b) `internal/correlator/` is currently only a `.gitkeep` placeholder for Story 1.14 -- putting a sensing primitive inside a not-yet-existing correlator package would invert the dependency direction (sensing imports correlator); (c) Story 1.14 may add a separate correlator-side sampler for fan-out concerns; that separate primitive can live at the architecture's stated path and consume `internal/ratelimit/` if needed. The LLM-tier breaker (`internal/decision/agent/circuitbreaker.go`) is out of scope here and lands in a future Story 3.x.
- **Schema additivity:** `Sampled bool` and `SamplingRate float64` are appended to `internal/schema/event.go` with `json:"sampled,omitempty"` and `json:"sampling_rate,omitempty"` tags. An unsampled event marshals to a byte-identical wire form against the pre-1.13 schema, so the EVENTS.raw `MaxMsgSize` budget (256 KiB per Story 1.9) is unaffected. The `docs/schema-versioning.md` ADR records this as a MINOR version bump per the project's semver convention.
- **Sliding-window measurement:** the limiter measures rate over a 1-second sliding window via a 10-bucket ring (100ms per bucket). Per-`Allow` cost is O(1); no per-instance goroutine is spawned; engage/disengage is evaluated lazily on the hot path (guardrail 30). The ring is protected by a `sync.Mutex` held only over the bucket-increment + transition-evaluation arithmetic; the `OnTransition` callback fires after the mutex is released so subscribers cannot deadlock by calling back into the limiter.
- **Three new guardrails (28-30):** (28) deterministic sampling via stable event-ID hash -- the decision uses `FNV-1a(event.ID) mod 100 < samplingRate*100`, not `math/rand`, so Epic 5 evaluation re-runs are reproducible against the same event sequence; (29) engage/disengage transitions log once -- sustained engagement does NOT emit periodic logs because that would re-amplify the log-volume problem the breaker mitigates; (30) cooldown re-evaluates lazily on the next Allow call -- no per-(source, node) timer goroutine is spawned, eliminating goroutine-leak risk and keeping the limiter at zero idle goroutines.
- **Per-adapter wiring:** every streaming adapter (falco/audit/cri/cni/applog) gains a `RateLimit *ratelimit.Limiter` field on its Config, a `*ratelimit.Limiter` field on the Adapter struct (always non-nil after `New` via a disabled-fallback), an `EngagedTotal() int64` getter delegating to `Limiter.EngagedTotal()`, a `DroppedBySampling() int64` getter for the future Story 1.18 metric binding, and a `Limiter() *ratelimit.Limiter` accessor so the hot-reload Subscribe callback can call `UpdateThreshold/Cooldown/SamplingRate/Enabled` without a process restart. The pre-publish path on each adapter calls `Allow(ev.ID)` and either drops the event, annotates the event with `Sampled=true`+`SamplingRate`, or publishes unmodified depending on the engagement state.
- **Metrics binding:** `olaitan_sensor_circuit_breaker_engaged_total{source, node}` is registered in `cmd/olaitan/metrics.go registerAdapterCounters` for every streaming adapter via the existing `metrics.Registry.RegisterCounter` API. The `node` const-label is sourced from `K8S_NODE_NAME` at metric-registration time, which keeps cardinality bounded under NFR32 (5 sources x 1 node-per-pod = 5 series per pod). The `adapterMetrics` structural-typed interface gains `EngagedTotal() int64` so a new streaming adapter inherits the wiring without amending the per-adapter switch.
- **Helm wiring:** the chart introduces a top-level `rateLimit:` values block with four knobs (`enabled`, `thresholdEventsPerSec`, `cooldownSeconds`, `samplingRate`). The `configmap.yaml` bridge propagates all four knobs into the rendered `olaitan.yaml` via the Story 1.10 P28 `mustRegexFind + regexReplaceAll` pattern, with per-knob anchor checks so a missing anchor fails render with a clear message rather than silently dropping the operator's `--set`. Three new helm tests (`TestRateLimitDefaultsRendered`, `TestRateLimitBridgeRendersThreshold`, `TestRateLimitDisabledRendered`) lock the chart contract in.
- **Hot-reload contract (FR49):** `startCollectorRing` accepts a `subscribe func(func(*config.Config))` parameter that the runtime wires to `mgr.Subscribe`. The callback inspects `newCfg.RateLimit` and calls `UpdateThreshold/Cooldown/SamplingRate/Enabled` on every per-adapter limiter, so a `helm upgrade --set rateLimit.thresholdEventsPerSec=500` propagates within the watcher's debounce window without a process restart. `TestIntegration_RateLimitHotReloadViaManager` exercises this end-to-end against a real file rewrite + fsnotify watcher.
- **Hand-offs:**
  - **Story 1.14 (correlator and EvidencePackage assembly)** will consume `schema.Event.Sampled` in its trigger logic per FR12: sampled events should be deprioritised under EvidencePackage 128 KiB overflow (rule matches first, then baseline deviations, then severity >=50 events, then summarisation; sampled events are summarisation candidates not full-fidelity carriers).
  - **Story 1.18 (deterministic detection observability)** will register dashboards reading `olaitan_sensor_circuit_breaker_engaged_total{source, node}` as the primary back-pressure signal. The expected alert is "any sustained engagement >5 minutes" (`increase(...[5m]) >= 1` for 5 minutes) indicating a workload or attack source the operator should investigate. The same story will bind `DroppedBySampling` to a Prometheus counter so dashboards can show the drop volume.
  - **Story 3.x (LLM-tier circuit breaker)** will register `olaitan_decision_circuit_breaker_engaged_total{tier="llm"}` via the same `metrics.Registry.RegisterCounter` API. The LLM-tier breaker is global (not per-source per-node) and the threshold is 10 LLM-eligible packages per minute per NFR23. The implementation lives in `internal/decision/agent/circuitbreaker.go` per `architecture.md:966`.
  - **Story 4.x (DFIR forensic report writer)** will surface the `Sampled` flag in the report's evidence-fidelity section: a forensic report covering a sampled period must honestly disclose "10 percent of source X's events captured during 14:23:04 -- 14:24:17" rather than implying full-fidelity coverage. This is the FR45-adjacent integrity requirement.
- **External dependency:** no new external dependencies. `hash/fnv`, `sync`, `sync/atomic`, and `time` are all stdlib.
- **ADRs:** none. The binding interpretations above resolve the AC-vs-codebase asymmetries inline. The schema additivity is recorded in `docs/schema-versioning.md`.
- **eval_run_ids rationale:** infrastructure substrate; the rate limiter's engagement during high-volume attacks is observed via metric counters in Epic 5 evaluation runs but is not gate-checked. Production evaluation lands with the Epic 5 scenario runs once Stories 1.18 and 5.6 wire the dashboards and FR55 trials.

### `c3.4-per-source-metrics`

- **Story:** 1.12 (Per-source health and event metrics surface).
- **Merge SHA:** `45bc5af3f8d2a4c25ed3a53b3980710f444714ae` (PR #21, merged 2026-05-14T11:17:59Z). Branch carried three commits: `90d2ecb` (main implementation), `a267241` (bmad-code-review follow-up applying six patches plus two binding-interpretation Dev Notes additions: P1 audit reason bucket labels realigned to the rejectedCounters canonical six in `internal/collector/audit/audit.go:33-40`, P2 scrape-window race closed by registering per-adapter detail counters inside `startMetricsServer` before the HTTP server goroutine starts, P3 `Server.Start` propagates Shutdown errors and non-`http.ErrServerClosed` Serve errors, P4 Helm `containerPort: metrics` blocks wrapped in `{{- if .Values.metrics }}` mirroring the configmap.yaml nil-guard pattern, P5 dead `_ = errors.New` removed from `cmd/olaitan/metrics_test.go` plus the unused `errors` import, P6 misleading `_ = ctx` discard removed from `startMetricsServer`; AC1 binding interpretation #8 documents that applog runs in a per-pod sidecar and is intentionally not wired into the agent's metrics surface in Story 1.12, AC7 binding interpretation #9 documents that `TestIntegration_PromlintZeroProblems` uses an in-process recorder rather than `httptest.NewServer` because promlint is a static text-format check), and `7423364` (Copilot review follow-up applying three additional patches: CR1 `audit.Adapter.RejectedByReasonValue(reason)` single-key locked lookup replaces per-scrape `Snapshot()` map allocations, CR2 `internal/metrics.Registry.RegisterGauge` added and `olaitan_sensor_cni_consecutive_eofs` switched from counter to gauge because `ConsecutiveEOFs()` resets to 0 on success, CR3 `case *applog.Adapter:` added to `registerAdapterCounters` registering the four applog detail counters as `olaitan_sensor_applog_{translate_errors,publish_drops,lines_shed,lost_on_shutdown}_total` for the future sidecar metrics surface tracked in `deferred-work.md` W7). All three commits AC10-clean.
- **PR:** #21 (merged 2026-05-14T11:17:59Z by `olokotoh`).
- **FRs/NFRs:** FR8 (satisfied; every streaming sensor adapter exposes `olaitan_source_healthy{source}` as a Prometheus gauge via `internal/metrics.Registry.RegisterAdapter` reading from the existing `sourcehealth.Reader` on the adapter). FR50 (satisfied; the `:9090/metrics` endpoint is served by `internal/metrics.Server` running alongside the existing `:8080/healthz` kubelet probe surface under the same errgroup; `cmd/olaitan/metrics.go` wires both rings). NFR32 (satisfied; every metric carries a documented `# HELP <one-liner>` and `# TYPE <gauge|counter>` line per the Prometheus text format, with `promlint` enforcement at the integration tier via `TestIntegration_PromlintZeroProblems`; cardinality stays bounded under the five-source enum plus the unlabelled posture counters). NFR35 (informed; the integration substrate is a real `httptest`-style boundary -- `metrics.Server` binds a real TCP listener via `net.Listen` and the test issues real `http.Get` round-trips, with the kernel-assigned-port pattern from `internal/health/server_test.go`). NFR36 (informed; the integration tests never substitute a mock for the Prometheus exposition format -- they use the real `promhttp.HandlerFor` against a real `prometheus.Registry`).
- **Architecture reconciliation:** AC1 names the source label values `network_flow` for the Calico CNI adapter; the existing `schema.SourceNetwork` constant is `"network"`, which is what Story 1.10 actually publishes on. Binding interpretation: use the schema constants verbatim (`falco`, `audit`, `runtime`, `network`, `applog`) so dashboards can join the metric on `schema.Source*` without renaming. The asymmetry between the AC wording and the schema constant is preserved in the metric label so a future operator dashboard reader will see `source="network"` rather than `source="network_flow"`. AC1's "buckets per NFR32" wording was over-specified for a Gauge + Counter pair; the actual NFR32 requirement is type+unit+label-set documentation, which Story 1.12 satisfies via the `# HELP`/`# TYPE` lines. Histogram bucket boundaries land with Stories 1.13/1.14/1.18.
- **Two-server lifecycle:** `internal/metrics.Server` is a separate `http.Server` from `internal/health.Server`. The package docstring on `internal/metrics/metrics.go` documents the rationale: kubelet's probe SLO and Prometheus' scrape SLO have different consumer expectations, and bundling them on one port would force kubelet to inherit scrape latency. The two servers run in parallel under the same errgroup; `TestIntegration_DualServerGracefulShutdown` locks in the no-deadlock invariant on ctx-cancel within the 5s `shutdownTimeout` budget per server.
- **Registry isolation (guardrail 25):** Story 1.12 never touches `prometheus.DefaultRegisterer`. Every `metrics.Registry` is constructed via `prometheus.NewRegistry()` so tests are order-independent and future surfaces (debug exporters, sidecar exporters) can land without colliding on metric names. The same isolation pattern Stories 1.7 and 1.8 used for the `httptest.NewServer` substrate.
- **Reader-side counters (guardrail 26):** every per-adapter counter is registered via `prometheus.NewCounterFunc` reading from the existing adapter-side atomic (`eventsPublished`, `translateErrors`, `publishDrops`, `oversizeDropped`, `consecutiveEOFs`, `rejected`). The metrics layer never owns a writeable counter that the adapter increments; the adapter's atomic remains the single source of truth and there is no duplicate state to synchronise. Locked in by `TestRegisterAdapter_ConcurrentIncrementsNoDataRace` (race detector + 1M concurrent increments + concurrent scrapes).
- **Mandatory endpoint (guardrail 27):** `config.Validate` rejects an empty `metrics.address`; the FR50 surface is not optional. `Load` substitutes the package default `:9090` when the operator omits the block (chart-deploy compatibility); operators who explicitly type `metrics.address: ""` get the rejection at `Load` time. Locked in by `TestMetricsConfigValidateRejectsEmptyAddress` and `TestMetricsConfigOmittedSubstitutesDefault`.
- **Posture binding interpretation:** posture is the sixth Ring 1 source per `architecture.md:45` but it is read-on-demand, not streaming. It does NOT expose `source_healthy` (no continuous connection to lose; "healthy" is ambiguous for a request-response client) and does NOT expose `olaitan_sensor_events_total` (does not publish to `EVENTS.raw`). Instead it exposes the six counters from Story 1.11 (`PostureCacheHits`, `PostureCacheMisses`, `PostureCacheBypasses`, `PostureAPIErrors`, `PostureOrphanPods`, `PostureUnavailable`) as `olaitan_sensor_posture_*_total`. When `posture.enabled=false`, a single `olaitan_sensor_posture_disabled` gauge (value 1.0) is registered instead so dashboards distinguish "intentionally off" from "broken".
- **Helm wiring:** the chart introduces a top-level `metrics:` values block with three knobs (`address`, `containerPort`, `scrapeAnnotations`). The DaemonSet and Deployment templates gain a `metrics` named containerPort (`9090` by default) and three `prometheus.io/{scrape,port,path}` pod annotations gated by `scrapeAnnotations`. The `configmap.yaml` bridge propagates `metrics.address` into the rendered `olaitan.yaml` via the Story 1.10 P28 `mustRegexFind + regexReplaceAll` pattern; four new helm tests (`TestMetricsContainerPortPresent`, `TestMetricsAnnotationsPresentByDefault`, `TestMetricsAnnotationsAbsentWhenDisabled`, `TestMetricsBridgeRendersAddress`) lock the chart contract in. A `ServiceMonitor` resource is left as a future Prometheus-Operator-aware deployment follow-up tracked in `deferred-work.md`; a `:9090` ingress NetworkPolicy template lands with the cluster-wide policy story in Epic 6.
- **Hand-offs:**
  - **Story 1.13 (rate-limit circuit breaker)** will register `olaitan_sensor_circuit_breaker_engaged_total{source,node}` via the same `metrics.Registry.RegisterCounter` API; the `source` label values match the five-source enum landed here.
  - **Story 1.14 (correlator)** will register `olaitan_correlator_trigger_to_publish_seconds` (histogram, bucket boundaries match the 60s sliding-window + 100ms NFR2 ceiling envelope) and `olaitan_correlator_evidence_packages_total{trigger}` via the same Registry.
  - **Story 1.18 (deterministic detection observability)** will register the rule-engine and baseline-engine surfaces under the same naming convention (`olaitan_<ring>_<metric>{labels}`) and registry-isolation pattern.
  - **Story 3.15 (LLM observability)** will register `olaitan_llm_*` metrics with a `provider` label per NFR32 cardinality bounds.
- **External dependency:** `github.com/prometheus/client_golang v1.23.2` was already present as an indirect dep (pulled in via the controller-runtime dependency graph from Story 1.11). Story 1.12 promotes it to a direct dep via the explicit import.
- **ADRs:** none. The metric naming convention (`olaitan_<ring>_<metric>{labels}`) lives in `architecture.md:374`; the binding interpretations above resolve the AC-vs-codebase asymmetries inline.
- **eval_run_ids rationale:** infrastructure substrate; production evaluation in Epic 5 once the detection surfaces (Stories 1.18, 3.15) populate the metric families.

### `c3.4-workload-posture-client`

- **Story:** 1.11 (On-demand workload posture client).
- **Merge SHA:** `98a643a` (PR #20, merged 2026-05-13). Story 1.11 round-1 follow-up commit `2a03d2c` ("Story 1.11 follow-up: address Copilot + bmad-code-review on PR #20") applied 20+ patches (8 Copilot + 12 reviewer findings across Blind Hunter / Edge Case Hunter / Acceptance Auditor layers) on top of the main commit `6207c0e`; round-2 follow-up commit `a7959c1` ("Story 1.11 follow-up: address round-2 Copilot review on PR #20") applied 7 additional Copilot patches. Both follow-up commits are covered by this row.
- **PR:** #20 (merged 2026-05-13).
- **FRs/NFRs:** FR7 (satisfied; `internal/collector/posture/posture.Client` returns a `*schema.WorkloadPosture` populated with RoleBindings, ClusterRoleBindings, applicable NetworkPolicies, pod-level PodSecurityContext, and per-container SecurityContext summaries from a single synchronous K8s-API fan-out per `Get`). FR11 (informed; the posture struct travels inside `EvidencePackage.workload_posture` at correlator assembly time; Story 1.14 wires the consumer). FR13 (informed; the owner-resolver walks `Pod → ReplicaSet → Deployment` or `Pod → Job → CronJob` at most two K8s API hops per architecture's read-on-demand cost budget, with orphan-pod fallback to `<namespace>/Pod/<pod-name>` per `architecture.md:457`. Story 1.10's `pod_name_kind:generatename` tag is the upstream hint consumed by the correlator before calling `Get`). NFR10 (informed; the in-memory TTL cache is unbounded by entry count under the O(100)-workloads design assumption; at ~2 KiB per posture struct the cache costs ~200 KiB inside the 1 GB aggregator budget). NFR12 (informed; the new aggregator ClusterRole rules grant `get, list` on `rolebindings`, `clusterrolebindings`, `roles`, `clusterroles`, `networkpolicies`, `deployments`, `replicasets`, `statefulsets`, `daemonsets`, `jobs`, `cronjobs`, `serviceaccounts` per `architecture.md:257` -- no `watch`, no wildcards). NFR15 (satisfied; the `UnavailableReason` field on `WorkloadPosture` carries one of four classes (`transient`, `permission_denied`, `not_found`, `permanent`), never the raw K8s API error text; the full err string is logged via `slog.Error` with `error_class` keyed but never embedded in the LLM-bound payload). NFR35 (satisfied; ≥80% line coverage on `internal/collector/posture/`; integration tests use `sigs.k8s.io/controller-runtime/pkg/envtest` (real apiserver + etcd, no kubelet) per `architecture.md:389`). NFR36 (satisfied; integration tests exercise the real typed clientset against a real apiserver -- no mock-only crossings of the K8s-API ring boundary).
- **Architecture reconciliation:** `WorkloadPosture` is defined in `internal/schema/posture.go` (new file, per `architecture.md:728`). The new `internal/keys/workload.go` builders produce the canonical `<namespace>/<owner-kind>/<owner-name>` form (URL-encoded) per `architecture.md:457`, with `<namespace>/Pod/<pod-name>` for orphan pods. The aggregator-vs-controller naming asymmetry is preserved: the codebase uses `aggregator` as the Helm name (`olaitan.aggregator.serviceAccountName`); `architecture.md:257` uses `olaitan-controller` for the same Pod role. The new RBAC rules extend the existing aggregator ClusterRole.
- **First-`client-go` entry cost:** Story 1.11 is the first story to introduce `k8s.io/client-go` and `sigs.k8s.io/controller-runtime` to `go.mod`. The actual pins shipped are `k8s.io/api v0.36.0`, `k8s.io/apimachinery v0.36.0`, `k8s.io/apiserver v0.36.0`, `k8s.io/client-go v0.36.0`, and `sigs.k8s.io/controller-runtime v0.24.1`; `k8s.io/cri-api v0.29.10` is retained from Story 1.8 for backward compatibility with the containerd CRI adapter's protobuf surface. These versions deviate from the story spec's pre-implementation pin (v0.29.10 / v0.17.6); the upgrade was applied during implementation when `go mod tidy` resolved the upstream graph and the older pins were no longer reachable. The CI integration-test job needs `setup-envtest` (pinned to `release-0.24` in the Makefile to match controller-runtime) and the `bin/k8s/` apiserver+etcd binaries; the Makefile gains the `envtest-bin` target codifying the one-shot setup. The `ENVTEST_K8S_VERSION` default remains `1.29.x` for the apiserver+etcd image set; bumping to 1.36.x to match the client surface is a follow-up tracked in deferred work.
- **Read-on-demand only:** the package does not use the informer cache (`k8s.io/client-go/informers` or `controller-runtime/pkg/cache`). Every cache miss triggers a fresh K8s API round trip. Continuous watching is explicitly out of scope per `architecture.md:324`; the 60s in-memory TTL covers the freshness budget. Story 3.x's LLM-eligible fresh-fetch path consumes `posture.WithBypassCache()`.
- **Two-API-call ceiling:** the owner-resolver walks at most two K8s API hops per pod (RS → Deployment OR Job → CronJob). Deeper walks return the deepest resolved level rather than continuing.
- **Error redaction:** the `UnavailableReason` class never includes the raw err.Error() text. The four classes (`transient`, `permission_denied`, `not_found`, `permanent`) are the only values the LLM-bound payload sees. Locked in by `TestIntegration_UnavailableReasonStaysClassedNotRaw`.
- **Helm wiring:** the `posture:` values block introduces `enabled` (default true), `cacheTTL` (default 60s; ceiling 60s enforced via `fail` in `templates/configmap.yaml`), and `fetchTimeout` (default 5s). The aggregator ClusterRole gains the read-only RBAC rules per `architecture.md:257`. The configmap bridges all three knobs into the rendered `detection.posture` block via the Story 1.10 P28 per-knob `mustRegexFind + regexReplaceAll` pattern.
- **eval_run_ids rationale:** infrastructure substrate; production evaluation in Epic 5 once the correlator (Story 1.14) consumes the posture client.

### `c3.4-containerd-cri-adapter`

- **Story:** 1.8 (containerd CRI lifecycle adapter).
- **Merge SHA:** `b1a80c4` (PR #17, merged 2026-05-08). Story 1.8 follow-up commit `97728e2` ("Story 1.8 follow-up: address bmad-code-review on PR #17") applied 28 patches (2 decisions resolved + 26 quality patches) on top of the main commit `8da123d` and is covered by this row.
- **FRs/NFRs:** FR3 (satisfied; the adapter ingests containerd CRI `ContainerEventResponse` messages via `RuntimeService.GetContainerEvents` server-streaming RPC and produces canonical `schema.Event` records of `Source=SourceRuntime` + `Category=CategoryLifecycle`). FR6 (satisfied; per-event publish to `subjects.RawRuntime` via `jetstream.WithMsgID(ev.ID)` for at-least-once with server-side dedup over the JetStream 2-minute window). FR8 (partially satisfied; the in-process `sourcehealth.Tracker` is wired and Story 1.12 will bind `Adapter.Health()` to the Prometheus gauge `source_healthy{source="runtime"}`). FR18 (prepared; the adapter emits `attempt:N` tags for sandbox restarts so the Welford engine in Story 1.17 can invalidate per-workload baselines on restart -- the consumer logic is owned by Story 1.17). NFR1 (informed; the bench `BenchmarkAdapter_PublishLatency` measures p50/p99 receive→PubAck on a real embedded NATS server with the AC3 50ms p99 gate baked in via `b.Fatalf` -- production-class measurement under load awaits Story 5.1 eval harness). NFR11 (informed; the host-socket mount is documented in `deploy/helm/olaitan/CRI.md` as privilege-equivalent to the existing Falco socket mount; threat model unchanged from the post-Story-1.6 baseline). NFR35 (satisfied; 85.1% line coverage on `internal/collector/cri/`, measured on the unit-test build only -- integration paths exercised separately under `-tags=integration` and not included in the headline figure -- with the integration test substrate using real boundaries (embedded NATS server + bufconn-backed real `runtime.v1.RuntimeService` gRPC server)). NFR36 (informed; integration test exercises the real protobuf decoder, the real JetStream publish path, and the real CRI gRPC client -- only the runtime-service implementation is in-process via bufconn, mirroring Story 1.6's Falco pattern. Kind-cluster e2e test deferred per Story 1.8 Task 7.6 to a future hardening pass).
- **Architecture / naming reconciliation:** The story epic and ACs read "source containerd / category lifecycle". The schema package (`internal/schema/event.go`) was bootstrapped with `SourceRuntime = "runtime"` (and `CategoryLifecycle = "lifecycle"`) so the adapter axis identifies the architectural layer rather than the specific implementation -- a future CRI-O adapter would publish on the same `RawRuntime` subject. The binding interpretation: `SourceRuntime` + `CategoryLifecycle` satisfies the AC's intent, and downstream correlators read by `Source` rather than by implementation, so the polymorphism is preserved. AC2's "payload includes the workload identity (`namespace/owner-kind/owner-name`)" is satisfied by populating `PodRef{Name, Namespace, UID, Node}` from `pod_sandbox_status.metadata` -- the canonical workload-id resolver (`internal/keys.WorkloadID`, lands in Story 1.11) computes `namespace/owner-kind/owner-name` from `PodRef` plus a K8s API call only at the moment workload-identity is needed, not in the adapter's hot path. Mirrors Stories 1.6 and 1.7.
- **ADRs:** none. The `SourceRuntime` vs `SourceContainerd` naming choice is documented in the package docstring (`internal/collector/cri/translate.go`) and in the PR description rather than as a separate ADR. The host-socket mount security boundary is documented in `deploy/helm/olaitan/CRI.md` rather than as a separate ADR; a future ADR may capture a `containerdSensor.runAsRoot` toggle if/when the chart adds one to lift the nonroot UID issue documented in CRI.md's troubleshooting section.
- **Watchdog design difference vs Stories 1.6/1.7:** CRI lifecycle events are sparse by design (a stable cluster with no Pod churn can go hours without an event). The staleness watchdog therefore only flips `MarkUnhealthy` when staleness AND a non-Ready connection state coincide -- "no events for an hour on a connected stream" is healthy. Locked in by `TestRun_StalenessWatchdog_QuietButConnected_DoesNotFlipUnhealthy`.
- **eval_run_ids rationale:** infrastructure adapter; the bench is for AC3 verification, not for the Epic 5 evaluation harness's per-scenario runs.

### `c3.4-calico-flow-spike`

- **Story:** 1.3 (Calico flow record export feasibility spike).
- **Merge SHA:** `ce9f9ca` (squash on `main`, 2026-05-01).
- **PR:** none (single-PR squash directly on `main`; ADR carries the durable record).
- **FRs/NFRs:** FR4 (informed, not satisfied; production wiring lands with Story 1.10).
- **ADRs:** ADR-2026-04-30-01 (Calico Goldmane gRPC API).
- **eval_run_ids rationale:** spike, pre-dates the Epic 5 evaluation harness.

### `c3.4-falco-syscall-adapter`

- **Story:** 1.6 (Falco gRPC sensor adapter).
- **Merge SHA:** `7d1d732` (PR #13, merged 2026-05-06). Story 1.6 follow-up commit `c358eed` ("Story 1.6 follow-up: address bmad-code-review on PR #13") landed alongside the main commit `3397791` and is also covered by this row.
- **FRs/NFRs:** FR1 (satisfied; the adapter ingests Falco gRPC syscall events and produces canonical `schema.Event` records of `Source: SourceFalco` + `Category: CategorySyscall`). FR8 (partially satisfied; the in-process `SourceHealth` tracker is wired and Story 1.12 will bind it to the Prometheus gauge `source_healthy{source="falco"}` per `architecture.md:946`). NFR1 (informed; the bufconn benchmark measured ~100 microseconds per event mean throughput, well inside the 50ms p99 budget; production-class measurement awaits Story 5.1 eval harness). NFR35 (satisfied; ≥80% line coverage on hand-written files in `internal/collector/falco/` and `internal/retry/`, integration test uses real boundaries (embedded NATS server + bufconn gRPC server)). NFR42 (satisfied; this row plus the back-filled `c3-traceability-bootstrap` row close the gate's first-PR onward contract).
- **ADRs:** none for the adapter itself. The Falco gRPC client choice (vendored protos at `internal/collector/falco/falcopb/` rather than the archived upstream `falcosecurity/client-go`) is documented in `internal/collector/falco/falcopb/README.md` rather than as a separate ADR; the deprecation of the upstream module is a pure circumstance, not a project decision worth ADR-ing.
- **eval_run_ids rationale:** infrastructure adapter; the bufconn benchmark is for AC3 verification, not for the Epic 5 evaluation harness's per-scenario runs.

### `c3.6.1-sigma-parser-spike`

- **Story:** 1.2 (Sigma parser strategy spike + OLT dialect spec).
- **Merge SHAs:** `c9acb45` (PR #8, initial spike, 2026-04-29) and `3b4bc34` (PR #9, Round 1 audit follow-up, 2026-04-30).
- **FRs/NFRs:** FR14, FR15 (informed, not satisfied; production engine lands with Story 1.15).
- **ADRs:** ADR-2026-04-28-01 (Sigma parser strategy: wrap upstream `sigma-go` + thin OLT validator).
- **eval_run_ids rationale:** spike, pre-dates the Epic 5 evaluation harness.

### `c3.7.4-criu-checkpoint-spike`

- **Story:** 1.4 (CRIU forensic checkpoint feasibility spike).
- **Merge SHA:** `205d7ff` (PR #11, 2026-05-04T08:10:23Z).
- **FRs/NFRs:** FR36 (informed, not satisfied; documented infeasible on the pinned containerd 1.7 runtime so Story 4.2 ships the kubectl-checkpoint Path A fallback by default).
- **ADRs:** ADR-2026-05-02-01 (CRIU infeasible on containerd 1.7; Path A default in Story 4.2).
- **eval_run_ids rationale:** spike, pre-dates the Epic 5 evaluation harness.

### `c3.8-helm-chart-skel`

- **Story:** 1.1 (Helm chart skeleton and cluster bootstrap, brownfield carry-over of the v1 Story 1.7 work).
- **Merge SHA:** `d269861` (PR #7, 2026-04-27).
- **FRs/NFRs:** FR46, FR47, NFR13 (satisfied).
- **ADRs:** ADR-2026-04-27-01 (Calico v3.29.0 reconciled), ADR-2026-04-27-02 (single distroless base image), ADR-2026-04-27-03 (Bitnami Redis OCI registry, accept current path with `Chart.lock` committed).
- **eval_run_ids rationale:** deployment substrate, pre-dates the Epic 5 evaluation harness; the harness will reference the chart by Helm release name, not by `eval_run_ids`.

### `c3.7.x-operator-override-pin` / `c3.7.x-operator-override-ttl`

- **Story:** 2.7 (operator override annotations and TTL, FR38/FR39).
- **Merge SHA:** TBD (back-filled post-merge by the `chore: backfill Story 2.7 merge SHA in traceability matrix` commit).
- **FRs/NFRs:** FR38 (satisfied; an `olaitan.io/state-override` annotation pins the FSM and defers ThreatScore transitions via the in-fsm `Pin`/`ReleasePin` seam consulted by `Evaluate`). FR39 (satisfied; the `override:{workload_id}` Redis key carries a native TTL equal to the requested duration and the poll/reconcile releases the pin on native-TTL expiry or manual annotation removal, then resumes score-driven control). NFR35 (touched-package coverage held: override 82.5%, fsm 84.5%). NFR8 (the controller's dedicated Redis client requires `REDIS_PASSWORD` AUTH).
- **ADRs:** none (the two load-bearing decisions, poll-over-watch observation (BI-1) and the in-fsm `Pin`/`ReleasePin` seam (BI-2), are recorded as Binding Interpretations in the story artefact rather than as separate ADRs).
- **eval_run_ids rationale:** response-ring control-plane behaviour, not an Epic-5 evaluation-bearing claim.

## Conventions

- **`claim_id` grammar.** `c<chapter>.<section>[.<sub>]-<short-slug>`. The slug
  is lowercase, ASCII, hyphen-separated, and short (target ≤30 characters).
  It identifies the claim, not the implementing artefact, so a refactor that
  moves a package does **not** churn the `claim_id` (only `code_package`
  changes). Slugs are write-once and never renamed; if a claim is retired,
  the row is annotated and kept for audit, not deleted.
- **`eval_run_ids: n/a`.** Used for substrate (Helm chart, NATS subjects,
  Redis schema), infrastructure (CI gates, build pipelines), and feasibility
  spikes that pre-date the Epic 5 evaluation harness. Once the harness lands
  (Story 5.1), every row whose claim has empirical numbers in Chapter 4
  must back-fill this column with the canonical run identifier(s).
- **One ADR per row when one applies.** ADRs are recorded in
  `docs/deferred-decisions.md`. The matrix references them by ID
  (`YYYY-MM-DD-NN`) in the *Provenance* annex above. Multiple ADRs are
  comma-separated.
- **Sorting.** Rows are sorted by `claim_id` lexicographically. This makes
  the table byte-stable across re-renders and gives a natural §-order layout
  because the chapter prefix sorts first.
- **Provenance discipline.** Every new row added by a PR adds a
  *Provenance* sub-section (### `claim_id`) carrying the PR number, merge
  SHA, FRs/NFRs satisfied or informed, and any ADRs. The `traceability` CI
  gate does not parse this annex; it is for humans, but it is required by
  AC4 of the bootstrap story (Story 1.5) for every row.
- **Forward reference to NFR34.** Per NFR34, the operator runbook
  (`docs/runbook.md`, owned by Story 6.8) must include a sample matrix row
  in its on-boarding section so an operator copying the runbook can see the
  claim chain in situ. This file holds that obligation in trust until the
  runbook lands.
